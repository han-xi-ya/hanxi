# WSL 模块改造进度存档

存档日期：2026-09-08
状态：调研未完成，尚未开始业务代码改造。

## 1. 任务与已确认范围

用户要求分析本地参考项目 `E:\System\桌面\工具\wsl-dashboard`，据此改造当前 Hanxi 的 WSL 模块。

用户已明确选择：**界面与核心功能一起改**。

具体含义：
- 对比两个项目的实际实现，重整 WSL 工作台，补齐实用管理能力。
- 不以完整照搬参考项目、逐项对齐所有功能为目标。
- 保持 Hanxi 现有架构及工作台设计语言。

**注意：用户尚未批准具体功能清单或实现方案。选择改造方向不等于批准系统配置变更或危险操作。**

## 2. 当前进度

整体粗估约 **10%–15%**，该比例仅为沟通估算，并非按已确认工作项计算。

### 已完成

- 确认上述改造方向。
- 加载 `hanxi-workbench-ui` 技能，并读取：
  - `.claude/skills/hanxi-workbench-ui/references/design-system.md`
  - `.claude/skills/hanxi-workbench-ui/references/component-patterns.md`
  - `.claude/skills/hanxi-workbench-ui/references/quality-checklist.md`
- 执行 `git status --short` 检查工作区。
- 建立初步计划草稿：
  `C:\Users\Administrator\.claude\plans\floating-tumbling-candle.md`
- 启动两个只读分析任务，分别分析参考项目和 Hanxi 当前 WSL 模块。

### 中断情况

两个分析任务均被用户中断，**主会话没有收到完整分析报告**。

因此，目前没有可直接采用的、经过核实的：
- 参考项目功能清单与许可证结论；
- 两个项目的功能差异表；
- WSL 核心文件与函数定位报告；
- 具体改造设计与测试方案。

下次不能将“启动过分析”视为“分析已完成”，也不能凭空补齐子任务结果。

### 尚未开始

- 确定本期具体功能及非目标。
- 完整实现计划的评审与确认。
- 修改 WSL 前后端业务代码或绑定。
- 编译、自动化测试及真实应用验证。

## 3. 工作区与已创建文件

项目目录：`E:\System\桌面\工具\hanxi`

会话开始时分支为 `dev`。本次检查到的未跟踪内容：

```text
?? .claude/worktrees/
?? docs/PROJECT_REVIEW_2026-09-08.md
```

上述内容在本次检查时已存在，未清理、未纳入改造。下次开始前应重新检查 `git status`，不要假定工作区仍完全相同。

本次明确创建的文档：
- 外部计划草稿：`C:\Users\Administrator\.claude\plans\floating-tumbling-candle.md`
- 本进度存档：`docs/WSL_REDESIGN_PROGRESS_2026-09-08.md`

没有修改 WSL 业务代码，没有执行 commit 或 push。

## 4. 改造必须遵守的边界

- 使用现有 Go / Wails / Vue 3 / TypeScript 技术栈。
- 复用 Hanxi 全局 CSS token、组件、组合式函数与现有事件机制，不引入参考项目的整套运行时或第三方 UI 框架。
- 保留现有 WSL 安全与生命周期机制，重点核实并防止回退：
  - 发行版安装前的虚拟化装载检查；
  - 提权通道子进程退出码传播，不能将失败误报为成功；
  - 待重启状态与功能启用状态的判定；
  - 后台任务、事件订阅和视图切换的生命周期清理。
- 上述重点来自项目指令与会话提供的近期提交摘要，**仍需到当前源码验证具体实现**。
- 删除文件、覆盖重要内容、修改敏感配置及系统权限等操作须先确认。
- 不为测试擅自安装、停止、迁移或卸载用户的 WSL 发行版，不擅自更改系统配置。
- 不自动 commit / push，不处理无关未跟踪内容。
- 实现中如遇实际开发或运行陷阱，按项目规范沉淀至 `docs/TROUBLESHOOTING.md`；不要将尚未验证的猜测记录成事实。

## 5. 下次继续的推荐步骤

### 第一步：短范围核实与对比

1. 重新读取项目规范、检查工作区状态。
2. 只读分析 `wsl-dashboard` 的 README、许可证与实际功能入口，收敛最值得借鉴的 3–5 项能力。
3. 定位 Hanxi 当前 WSL 后端、前端视图、绑定、事件、测试和相关故障文档。
4. 核实已有能力，避免将现有功能当作待新增项。
5. 形成简短对比表：参考能力 / Hanxi 现状 / 是否本期实现 / 技术与安全影响。

### 第二步：提交可执行方案

- 提出推荐的页面结构和核心功能清单。
- 明确保留能力、排除项、关键修改文件、复用函数与验证方法。
- 核心模块改造前说明影响范围；涉及配置写入或危险操作的能力说明确认机制。
- 让用户确认具体方案后再开始实现。

### 第三步：实现与验证

- 按现有架构改造，合理拆分模块而非直接移植整套参考项目。
- 覆盖加载、空数据、错误、过期数据、忙碌、取消及部分成功等适用状态。
- 验证深浅主题、窄屏、长路径、键盘操作及页面切换后的清理。
- 使用仓库实际存在的构建与测试命令；这些命令尚未完成核实。
- 真正需要启动应用进行验证时加载 `run` 技能；需要可视化图表时先加载 `dataviz` 技能。
- 如测试失败或真实应用验证未执行，明确记录，不宣称全部验证通过。

## 6. 时间估算与沟通改进

此前向用户提供的粗略剩余时间：

| 阶段 | 粗估 |
|---|---:|
| 完成对比并提出具体方案 | 10–20 分钟 |
| 界面重整与核心功能实现 | 45–90 分钟 |
| 编译、测试与修正 | 20–40 分钟 |

合计约 1–2.5 小时，仅供参考。具体范围未确定，不能视为交付承诺；真实安装、迁移等验证可能需要更久和额外授权。

本次调研推进不够紧凑、进度反馈不足。下次应优先给出经过核实的差异清单与本期建议，避免长时间泛泛扫描；分阶段汇报实际产出，而非只更新任务状态。

## 7. 可直接使用的续接指令

> 阅读 `docs/WSL_REDESIGN_PROGRESS_2026-09-08.md`，继续分析 `E:\System\桌面\工具\wsl-dashboard` 并改造 Hanxi 的 WSL 模块。方向已确定为“界面与核心功能一起改”，但具体方案尚未确认、业务代码尚未修改。请先核实两个项目的实际实现，给出差异清单和推荐改造计划供我确认；保留已有 WSL 安全机制，不操作真实发行版或系统配置，不自动 commit / push。
