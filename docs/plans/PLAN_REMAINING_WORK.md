# Hanxi 剩余工作排期(项目暂停基线)

> **文档状态**:唯一复入口。项目于 2026-09-20 暂告一段落,恢复工作时以本文为起点,不必再跨多个文档自行拼接现状。
>
> **基线**:`dev` @ `5773ff9`,工作区无未提交代码改动;Wave 0–5 全部落地;41 个业务模块;前端全量门禁绿(112 测试文件 / 1261 项 / `vue-tsc` / 生产构建 / ESLint 0 error)。并行会话(F1–F10、Wave 系列)的工作均已入库,经全 worktree 扫描确认**无任何未提交的代码欠账**。

---

## P0:代码质量修复(复进第一件事)

证据与逐条代码定位全部在 [CODE_QUALITY_REVIEW_PROGRESS_2026-09-19](../CODE_QUALITY_REVIEW_PROGRESS_2026-09-19.md),已暂停待批,恢复时按原批次执行、不重做审查:

| 批次 | 内容 | 关键落点 |
|---|---|---|
| 批 1 ✅ **已完成(2026-09-21,f63d9b2+c679764)** | Registry 覆盖状态数据竞态(值类型投影消灭指针逃逸);receipt 与 `known-modules.json` 成组缺陷(账本独立命名空间/独立 schema/三态保守重建/fail-closed 卸载事务;计划见 [PLAN_P0_BATCH1_FIX](PLAN_P0_BATCH1_FIX.md)) | `internal/extapi/registry.go`、`internal/settings/receipts.go` |
| 批 2 → **2a ✅ 已完成(2026-09-21,4725e52+5881c8e)**:journal 步进 fail-closed 闸门(降级拒新事务+journal-degraded 收口+GetJournalHealth 观察面);Artifact Commit 原子边界收口 rename(黑户隔离放行重装)。**2b 🔄 施工中(机主 2026-09-22 选案 B)**:ctx+租约一并买断。内核已落(`extapi.BackgroundLease`/`EnterBackground`+Registry 停用先 cancel 再 drain+`ops.BeginTxnWithLifecycle`/`Txn.Context/Cancel/Close/JournalFailed`+`artifact.UnpackZipContext` 可取消解包);**已迁移 19/19 journal 模块**(黄金样本 flclash/paseo;普通 zip 族 markeron/ccswitch/everything/litemonitor/ddnsgo/bcu/bili23/mangodisk/rufus/translucenttb;特殊形态 quicklook(bespoke extractAll ctx 化)/keyviz+piclite(MSI:提取器返回后与轮询边界收口,msiexec 不可强杀如实注释)/papertodo(降级下载链 ctx 化)/guoheview(MD5 bespoke 链 ctx 化)/recordly+vscode(安装器一旦拉起即走完,启动前审取消;双形态全链 ctx),全仓 build/vet/单测绿);**2b 遗留待办**:①~~下载中停用/退出三场景 service 级集成测试~~ ✅ 已落(2026-09-23,ops 层全链集成 `TestDeactivateCancelsInFlightTxn`:真 Registry+真 journal,断言停用先取消、operation-cancelled 收口、无 succeeded 假账、drain 放行;退出场景由 ShutdownAll 同构 cancelBackgroundLocked 路径覆盖,真机冒烟见 ② 后补跑);②`go test -race` Windows 侧补跑;③已知环境 flake 与本批无关:papertodo/instance(cmd.exe 秒退 #81 家族)、portscan(port+1 撞外部监听)(zip+Fetch 族为机械迁移;keyviz/piclite MSI、recordly/vscode 安装器、guoheview bespoke 需按各自取消边界处理,不伪造即时取消) | 迁移模板见 flclash service/manager 两文件;对比表见 [PLAN_P0_BATCH2_FIX](PLAN_P0_BATCH2_FIX.md) |
| 批 3 ✅ **已完成(2026-09-23,五子批 b3-a~e,计划见 [PLAN_P0_BATCH3_FIX](PLAN_P0_BATCH3_FIX.md))** | 4.1 stale 呈现(statusError/lastStatusAt+降灰灯)、4.2 本地未决不判空(localResolved)、4.3 清票走 sameVersionOf 互认、4.4 vscode/everything 自定义加载代次闸(状态/加载独立计数)、4.5 专属动作统一互斥(ddnsgo console/extras 卡/everything 写动作)、4.6 键盘可达(TabNav roving+方向键、progressbar 语义、重试转按钮)、§5-1 cancellable 诚实标注(仅 install/update 有 2b 真链)。**人工余项**:390px/200% WebView2 真机验收(归 P1 清单随 F6 重跑同场) | `frontend/src/components/managed/`、`adapters/vscode.ts`、`views/EverythingView.vue`、`ui/MainTabNav.vue`、`packages/go/operation/hub.go` |

- 每批开工前先形成正式修复计划确认范围;批 1、2 完成后补 `go test -race`(Windows 侧走 CI/真机;云侧已具备 hanxi-dev:2404 容器回路,43 个 Linux 可构建包可本地跑 race,Windows-only 包仍须 CI——配方与豁免清单见踩坑 #84)与组合级故障注入。
- 审查文档 §5 另有 9 项"已发现待复核/裁决"事项,随批次一并收口。

## P1:真机验收债

1. **F10 网页应用(webapp)冒烟**:task dev 开微信扫码、X 关窗后 WebView2 内存回落、收起 TTL 释放——唯一纯新未验项,成本最低,建议最先做。
2. **F6(存储目录)、F3(快照)、F4/F4b(MCP 与向导)、F7(OCR 托管)、F9(WSL USB)**:按 [PROGRESS.md](../PROGRESS.md) 既有人工清单补跑;**随 F6 加测批 3 人工余项**——共享托管面板/版本表在 390px 宽 + 200% 缩放下无溢出、TabNav 纯键盘(←/→/Home/End/Tab)可完成换选、NVDA/讲述人可读进度与"暂不可确认"标注。
3. **A 机 → NAS → 干净 B 机黄金路径**:先完整走一次手工验收(建立模块迁移等级:可携带/需重建缓存/需系统注册/需重填凭据/不适合临时电脑),据结果再决定机器绑定恢复提示与临时电脑模式是否立项。

## P2:文档与工程卫生

- [x] 三份未跟踪文档已随本排期一并入库:本文、CODE_QUALITY_REVIEW_PROGRESS_2026-09-19、FUTURE_FEATURE_RECOMMENDATIONS。
- [x] 卫生清理已执行完毕(2026-09-20,机主点名批准):28 个 worktree 目录(全 MERGED)注销删除,含 hubkit 旧名时代 3 个失效 locked 登记;已合并分支(`feat/f*`、`fix/q*`、`feat/r*`、`fix/nine-suite-quality`)与全部无主 `worktree-agent-*`  scratch 分支删除。现仓库仅存 `dev`/`main`/`pre-split-backup` 三分支、单一工作树。注:8 个 scratch 分支同指 hubkit v0.2.0 时期 release 合并点 `60aff0c`(不在任何 tag/远程/备份可达范围),删除后仅 reflog 可回捞(约 90 天窗口)。
- [ ] 事实漂移收口:README/PRD/PROJECT_STRATEGY 的 37/38 旧统计 → 41;旧数据根语义(`hanxidata` 标记、`%APPDATA%` 回退、旧 `data/` 兼容)→ 现行"exe 同级 + `hanxi.bind`",含 Release workflow 便携说明;BACKLOG 中 WebApp 状态更新。
- [ ] gofmt 闸门红在 8 个历史文件(`logging/everything/ocr/platform` 等):机主裁决——建议单开一次 `style(frontend)`/`chore` 性格式化提交,不与功能混淆。

## P3:已暂停批次(恢复与否由机主定)

- **前端打磨批次一**:审查与实施计划已完成,三路实现已停止且主工作区无本轮改动;恢复时直接重新并行实施,不必重做审查。

## P4:复进日新增(2026-09-20,机主点名,只登记不即修)

| # | 事项 | 现状与落点 |
|---|---|---|
| N1 | **snipaste 打开/退出异常**(机主真机报告) → **✅ 实施已落(4cfd83f,待真机复验)** | 模块无 OpenWindow 动词(仅 Launch);状态词表无 external——`noExternalProbe` 恒报"不在运行"(`internal/modules/snipaste/instance/instance.go`),管理边界=本会话启动的进程树。先真机复现定位,再定修复范围(含是否引入外部检测)。 |
| N2 | **everything 外部实例可检测但退不掉**(机主真机报告) → **✅ 实施已落(de27a5f,待真机复验)** | external 探测与信使唤窗可用;`Quit()` 在 external 态刻意不越权、只回指引(`internal/modules/everything/service.go` ~303 行,探针窗口类/互斥体通道拿不到 PID)。需补"外部 PID 取得(按进程名/窗口枚举)→ `-quit` 优雅退出或 WM_CLOSE"。 |
| N3 | **Wave 4X 复活评估:托管家族外部实例退出/唤窗策略统一** | 唤窗两方案并存(单实例信使:ccswitch/everything;按 PID EnumWindows:rustdesk/litemonitor/subnetdesk/rufus/flclash/bcu/guoheview);外部实例退出一律只指引。已知隐患:①`focusWindowsByPIDs` 族以 `IsWindowVisible` 判恢复,最小化窗仍带 Visible 标志→不 SW_RESTORE、置前台无效,guoheview 的 `IsIconic` 方案为标杆;②托盘/后台唤起需 `platform/windows.SetForegroundForce` 借前台特权,族内多用裸 `SetForegroundWindow`。收口"是否越权强杀外部实例"决策后按模块推开,吸收 N1/N2 结论。 |
| N4 | **RAMMap64 托管集成** → **✅ 代码收口(2026-09-23,机主拍板路线 A 托管本体,待真机验收)**(微软 Sysinternals 内存释放) | 单文件绿色 exe,源 `download.sysinternals.com/files/RAMMap.zip`(非 GitHub,借用 integrate-github-tool 托管模式但换下载源);一次性"清空待机列表"语义、需提权、非常驻 GUI,形态近 recordly/paseo 单目录家族,提权与退出语义开工前正式计划确认。 || N4 | **RAMMap64 托管集成**(微软 Sysinternals 内存释放) | 单文件绿色 exe,源 `download.sysinternals.com/files/RAMMap.zip`(非 GitHub,借用 integrate-github-tool 托管模式但换下载源);一次性"清空待机列表"语义、需提权、非常驻 GUI,形态近 recordly/paseo 单目录家族,提权与退出语义开工前正式计划确认。 |
| N5 | **quickmenu 右键轮盘交互改进**(机主:"实在是不好用") | 借鉴 GitHub 开源项目 starpie(登记日检索未定位到确切仓库,开工先核实链接)。落点 `QuickMenuPopup.vue`/`useWheelRingState.ts`/`wheelGeometry.ts`/`internal/modules/quickmenu/`;既有欠账:前后端几何不同源(512 DIP vs 376 viewBox,见 FEATURES_OVERVIEW 与踩坑 #50)、触发时长/容差为编译期常量。先出改进分析+实施计划,再动代码。 |
| N6 | **msgboard 桌面留言板 UI 重设计**(机主:不喜欢、不友好) → **✅ 实施收口(2026-09-23,牌面观感待真机验收)** | 走 hanxi-workbench-ui 设计语言评审;落点 `MsgBoardView.vue` 与留言板组件族,`internal/modules/msgboard/` 后端契约尽量不动。先出方案给机主过目再实施。 |
| N7 | **飞牛同步客户端托管集成**(标准安装形态) → **⏸️ 机主裁决暂停(2026-09-23):CDN 三种客户端均 403,取证成本>收益,先放下;侦查结论留存下行,若重启从"机主浏览器下载 setup.exe 交卷"接上** | 源 `fnnas.com/download?key=fn-sync-client`;机主明确不能免安装,须走安装型先例(recordly NSIS 静默安装、vscode installer 双形态)。开工前确认:静默参数、版本提取、升级语义(覆盖/先卸后装)、且本机正在履行同步义务的实例按 N3 终裁登记为 `confirm_force` 档(优雅退出优先,强杀须 UI 明示"传输中"并确认,不闷头杀)。缘起级用途(MOTIVATION:NAS 同步随身),优先级建议靠前。 | **侦查结论(离线,部分实证)**:①下载页 JSON 实证当前版 `0.2.3`,Windows 直链 `iso.liveupdate.fnnas.com/pc/fn-sync_0.2.3_x64-setup.exe`(另有 macIntel/macApple dmg,无 Linux 桌面包);②`fn-sync` 系 Tauri 应用(命名 `<name>_<ver>_x64-setup.exe` 是 Tauri v2 NSIS 打包器签名产物——非 Inno,与 recordly NSIS 先例同族但 Tauri 有自己的静默参数约定);③**版本提取无需 GitHub API**:直链文件名即含版本,或解析 download 页 JSON 的 `version` 字段(everything 网页解析+快照兜底先例);④**卡点**:该 CDN 对非国内/数据中心 IP 返 403(`X-Error-Info: typeC`,四种 UA/Referer/Range 全拒),离线无法下载二进制做静默参数实证——按 skill"无实证不猜参数"铁律不预写 NSIS 解码链。**需机主侧一步取证**:在你本机(能正常访问 fnnas)浏览器下载该 setup.exe 或 `curl -sIL` 存 headers,或授权我在你网络环境跑一次下载,再定 `-S /当前用户静默` 具体参数与退出码族。
| N8 | **WindTerm 托管集成** → **✅ W1 复核更正:无 7z 门槛;W4 实施再更正:上游全量无官方摘要,须无摘要降级链(非零改动)** | 仓库 `github.com/kingToolbox/WindTerm`,走 integrate-github-tool 托管模式。**登记时"7z 绿色包"有误:GitHub API 全量核对,Windows 资产均为 `*_Windows_Portable*.zip`**(仅 2020 v1.1 带过历史 7z)。稳定版已至 2.7.0(2025-03)+Prerelease 线(原"2.6.1 稳定/2.7.0 beta"表述过期),channel 适配按 paseo/recordly 先例;Qt 单 exe 无信使→唤窗按 PID EnumWindows(guoheview IsIconic 标杆)、退出 WM_CLOSE+JobObject 兜底不变。⚠️ `windterm.cn` 为第三方网盘站(自动化只走 GitHub releases);官方称 Apache-2.0 但仓库根缺 LICENSE 文件,集成文档留备忘。 |
| N9 | **Termora 托管集成** → **✅ 代码收口(2026-09-23,待真机验收)** | 仓库 `github.com/TermoraDev/termora`(机主指定),同技能同模式。jpackage 双发布型:zip(内嵌 JRE 绿色,优先收编版本目录)与 exe 安装器(若采则归 N7 安装型先例族);Java 应用注意子进程树收口。与 N8 同类,建议同批实施、互相抄验。 |
| N10 | **借鉴 MooTool 系统信息功能**(自研新模块) | 参照 [rememberber/MooTool](https://github.com/rememberber/MooTool) 的系统信息工具:CPU/内存/磁盘/主板/显卡/OS/网络等软硬件档案一览。借鉴纪律沿 `docs/MOOTOOL_ANALYSIS.md`(学机制不抄实现);Go 候选 gopsutil 或 WMI/syscall 直采,开工定。划界:envcheck=开发工具链、litemonitor=托管第三方实时监控,本模块=静态软硬件档案,独立模块或并入 envcheck 家族开工定。 |
| N11 | **数据根占用可视化**(hanxidata 每子文件夹大小一览) | 数据根解析走 `internal/settings/paths.go` 现成通道(同级 `hanxidata/` 或 `hanxi.bind` 绑定);每个一级子文件夹大小=每便携软件磁盘占用,一眼看清。要点:walk 统计缓存+手动/后台刷新、巨型目录(Everything 索引/快照库)限时渐进、紧凑排序列表走 hanxi-workbench-ui;挂设置页「存储目录」分区还是独立模块开工定。 |
| N12 | **litemonitor 简介补「释放内存」能力**(机主指出) → **✅ 已落(f41f1a7)** | 上游 LiteMonitor 支持释放内存,hanxi 模块简介未写。定位 catalog/视图简介文案,对照上游功能清单核实后补齐,顺带排查其他"已支持未宣传"缺项;纯文案低风险可插空。与 N4(RAMMap 内存释放)同主题,文案口径可同批收口。 |
| N13 | **托管版本列表显示全平台安装包并标注形态** | 现 release 被过滤到只剩当前 Win 机器可用包(以 flclash 为例);改为展示 Windows/macOS/Android APK(Linux 可不要),逐包写清**便携版/标准安装版**。三处落点:①各模块 `version` 包平台过滤改"全量返回+平台标注";②共享契约 `ManagedReleaseRecord`(现仅 version/published/size/isPre)扩平台/形态/URL 字段,Wave 5 版本面板加平台与形态 chip——**跨模块契约变更,开工前出正式计划评审**;③非 Windows 资产只给"浏览器打开下载",不进 hanxi 下载/设使用链路。 |
| N14 | **envcheck 扩展:环境本体+依赖目录占用显示** | 现只报"装了什么/什么版本",看不到空间家底:①每个环境**本体安装目录**大小(Go SDK、Node、JDK、Python、.NET 各占多大);②依赖/缓存目录位置与大小(Go `GOPATH/pkg/mod`/GOCACHE、npm 全局包与缓存、pip/Maven `~/.m2`/Gradle/NuGet)。落点 `internal/modules/envcheck/` 检测器补双目录推导(env var→默认路径→工具命令三级回退)+`EnvCheckView` 展示"本体/依赖家底"两列。**与 N11 共用同一 walk 目录统计公共件,两项同批:先下沉公共包再各自接线**(实践"新功能第一天走公共包"纪律);UI 走 hanxi-workbench-ui。 |
| N15 | **评估:OCR 与 Snipaste 结合**(机主:"不能就算了") → **✅ 已定:可行,免费路径可落地,W1 调研收口** | 核心机制=Qt 单实例命令行通道(`Snipaste.exe snip -o 文件` 免费,外部实例同样可达),hanxi 目录监听落盘文件接现有 `ocr` 模块;PRO 另有 exec/`ocr_clipboard`/`--block` 增强。边界:目标提权运行被 UIPI 拦→降级指引;免费版无官方退出命令(`exit` 属 PRO)。详见 [PLAN_W1_DECISIONS §2](PLAN_W1_DECISIONS.md)。 |
| N16 | **随手记(memo)演进:路线已定 A→B→C**(首轮沟通 2026-09-20 完成) | 机主四类痛点全选(怕丢/记不快/找不回/用不上)→ 完整升级,三批次:**A** 文件库化+空闲静默 git 快照(无 git 降级普通备份,底稿 `PLAN_SNAPSHOT.md`,与 F3/N7 飞牛同步天然合流)→ **B** 全局热键快记+悬浮速记卡+全文/模糊检索 → **C** MCP/CLI 联动深化与导出(依赖 A/B 形态)。每批开工前出实施计划;后续批次细节沟通随批进行。 |
| N17 | **TranslucentTB 跨机环境脆弱**(机主真机报告 2026-09-23,只登记不即修) | 现象:旧机"之前合适、后来坏了",换本机(26200)即正常——二进制未坏,坏的是环境匹配。三条嫌疑待定位:① portable 依赖系统已装 WinUI 2.8/VCLibs 框架包,旧机可能被系统更新重置/精简掉(模块头注释已记此依赖);② OS 大版本更新(如 23H2→24H2)致所用 TranslucentTB 版本失效(上游任务栏 shell 变更有失效史);③ 旧机若手动双击起过外部实例(持单实例互斥体),接管语义与托管启动不同。**待机主补事实**:坏的具体症状(任务栏完全不透明/exe 没反应/面板与现态不符)、旧机破坏时段前后的 Windows 更新历史、两机托管版本号差。定位后:若①,启动前加框架包探针(`Get-AppxPackage Microsoft.UI.Xaml.2.8`/VCLibs.UWPDesktop)缺件如实预告并引导补装,不静默拉起注定不透明的进程;若②,沉淀踩坑并评估版本兼容下限提示 |
| N18 | **WSL 本机发行版页签界面问题**(机主报告 2026-09-23,只登记待细化) | 现象待补:机主报"本机发行版"页签界面有问题,具体症状未描述(布局溢出/状态错位/操作按钮异常?)。嫌疑落点近期改造面:32f3c15 本机发行版独立标签页与行内操作面重排、1287be6 W1–W9 交互整改与页签合并——多轮重排后回归风险高。待机主给症状与截图/屏录后定修复范围,纯前端问题优先随真机验收并段处理 |
| N19 | **设置「存储目录」分区界面优化**(机主报告 2026-09-23) | 机主点名优化存储目录设置分区界面,具体痛点待补(疑与数据根占用一屏总览 W3/N11 观感相关)。落点 `frontend/src/views/settings/StorageSection.vue`。建议走 hanxi-workbench-ui 设计语言评审后重排,与 P3 前端打磨批次、W4 各真机验收同窗并流 |
| N20 | **OCR「识别历史」弹窗界面优化**(机主报告 2026-09-23:"不够友好") | 落点 `frontend/src/views/OcrView.vue` 内自取数的历史弹窗(约 511/790 行起,Teleport 遮罩语系外壳,非公共 HistoryPanel 组件,改动无波及其他接入模块)。"不友好"具体痛点待机主补(条目密度?时间呈现?回填交互?空态?),建议走 hanxi-workbench-ui 重排;可顺带评审是否值得收编回统一 HistoryPanel 口径。纯前端,随真机验收并段 |
| N21 | **系统信息页(N10)两处显示缺陷**(机主报告 2026-09-23) | ① **容量单位恒 MB 不换 GB/TB**:根因实证——视图走共享件 `utils/format.ts::fmtSize`,上限即 MB(`>1MB → 一位小数 MB`),16GB 内存显示成 "16384.0 MB";**改共享件有波面**(BCU 等托管家族视图共用,且 `format.spec.ts` 明钉 "1024 KB/4.0 MB 现实现口径"),方案二选一:全局加 GB/TB 档(改契约,顺带全家族受益)或 sysinfo 私有 auto-scale 件(零波面);建议全局档+改钉规格,开工时机主定。② **网络接口不显示 IPv4**:后端 `collectNetwork` 用 `net.Interface.Addrs()`(理论含 IPv4)、前端已有「地址」列(`SysInfoView.vue` ~167 行 join 渲染)——链路看着都在,需真机复现断点:采集面真缺(如地址族/时序问题)还是 IPv6 淹没观感缺;若采集真缺,可下钻 `GetAdaptersAddresses`(Windows 原生含 IPv4 单播)或借 `Get-NetIPAddress` WMI 面补,并把 IPv4/IPv6 分列呈现 |
| N22 | **提权工具启动体验重做——双路并存+双向检测+友好话术**(机主诉求 2026-09-23,细化同日) | 现状:非提权 hanxi 启 RAMMap 被 740 特判拒→引导条只有一条路「以管理员身份重启整个 Hanxi」(ElevateRestart)。**机主已确认:两条路都要**——A「双击式」runas 只提权目标工具(`ShellExecuteEx runas`,系统 UAC 弹窗,hanxi 本体保持普通权限),B「提权重启」维持 ElevateRestart 全家提权。缺的不是通道是**判断与呈现**:①**双向检测**——"hanxi 是不是管理员"基建现成(ElevationStatus/#17);"**这个软件需不需要管理员**"新做:安装/导入时解析托管 exe 内嵌 manifest(`requireAdministrator`/`asInvoker`/`highestAvailable`,纯 Go 读 PE RT_MANIFEST)**自动探测**,模块显式声明可覆盖,启动 740 特判**兜底**(探测失真不误报);②**四象限决策**——不需提权→照常入 JobObject 托管;需提权+hanxi 已提权→直接托管起;需提权+未提权→友好选择弹窗双按钮(场景话术:一次性看数据选 A,常用水工具选 B);③**诚实呈现**——A 路起的实例是外部提权进程,进不了 JobObject、UIPI 下不可控退出,琥珀徽标「运行中·已提权(外部)」+ tooltip"这个实例 Hanxi 管不到,关窗请手动",绝不说"启动失败/管不了"这类吓人词;④文案纪律:740/JobObject/UIPI/manifest 等术语全部退居「技术细节」折叠区,主界面大白话+一句话场景判据。**落点**:runas 通道做成平台共享原语(「按需提权拉起」),RAMMap 首接、litemonitor(#17 同族)/Rufus/未来提权工具与 **N23 自研释放**复用,守"新功能第一天走公共包"纪律;manifest 探测件同批下沉(托管家族共用)。**版本管理面不受影响**,只改运行控制呈现。**随批影响**:N4 真机验收清单第②条需按双路新形态改口径 |
| N23 | **自研「一键释放内存」能力**(机主诉求 2026-09-23:RAMMap 之外,自研一个) | 与 N4 关系:N4 当年拍板只选了路线 A(托管 RAMMap 本体),**路线 B 当时已侦查完备**——方案/实施拆分/代码量估算(~400 行)全在 [PLAN_N4_RAMMAP §1/§3](PLAN_N4_RAMMAP.md),现按机主新诉求复活为独立排期项。与 A 不互斥:A 管看细节(完整版 GUI),B 管 hanxi 内**一键动作+释放量数字**。技术骨架(照方案):①`platform/windows/standby.go`——`SeProfileSingleProcessPrivilege` + `NtSetSystemInformation(MemoryPurgeStandbyList)` 单系统调用封装(数十年稳定,零第三方二进制);②提权走一次性子进程 runas + 临时文件握手回传(与 N22 的「按需提权拉起」共享原语同域,**两卡开工时并流复用,勿各造一套**);③sysinfo 服务挂 `PurgeStandby()` RPC(走调用门,返回前/后 AvailPhys+释放量+拒绝原因)+ 内存卡按钮 + 结果 toast。沿用 N4 旧拍板点 ②③:入口先页内还是同要托盘命令;释放前 UAC 弹窗属预期打扰的接受度确认。文案纪律:如实讲"清理可回收缓存换立即可用",不写"提速"营销话术 |
| N24 | **托管家族"导入本地目录"未对齐首装即立用**(机主报告 2026-09-23) → **✅ 已修(2026-09-23 自主批)**：17 家 ImportLocal 全补 `GetActive()==""` 守卫（rustdesk/subnetdesk 双值形、everything 通知前插），17 模块 52 包测试绿；service 级 DI 断言从略（守卫三连行同构，排期原文"可随批"非硬性） | 契约:"没有任何版本时,第一个到手的版本默认=使用版本"。核查实证:**下载链路 24/24 模块已统一**("未设使用版本时自动把刚下载完的版本设为使用",含 rustdesk/subnetdesk 双值形态);**导入链路只有新样本做了**——snipaste/termora/windterm/paseo/rammap 导入即立用,存量 **17 模块**(`bcu/bili23/ccswitch/ddnsgo/everything/flclash/guoheview/keyviz/litemonitor/mangodisk/piclite/quicklook/rufus/rustdesk/subnetdesk/translucenttb/vscode`)的 service.ImportLocal 直接 `return s.manager.ImportLocal(...)`,导入后不设 active(前端"使用中"徽标空,行为不一致)。markeron 无导入面、papertodo/frpc 无 active 版本概念(恒自动最新已装),均不涉及。修法:照 snipaste 样板在 17 个 ImportLocal 成功路径补 `GetActive()==""` 守卫(rustdesk/subnetdesk 用 `(info.Version, version.FormPortable)` 双值),机械改动一次扫过;行为无单测钉(现有测试未覆盖守卫层),可随批给 1~2 代表模块补 service 级断言 |
| N25 | **托管下载不吃系统代理——代理链下沉公共件**(机主提问引出的排期 2026-09-23) | 现状实证:托管家族的版本 API/资产下载用裸 `http.Client`(含 `packages/go/artifact` 内核,均 `ProxyFromEnvironment`)——**只认环境变量代理**;Clash/v2rayN 默认"系统代理"只写 WinINET 注册表,Go 不读→浏览器能上、hanxi 直连(用户体感"开着代理还是慢/挂")。现仅两条缓解:①21 模块 gh-proxy/ghproxy.net 镜像前缀降级(绕代理但非真走用户代理);②**wsl/netx 代理链(env→WinINET 兜底→直连)是孤例**,仅 WSL 模块与 ocr 注释引用。修法:按踩坑 #35 既定口径把 netx 泛型化为公共件(`packages/go/netx` 或 platform 层),托管家族 remote/downloader 与 artifact fetch 统一接入(PAC 形态不猜、如实直连;env 命中优先保持);收益:用户"系统代理开着"即全站(含 GitHub 直链)同进退,镜像降级退居真·兜底。注意 #35 坑中坑:`http.ProxyFromEnvironment` 按进程缓存环境,单测用注入链不碰真 env。**机主 2026-09-23 扩面(痛点实报:经常下载失败)**:凡 HTTP(S) 出网一律接代理链——托管下载/版本 API/更新感知、publicip 等在线查询、各模块网络探测的 HTTP 探针全量收编,不留裸连孤岛;如实边界:ICMP ping/端口扫描/LAN 发现属原始协议,HTTP/SOCKS 代理帮不上,探测器按协议决定能否走链并如实标注 |
| N26 | **下载任务用户可取消(界面缺最后一公里)**(机主诉求 2026-09-23) → **✅ 代码收口(2026-09-23 自主批)**:ops activeTxns 登记表+CancelModuleTxn(只发信号不代收口,ID 失配拒杀)+AppService.CancelOperation+横幅在途行「取消」钮(cancellable&&txnId 双闸,不可取消维持如实提示)+22 家 worker 取消话术统一(票面「托管下载已取消」不露 ctx 原文、主动取消不发失败系统通知);journal 收口/残件清理沿用 2b 既有纪律零改动,真机补看:取消后版本区即时回未装、banner 记录标注 | 现状:P0 批 2b 已把生命周期管道铺好——`ops.Txn.Cancel()`/`operation.Hub` 支持事务取消、停用/退出联动已落,批 3 §5-1 也做了 cancellable 诚实标注(仅 install/update 真链);**但面向用户的取消通道没出**:无 Cancel RPC 绑定、共享托管面板下载/安装中无「取消」按钮(核查 frontend/bindings 与 managed 组件零命中)。落地四件:①ops/模块侧暴露按事务取消 RPC(经调用门,鉴 operation ID,只取消自家在途 install/update);②共享托管面板下载/安装进度行挂「取消」按钮(与 §5-1 cancellable 口径联动,不可取消阶段不出按钮);③取消语义收口:journal 收口 operation-cancelled、半截下载残件走 2a 收尸器/黑户隔离不留脏数据、 MSI 场景 msiexec 不可强杀如实文案(keyviz/piclite 既有口径);④取消后状态即时回"未安装",不轮询等灯。纯接线+前端活,2b 地基已在,成本低 |
| N27 | **托管软件真实图标进 hanxi(导航/卡片/列表 + 托盘子菜单)**(机主诉求 2026-09-23) | 诉求:拿托管工具自己的图标嵌进 hanxi,最快最直观认出软件;托盘右键子菜单最好也带。设计要点:①**取材通道**——集成时从官方包提取 exe 内嵌图标(PE RT_GROUP_ICON,纯 Go 解析或 `ExtractIconEx` 一次性脚本),规一化 32px PNG 落 `frontend/src/assets/` 并注册进现有 `i:` 图标注册表(AppIcon 家族,现 54 枚全是自绘矢量,真图标是第二来源需定命名口径如 `app:rustdesk`),**静态入库优于运行时提取**(版本目录可能未装、运行期提图标还要解决前端取图通道);②**展示面**——侧栏/导航 rail/首页模块卡/托管家族列表行,emoji→矢量→真图标三级回退;③**取不到的如实降级**:无 GUI 头件(ddnsgo/frpc 核心)与图标藏在伴生 DLL 的,先落统一"托管工具通用徽标",机主手头有官方 logo 可人工投喂(同"机主交卷"范式);许可注意:各上游 logo 版权状态入库前逐个过 THIRD_PARTY_NOTICES 纪律;④**托盘子菜单图标是硬不确定项**:Wails v3 beta 托盘菜单是否暴露 native menu 图标位需侦查(Win32 本体要 MFT_BITMAP/owner-draw,Wails 未必开口子)——侦查不过关就如实降级为无图标文字菜单,不为菜单去 hack 原生菜单。建议先侦查(半天出可行性+图标素材底账),再拆前后端两批实施 |
| N28 | **观察面操作卡进度与模块内进度不同步**(机主实报 2026-09-23,paseo 下载中:版本表 15%、模块中心横幅卡 0%) → **✅ 代码收口(2026-09-23 自主批,推荐案①)**:operation.Hub 加进度节流广播(800ms 窗口、事件驱动零 goroutine、锁外回调;装配根 SetChangeNotifier(ops.BroadcastChanged) 接线)，前端既有 operation:changed 重拉链自动受益;真机补看项保留:断流慢下载两面读数一致漂移 | 根因已定位:`ops.Txn.Progress`→`Handle.Progress` 写链**是通的**(2b 已接),但 Handle 进度更新**刻意不广播** `operation:changed`(避免下载逐块事件风暴),`BroadcastChanged` 只在登记/收口/相位迁移处调用;前端 `useOperations` 又**只在该事件驱动下重拉**、在途态无兜底轮询→横幅进度冻在上一快照(0%),直到切"解包"相位才跳一下;模块内 15% 走的是各模块 `*:version-download` 事件流,两通道本就异构。修法二选一:①ops 侧对 handle 进度变更做**节流广播**(500ms~1s 合并一次,推荐,单点收口全模块受益);②前端 `active>0` 时 1~2s 轮询 `ListOperations`(多端各写一份)。与 N26(取消按钮)同域,建议同窗实施;修后真机场景补看:断流慢下载时两面读数应一致漂移 |
| N29 | **桌面留言板支持多屏同时生效**(机主诉求 2026-09-23) | 现状:挂牌为**全屏覆盖单窗**(F8 设计口径"副屏跟随主屏选择",即只落一块屏)。诉求:多显示器同时挂牌。落点 `internal/modules/msgboard`(挂牌窗生命周期)与 `MsgBoardPopup.vue`:主屏窗已走"真销毁/TTL 释放"纪律(N6 重设计后),多屏化=每屏一窗的集合管理(创建/销毁/显隐要整组原子,防"一块屏摘了另一块还挂着"的半挂态);注意屏数/分辨率/缩放热变化(拔掉副屏时窗口集合要收回)、KeepAwake 联动与"随 Hanxi 关闭"语义不变、挂牌文字各屏一致(内容单一来源、多窗同源渲染);实现按"主屏窗升级为窗组 manager"重构而非复制粘贴。中等活,纯 msgboard 域内 |
| N30 | **留言板控制台右侧预览窗问题 + 预览要全屏**(机主报告 2026-09-23,与 N6 重设计/N29 同域并批) | ①"右边预览窗口有点问题"——具体症状待机主补(被裁剪?牌面不随编辑回显?玻璃/底色样式错?);②预览最好能**按全屏实际大小呈现**(现右侧小窗预览与真挂牌全屏观感差距大,所见非所得)。落点 N6 改造后的 `MsgBoardView.vue` 预览区与 `BoardCard.vue` 组件族;与 N29 同属留言板一批,建议一次沟通把两处症状+预期看感钉死再动。注意与轮盘 `WheelPreview.vue`(N6-2 同源预览先例)口径统一:预览=渲染同一组件实例数据源,禁止二次实现走样 |
| N31 | **usbipd-win 托管化**(F9 保守方案被机主推翻 2026-09-23:"目前这样不满意") | 现状:F9 落地时"未装引导"取保守口径——探测 usbipd-win 缺席→只给官方发布页链接+winget 命令复制,**没接托管链**。机主裁决改为托管接入。要点:①源 `github.com/usbipd/usbipd-win`(integrate-github-tool 标准流程),Releases 形态需实查:MSI 安装版与(4.x 起是否有)portable zip——形态矩阵决定托管型/安装型或双形态(vscode/recordly 先例);②**特殊性如实**:它不只有 CLI,还有 Windows 服务(Usbipd)+内核驱动(ViPciBus)+证书——驱动/服务安装的静默语义与升级路径开工前实证,**无实证不猜参数**(N7 铁律);portable 形态即使存在也解不开"驱动一次性安装"依赖,`usbipd install` 类子命令能力需真机核;③提权全程走白名单提权通道(F9 bind 已趟路)+N22 新原语;④托管完成后 WSLView「USB 直通」页的引导卡收敛为 hanxi 安装/更新/卸载操作面;用户已自行 winget 装过的场景走 **SystemVersion 外部安装版互认**(rustdesk 先例),不装第二份;⑤LICENSE 核对(GPL 系?)入 THIRD_PARTY_NOTICES。工期:托管八件套 1~2 天+服务/驱动形态特殊侦查半天;真机闸门=有可直通设备环境实测 install→bind→attach 全链,无设备环境如实标注未验 |
| N32 | **系统信息(sysinfo)接入 MCP 工具面**(机主诉求 2026-09-23:设置→AI 接入"感觉系统信息也可以接入") | 现 MCP 只读工具面四件(envcheck/everything/ocr/memo),设置「AI 接入」分区四开关授权(access.json fail-closed 引擎已落,R6 写入口现成)。接入 sysinfo=薄封装第五件:`GetReport` 数据源直供 MCP 工具(软硬件档案,天然只读,符合 PLAN_MCP 只读纪律),零新后端逻辑。落点:①internal/mcp 注册第五工具(输出体量注意——整份 report JSON 偏大,考虑档位化:概览摘要/全量两档,学 ocr 工具 Q1 全文档位先例);②access.json 第五键 + mcpwizard 四开关转五开关(写引擎/只读呈现/对拍矩阵测试机械扩列,R6 真读方验收先例复用);③安装向导三客户端配置文案同步(授权清单描述加"系统信息");④敏感面如实评估一句:report 含计算机名/IP/MAC/序列号,经本地 stdio 喂给用户自己的 AI 客户端属自机信息自取,不新增外泄面,但工具描述里讲清返回内容。成本:半天~1 天 |
| N33 | **设置「历史版本」分区交互重做**(机主裁决 2026-09-23:"反正不友好,排期") | 背景:机主看不懂/不会用,即便按"什么时候的哪份文件+预览+恢复"三步讲解仍判不友好——问题在界面概念模型,不在文案。病灶诊断:①"版本=全家福"是**以快照为轴**的心智,用户脑子里是**以文件为轴**("我这篇便签/这个配置之前什么样");②变更摘要是文件路径串(memo/xxx.md),不对非技术用户;③git 模式与"本机备份模式"两套 UI 落差大,降级时只剩一个按钮像坏了。改造方向(先出 hanxi-workbench-ui 方案给机主过目):以文件为一级入口(左侧受保文件清单:便签名/设置项中文名,右侧该文件时间线),每版直接 diff(改了什么一目了然,替代"内容"盲看),恢复按钮长在文件行上;自然语言摘要("删除了便签《购物清单》");备份模式降级态讲清"你的历史在这里,打开文件夹找"。落点 `SnapshotSection.vue`+snapshot 服务面(如需 per-file 历史查询 RPC 则补,学 DataRootSubUsage 单段名校验纪律)。中活 2~3 天 |
| N34 | **运行日志接入 MCP 工具面**(机主诉求 2026-09-23:AI 接入"可以直接把运行日志接入") | 诉求:AI 客户端经 MCP 查 hanxi 自己的运行日志(hanxi 数据根 `logs/`,MultiWriter 落盘件),问答式排障("昨晚 frpc 怎么断的")。落地:①新只读工具 `logs`——参数 行数上限/级别/module 关键字过滤,**硬顶截断**(学 ocr Q1 档位与 PLAN_MCP"合法 JSON 截断"纪律,超限仍须合法 JSON,#82 先例);②读源走只读 tail 解析(UTF-8 行协议,勿用 windowsgui stderr 假通道,#7e33ef0 教训);③**敏感面如实**:日志行会带机器名/IP/路径/微信昵称级信息,memo 工具"敏感遮罩条目不下发"先例在此升级为**行级脱敏规则(正则族:IPv4/邮箱/token 样式)**,脱敏口径写进工具描述,接入设置「AI 接入」第五开关(与 N32 并批,access.json 一次扩两键省一轮契约 churn);④轮转/占用:读当前活跃文件走共享行读句柄,不抢写者锁。成本:1~1.5 天(N32 同窗) |
| N35 | **设置「工作台入口」打开日志/关于后左侧菜单跳变**(机主报告 2026-09-23) | 症状:设置→工作台入口分区(8708953 改造:日志/关于迁入该分区+rail 收敛折叠)点开「日志」或「关于」,左侧菜单位/分组跟着变了(观感=入口不该动的东西动了)。疑点方向(实施时先复现钉死):①跳转后 AppSidebar/AppNavRail 的分组归属或激活态随路由重算,把用户停留的展开态冲掉(折叠持久化 8708953 只存"折叠",激活切换是否连带换组未核);②日志/关于的 `Section/Group` 归属与"设置"上下文不同源,路由门禁链(navGrouping)按新路由重排面板;③若为窄屏 flyout 场景另查浮层关闭后的落位。修法预期:入口跳转只换内容区,左侧菜单的分组/展开/滚动位置保持稳定(菜单状态与路由激活解耦,激活高亮可以变、结构不许蹦)。纯前端小活,半小时~半天,归 W5 体验债同窗 |
| N36 | **界面字体多套方案实装,机主 App 内亲测后终选**(机主裁决 2026-09-23:"你定几套方案做进去,我最后选") | 前置调研已定调:苹果系(苹方/SF Pro)授权禁嵌入分发,合法平替走可变字体+子集化。**方案组(全部免费商用/OFL,可变版单文件覆盖全字重轴)**:A 现状基线(系统栈,零内嵌,对照组);B MiSans Variable GB2312 子集(~1 MB,最像苹方);C Noto Sans SC Variable 子集(思源系,OFL 授权最硬);D Inter 拉丁子集 + B/C 中文混排栈(SF 味西文);E 等宽升级位(JetBrains Mono 子集,对照现栈 Cascadia 系统自带零成本)。工程要求:①子集化管线进构建(Go Toolchain 纪律不破——用 `github.com/nicholasjackson/...` 不可,选**纯 Go 子集器或 go:generate 调 fonttools 的 vendored 容器脚本**——坑 #65 环境差异先例,开工定实现路径并如实说明);②`unicode-range` 分片+FOIT/FOUT 加载策略;③tokens.css 字体三 token 扩为可切换命名栈,**设置→外观加字体选择器**,App 内热切换让机主同屏对比;④**体积硬预算**:字体资产总增量 ≤2 MB 入构建闸门断言,超标即方案降级;⑤缺字尾追微软雅黑兜底,永不黑洞;⑥终选后处置待定拍板:一把梭删其余(体积洁癖),或保留选择器长期给用户(多 1 MB 常驻)。工期 2~3 天(含管线一次性建设) |
| N37 | **双轴主题按钮配色返工(浅色字发黑/深色发光刺眼)**(机主报告 2026-09-23) | 症状两组:①**浅色模式**——实底按钮(尤其启动/运行类 primary)上文字发黑发沉、看不清;②**深色模式**——部分启动按钮白字+按钮外发光(glow 投影),看久了不舒服。病根初判:主题双轴(9adb7cb 五套色板×明暗)落地时只收了"状态色 AA",**"色板彩底上的前景色(on-color)没有逐板逐模式定义**——彩底配黑字/配白字靠各组件随手取 `--color-text`,五板两模式十组合必然顾此失彼;glow 阴影同理在深色底上对比过载。返工要求:①tokens 层为每板×明暗补 `--on-primary`/`--on-accent` 字色 token(按 WCAG AA 4.5:1 逐对核,复用 9adb7cb 的对照表工具口径),全组件族(`.btn` primary/secondary/danger、启动/运行按钮、徽标 chip)字色一律取 token 禁自取;②深色模式 glow 降级为**无发光语义**——实底色+1px 提亮描边或纯色底,彩色投影只留给"进行中"脉冲一处;③产出物=五板×明暗的按钮矩阵对照稿(Artifact 或 HTML 样张),机主过目再合码;④走 hanxi-workbench-ui 设计规范同步补"on-color 条款"入 skill。纯 token+组件皮活,1 天含矩阵稿 |
| N38 | **softver 微信支持下载安装包(明确不托管)**(机主诉求 2026-09-23) | 诉求:系统管理→软件版本页,微信条目给"下载安装包"能力;边界与现成先例完全对齐 **douzy「仅版本+下载」型**(a6d4fd9:不做版本目录、不 JobObject 托管、不接更新感知设使用——拿完包走人,安装由用户点自己)。实现要点:①**版本与直链取真值**:softver 已有微信官方页解析链(F5),扩展为同时提取当前版号与 `dldir1.qq.com` 系安装包直链——直链时效/参数形态开工实证,**无实证不猜**(微信 PC 官网长期是单一稳定直链,4.x 改版后页面结构需实查);②下载走 artifact 安全下载件复用(进度/断流收尸),但微信无官方 sha256 旁挂→**无摘要降级链如实标注**(WindTerm 先例三层里的可用层:字节数+PE 魔数,UI 明说"官方未提供校验值");③落位=用户下载目录或数据根 `downloads/`(开工定时口径),完成后行内给「打开位置」,可选「运行安装器」按钮走 ShellExecute **用户态拉起**(等价双击,不静默不托管);④旧版检测与新版并列展示,让用户判断"要不要更新"——升级动作仍是用户跑安装器(微信覆盖安装保数据,如实文案)。合规:只做官方源搬运,不入库不分发安装包。成本:半天~1 天(直链实证顺利的话) |
| N39 | **主题明暗切换后轮盘弹窗不跟色**(机主报告 2026-09-23:深色→浅色后,右键轮盘仍是深色) | 症状:主窗主题已切,quickmenu 轮盘浮窗残留旧(深色)配色。病根推断:轮盘弹窗走"按需创建+空闲 TTL 真销毁"(1822a39),**TTL 内存活的窗体不会响应主题变更**——主窗换主题只重设自己的 DOM tokens,无广播给浮窗 webview;浮窗创建时读持久化主题,但创建后主题再变即脱钩。修法(实施先复现钉死):①主题切换处(双轴 9adb7cb 落点)广播 `theme:changed` 事件,**所有活着的浮窗/挂牌窗/预览窗 webview 即时重应用 tokens**(Wails Events 天然可达,QuickMenuPopup.vue 订阅重套);②连带核浮窗族同源病灶:留言板全屏挂牌(N6 玻璃牌面)、OCR 悬浮结果卡(Acrylic 9c1e288)、轮盘实时预览盘、SnipCard——一处广播全员响应,勿逐窗打补丁;③Win11 Acrylic/强调色若随明暗变(dark-mode acrylic tint)需同步重设,不只 CSS 变量;④DWM 标题栏同色(c26e4e7)逻辑对浮窗是否生效一并核(轮盘 frameless 应无标题栏,确认即可)。兜底策略:若浮窗即时重套在某些窗体不干净,退为"主题切换即销毁全部在途浮窗,下次唤起重建"——TTL 架构下销毁重建是现成语义,更诚实。小活半天,归 W5/真机验收并段 |
| N40 | **轮盘命中区全扇面化 + 圆心静区去闪烁 + 对标 StarPie 美化**(机主诉求 2026-09-23:"现在感觉有点不舒服") | 三条:①**全扇面触发**——鼠标落在扇形任意位置(含牌面边缘空白)都要点亮+可点,现状疑似只有图标/文字热区响应(元素级 hover),牌面大片区域是死区;②**圆心静区**——指针移入中间圆心底座范围应清除一切选中/悬停态(回 idle),现状圆心区边缘与扇区内端交界处出现闪烁(hover 态来回抖动);③**样式美化**——对标 StarPie(Star-Pie/StarPie,N5 已核实的高置信参照,PLAN_N5_QUICKMENU v2 在册)。落点 `QuickMenuPopup.vue`+`useWheelRingState.ts`+`wheelGeometry.ts`:命中改**几何判定统一路由**——pointer 坐标→极坐标(半径 r/角度 θ)→环号+扇号,牌面渲染 hover 态由命中结果驱动(pointermove 单点权威,弃 DOM hover;与 1822a39 左键态轮询双通道收口对齐);**前后端几何必须同源**(r 阈值/扇区边界两侧共用一份函数)——#50"512 DIP vs 376 viewBox 不同源"欠账与 N5 既有改造同批清偿,否则命中永远比视觉差一圈。圆心判定= r<内半径 强制 hover=-1;闪烁排查补 hysteresis(交界处 2~3px 迟滞)防抖动。美化先出对比稿(现牌面 vs StarPie 化)给机主选向再动皮。与 N5 编辑器内嵌同模块同窗,合计建议一次收口 |
| N41 | **轮盘与托盘配置收敛单一来源**(机主裁决 2026-09-23:"两处配置独立分开,重复没意义") | 症状:右键轮盘的条目配置(`QuickMenuView`/quickmenu store)与底部托盘右键菜单的配置(`TraySection` 的快捷方式/条目维护)是**两套独立账**——同一条命令/工具想两边都有得配两遍。注:5032682 曾落"分组与托盘子菜单共用一份配置",机主实测仍判重复,开工先对账钉死哪些字段还各存各的(疑点:托盘的"快捷方式区"与轮盘分组条目是否真同源、webapp 条目两处消费口径)。目标形态:**命令/分组条目账本单源**(quickmenu store 为唯一事实源),轮盘与托盘两个面只保留**展示开关**(这条命令在轮盘显示?在托盘显示?)——配置改一处两面同生效,新增项不再两处补;数据迁移:旧托盘独立配置一次性并入单源账本(带"已见名单"式幂等,参照 371188d 卸载持久化先例),合并冲突如实提示不静默覆盖。与 N40/N5 同模块同批,美化-命中-配置一本账一次收 |
| N42 | **OCR 轮盘识别悬浮结果卡支持缩放**(机主诉求 2026-09-23) | 现状:轮盘框选识别的悬浮结果卡(b99a41c 落地的"系统截屏接图+悬浮结果卡+自动复制")**不可拖拽也不可缩放**——注意它与截图 snip 卡是两副面孔:跟手拖拽把手(dce4be3)只长在 snip 卡上,轮盘卡没接过;尺寸固定,长文本只靠内部滚动,读小字费劲。修法:①**拖拽**——把 dce4be3 的把手移动原语(`GetWindowRect`/`MoveWindowBy`+左键态轮询双通道收口)收编为共享件,两卡同装,别再各写一份;②**缩放**两解一并做:①**卡片窗口缩放**——右下角/边缘 drag resize(FramelessWindow 原语补 WM_NCHITTEST 边界命中或把手同族),尺寸持久化记忆(下次唤起跑上次大小),设最小/最大钳制与出屏回收;②**内容字号缩放**——卡片内 `Ctrl+滚轮`/± 按钮步进字号(结果文本 CSS 变量一处控),两态正交独立记忆;浮窗纪律连带核:缩放后仍守真透明无边框(98f3bce 白边教训)、TTL 空闲释放不因拖拽缩放误触发(1822a39 交互态刷新活跃)。"缩放"具体指哪种待机主一句话钉死,登记按全做(工作量都不大,合计一天内) |
| N43 | **状态真相补刀:未安装的不得谎报"未运行"**(机主报告 2026-09-23) → **✅ 代码收口(2026-09-23 自主批,真机复验随 P1 并段)**:notInstalled 判定+文案+hint 收在 managed store 契约层单源(词表接 SUMMARY_META['not-installed'] 冻结同源);primary 未装直落版本页(Shell goVersions 接线,独立挂载降级 toast 指路);running/external/quitting/failed 运行事实优先不遮盖(④口径);七视图夹具按病灶语义补已装记录、真未装用例显式传 [] 不受波及;store 层新 spec 钉三态 | 症状:多个托管软件从未下载安装,界面却显示"未运行"——机主被迫逐个点启动才发现根本没人装过;"未运行"与"未安装"是两个事实,前者暗示"可以起"。背景:P0 批 3 已做状态真相三收口(stale/localResolved/版本互认,25d0ce2)+四维状态统一词表(1831ed9)——**词表与契约都在,是消费面渲染短路**:把"无 running 态"直接兜底成"未运行",未先过 installed 维度。修法:①排查全部消费面(首页模块卡/模块中心/托管家族视图状态条/rail/托盘菜单),展示运行态前先过安装态闸:**未安装→"未安装"中性徽标+引导下载安装;已安装未启动才叫"未运行"**;②启动按钮联动:未安装态点击直落版本页,不发起启动 RPC 吃错;③收口在共享契约层(adapters 家族/managed console 契约 statusOf 纯函数)单点判定,禁 25 视图各修;词表若缺"未安装"档则 Go/TS 双端补档(1831ed9 门禁会逼齐);④连带核 N17 嫌疑③同源:运行探测失灵的 external/提权实例不得混入"未运行"桶。半天~1 天,真机验收并段顺路复验 |

> N1/N2 与 N3 同为"外部进程也能被 hanxi 退出/唤窗"诉求的具体症状:N1/N2 先真机复现修点,N3 决策收口成面。**2026-09-21 W1 已裁决收口(采纳 C+风险分档),三项调研结论与 W2 实施方法全部见 [PLAN_W1_DECISIONS](PLAN_W1_DECISIONS.md)。**

### P4 执行波次排序(2026-09-20 复核重排;执行以本节为准,上表保留原始登记与详情)

复核实测三条硬事实(原登记未覆盖,直接影响排序):

1. **artifact 包只支持 zip**:`packages/go/artifact/unpack.go` 仅有 `UnpackZip`(全套 zip 安全闸门),无 7z 解包能力。**但 W1 调研已推翻本条对 N8 的前提**:WindTerm Windows 资产实测全为 zip,门槛不存在;7z-only 场景(如未来 mpv)已归档选型 `bodgit/sevenzip`(纯 Go/BSD-3,见 [PLAN_W1_DECISIONS §4](PLAN_W1_DECISIONS.md)),触发时再实施、当前不预建。
2. **版本契约在 Go 侧并不共享**:21 个模块各有一份 `version/models.go`(`isPre` 字段重复 21 次),N13 实际成本="21+ 处字段扩散 + 前端共享层 + 版本面板";且新托管模块每先落地一个、N13 就多一处返工点。
3. **N1/N2 的卡点在决策不在代码**:`noExternalProbe` 恒报不在运行、`Quit()` external 态只回指引,均为刻意设计(管理边界=本会话进程树)。动代码前须先裁决"hanxi 是否有权控制自己未启动的进程",即 Wave 4X 的收口决策(从 N3 中拆出前置)。

| 波次 | 内容 | 成本 | 排位理由 |
|---|---|---|---|
| **W1 决策与前置调研**(半天,不写码) → **✅ 全部收口(2026-09-21)** | ①N3 决策(**已裁:采纳 C+风险分档**——`force_free` 档低损外部实例优雅后直接强杀、`confirm_force` 档打扰类须明示确认后杀、未声明默认保守档,详见 [PLAN_W1_DECISIONS §1](PLAN_W1_DECISIONS.md));②N15 查证销项(→已定可行,免费路径);③N5 核实 starpie 仓库(→Star-Pie/StarPie,高置信);④N8 的 7z 方案选型(→前提反转,WindTerm 全 zip,选型无需,归档见 §4)。成果载体:[PLAN_W1_DECISIONS](PLAN_W1_DECISIONS.md) | 小 | 四项全是后续波次的门闸,一次沟通全部裁决 |
| **W2 外部实例治理** → **代码侧收口✅(2026-09-22,余真机验收)**:公共件 `packages/go/externalquit`(5f12779,force-free/confirm-force 双档执行器,未声明默认保守档已入包文档)→ snipaste(4cfd83f:external 按名取 PID、OpenWindow 重定为官方 toggle-images 显隐贴图、Quit 分档+UIPI 提权降级)→ everything(de27a5f:-quit 信使优雅→验证→强杀兜底)+ N12(f41f1a7)。**真机验收项**:①snipaste 外部实例探测/显隐/退出;②everything 外部实例退出(优雅与强杀两路);③提权实例降级指引。catalog 级档位字段未预建(现两模块服务层显式决策即终裁口径,W4 新托管接入时如需声明再加) | N1+N2 同批按 W1 裁决(C+分档)实施:平台层"外部 PID 取得+分档退出执行器"公共件 → snipaste(external 探测+toggle-images 唤窗+`force_free` 退出)、everything(按名取 PID+优雅信号+`force_free` 强杀兜底);N12 纯文案 30 分钟搭车 | 中 | 机主真机报告的在用功能故障,痛感最高;与 P1 F10 冒烟同场跑,合并真机验收 |
| **W3 空间可见性** → **✅ 全部收口(2026-09-21)**:公共件 `packages/go/dirstats`(7bf1b3a)→ N11 数据根占用一览(46119bb)→ N14 envcheck 空间家底(f384948) | 先下沉 walk 目录统计公共包 → N11/N14 各自接线。真机验收项:家底路径族(AppData/GOROOT 推导)与占用数值抽查 | 中 | 原登记已自判同批;"新功能第一天走公共包"纪律;纯本地可开发,适合真机等待期填充 |

#### W3 真机反馈(2026-09-21 机主实测;三件当日实施完毕,待真机复验)

| # | 反馈 | 定位与方案 |
|---|---|---|
| W3-a ✅ | **重新测量时黑窗连闪** | 已修:`defaultRun` 套 `windows.HideConsole`(detect 家族本就 Windows-only,无需 stub);**并查出更深一层**——`.cmd/.bat` 分发器未如 `detect.runVersionCommand` 做 `cmd /C` 包装,命令实际可能静默走默认路径回退,已同型补齐 |
| W3-b ✅ | **versions 目录要再展开一层** | 已实现:`AppService.DataRootSubUsage(sub, force)`(安全单段名校验,扫描面锁数据根内)+ `groupBySoftware` 按首个 `_` 前缀聚合(纯函数带单测)+ `StorageSection.vue` versions 行"展开到每软件",N 版本 chip 悬停列版本目录清单 |
| W3-c ✅ | **占用行直接标删留建议** | 已实现:后端 `DirUsage.Verdict` 四档词表(keep/recommended/redownload/caution,静态建议非删除入口)+ `EnvCheckView` 徽章行;数据根一级行 `logs/memo/.snapshots/versions` 前端静态徽章(未列名目录不戴章,宁缺毋滥)。**一键清理动作仍未做**(待裁决) |
| 附注 ✅ | 小瑕疵两处 | 随 W3-a 顺收:`normalizePath`(Clean 去尾分隔符+盘符大写)统一所有推导路径;整串大小写如需彻底规一(GetLongPathName)另议 |
| **W4 托管集成批** → **N8 ✅ 代码收口(2026-09-23,待真机验收)**:windterm 模块全链接线(version GitHub 链+无摘要降级三层〔vscode 先例,实测上游 32 release 全量无 digest——登记时"现成 zip 管线零改动"不成立,artifact.Fetch 摘要必检须走 bespoke 分治〕/instance 多实例引擎+confirm-force 外部档〔N3 终裁点名〕/共享壳前端+图标+路由+契约锁)。**N9 ✅ 代码收口(2026-09-23,待真机验收,与 N8 互为抄验)**:termora 模块全链接线(version GitHub 官方摘要主链〔与 WindTerm 相反,34 release 全量带 digest,走 markeron 必检口径〕/tag x.y.z[-beta.N] 双收且 beta 即主干如实全列〔recordly 先例,不做通道藏行〕/instance 单实例信使引擎〔ApplicationSingleton CreateMutex+二次拉起 tick,ccswitch/snipaste 混合〕+confirm-force 外部档〔N3 终裁点名 Termora〕/portable data 激活器补齐/共享壳前端+server 图标+路由+契约锁)。**N4 ✅ 代码收口(2026-09-23)**:rammap 模块——日期版模型(Last-Modified 唯一诚实版本语义)+单源降级三层+提权三重契约(#17 litemonitor 同族:740 广播暂扣改写含「管理员」关键词喂 ElevateRestart、ElevationStatus 预告 RPC、UIPI blocked 如实降级)+force-free 外部档(零状态观察工具)。**W4 全部收口,余 N7 暂停件待机主取证** | 大 | N7 缘起级用途、N3 裁决落地后即可开工(先确认静默参数/升级语义);N8/N9 皆为 zip 绿色形态、同技能同管线,互相抄验效率最高;N4 提权+一次性语义开工前正式计划 |
> **N9 真机验收清单(W4)**:①远程列表 stable+beta 全量呈现、预发布徽标在位(上游 2.x 线 beta 即主干,无藏行);②下载安装全链(官方 sha256 四层校验→jpackage 嵌套根落位→data\ 激活器自动补齐)→启动/唤窗/退出三动词;③自带 JRE 确认:干净 PATH(无系统 java)也能冷启动;**单实例互斥验证**:自行打开一个 Termora 后点「启动」应被门禁如实拒绝(而非谎报成功);④外部自行启动的 Termora:external 黄标、「退出」弹风险确认后走优雅/强杀两路、提权实例降级仅指引;⑤导入本地解压目录(含 data\ 会话数据)→ 导入件 verifiedHash 如实 false、卸载运行中/使用中拒绝;⑥活动 SSH 会话下退出:关窗确认框挂起→宽限 3s→强杀兜底文案如实;⑦"随 Hanxi 关闭"开关双向。
> **N4 真机验收清单(W4)**:①远程列表恒一条"最新版(日期)"、下载安装全链(降级三层,已装未验官方哈希如实标注);②**未提权 Hanxi 启动**:应被 740 特判拒并以引导条呈现「以管理员身份重启 Hanxi」+一键 ElevateRestart 挂载;③提权态 Hanxi 启动/唤窗/退出三动词与外部实例 force-free 直退(多实例并看合法);④外部"自启且已提权"的 RAMMap:退出如实回 blocked 降级指引不谎报;⑤导入已解压目录→卸载保护;⑥上游改版(日期变)后 CheckUpdate 点亮、再装落新日期目录。
> **N8 真机验收清单(W4)**:①远程列表只显示稳定版 x86_64 zip(2.7.0 起,无 prerelease 混入);②下载安装全链(进度→落位→verifiedHash 徽章如实为"未验官方哈希"文案)→启动/唤窗/退出三动词;③退出含活动会话场景:关窗确认框挂起→宽限→强杀兜底文案如实;④外部自行启动的 WindTerm:external 黄标、「退出」弹风险确认后走优雅/强杀两路;⑤管理员权限运行的 WindTerm → 降级仅指引不谎报;⑥导入本地目录(含会话数据)→ 卸载保护(运行中/使用中拒绝);⑦"随 Hanxi 关闭"开关双向。

| **W5 体验债** | N6 msgboard 方案先行→实施(纯前端、盘子较小先做);N5 轮盘改进(前后端几何同源改造,大活,含踩坑 #50 欠账);P3 前端打磨批次一可同窗并流 | 大 | 属重设计而非修 bug,须先出方案给机主过目;出方案可与 W3/W4 并行,动代码排队 |
| **W6 契约收口** | N13 全平台安装包+形态标注,一次扫过 21+ 模块 models 与前端契约 | 大 | 后置于全部新托管落地,避免逐模块返工;与 P0 批 3(managed 前端)同域,排在批 3 之后;开工前正式计划评审 |
| **W7 长线** | N16 随手记 A→B→C;N10 系统信息新模块(独立无依赖,可作任意波次并行候选) | 大 | A 批同步语义宜等 N7(及 F3)收口后设计,与飞牛合流;N10 为借鉴型痒点,痛感低于其余,不占主线窗口 |

与原登记的关键差异:N3 拆"决策前置 W1 / 族推广后置于 N1、N2 验证";N8/N9 曾按"7z 门槛"解绑为 N9 先行,**W1 调研推翻前提(WindTerm 发布全为 zip),已恢复原"同批互相抄验"**;N12 从"与 N4 同批"改为搭 W2 便车(现成功能文案缺漏没理由等 RAMMap);真机类任务并段(N1/N2+F10),控制 Linux 开发环境与 Windows 真机的往返成本;与 P0 质量批的并行边界——W2 在实例管理层、P0 批 1/2 在 registry/租约层不冲突,仅 N13(W6)与 P0 批 3 同域需后置。

## 路线参考(未排期)

- 中长期三阶段(搬家与数据真相 → 统一治理 → 灾备与恢复)见 [FUTURE_FEATURE_RECOMMENDATIONS](../FUTURE_FEATURE_RECOMMENDATIONS.md);仅方向,非承诺。
- Wave 4X(外部实例控制)维持延期;manifest、签名 Catalog、物理拆包为 N/A 条件路径(ADR-0003),非默认欠项。

## 复进建议顺序

1. 提交三份文档、执行 worktree 清理(收尾本次暂停);
2. P1-1 F10 冒烟(半小时级,先清最便宜的债);
3. P0 批 1(竞态 + receipt 设计)→ 批 2(租约/journal/Commit)→ 批 3(前端真相);
4. P1-2/3 真机与黄金路径验收;
5. P2 文档漂移随各批提交顺带收口;P3 视意愿插空恢复。
6. P4 复进日新增项按上方"P4 执行波次排序"W1→W7 执行,真机类(W2/F10)并段验收。
