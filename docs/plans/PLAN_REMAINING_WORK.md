# Hanxi 剩余工作排期(项目暂停基线)

> **文档状态**:唯一复入口。项目于 2026-09-20 暂告一段落,恢复工作时以本文为起点,不必再跨多个文档自行拼接现状。
>
> **基线**:`dev` @ `5773ff9`,工作区无未提交代码改动;Wave 0–5 全部落地;41 个业务模块;前端全量门禁绿(112 测试文件 / 1261 项 / `vue-tsc` / 生产构建 / ESLint 0 error)。并行会话(F1–F10、Wave 系列)的工作均已入库,经全 worktree 扫描确认**无任何未提交的代码欠账**。

---

## P0:代码质量修复(复进第一件事)

证据与逐条代码定位全部在 [CODE_QUALITY_REVIEW_PROGRESS_2026-09-19](../CODE_QUALITY_REVIEW_PROGRESS_2026-09-19.md),已暂停待批,恢复时按原批次执行、不重做审查:

| 批次 | 内容 | 关键落点 |
|---|---|---|
| 批 1 ✅ **已完成(2026-09-21,f63d9b2+c679764)** | Registry 覆盖状态数据竞态(值类型投影消灭指针逃逸);receipt 与 `known-modules.json` 成组缺陷(账本独立命名空间/独立 schema/三态保守重建/fail-closed 卸载事务;计划见 [PLAN_P0_BATCH1_FIX](PLAN_P0_BATCH1_FIX.md)) | `internal/extapi/registry.go`、`internal/settings/receipts.go` |
| 批 2 → **2a ✅ 已完成(2026-09-21,4725e52+5881c8e)**:journal 步进 fail-closed 闸门(降级拒新事务+journal-degraded 收口+GetJournalHealth 观察面);Artifact Commit 原子边界收口 rename(黑户隔离放行重装)。**2b 🔄 施工中(机主 2026-09-22 选案 B)**:ctx+租约一并买断。内核已落(`extapi.BackgroundLease`/`EnterBackground`+Registry 停用先 cancel 再 drain+`ops.BeginTxnWithLifecycle`/`Txn.Context/Cancel/Close/JournalFailed`+`artifact.UnpackZipContext` 可取消解包);**已迁移 19/19 journal 模块**(黄金样本 flclash/paseo;普通 zip 族 markeron/ccswitch/everything/litemonitor/ddnsgo/bcu/bili23/mangodisk/rufus/translucenttb;特殊形态 quicklook(bespoke extractAll ctx 化)/keyviz+piclite(MSI:提取器返回后与轮询边界收口,msiexec 不可强杀如实注释)/papertodo(降级下载链 ctx 化)/guoheview(MD5 bespoke 链 ctx 化)/recordly+vscode(安装器一旦拉起即走完,启动前审取消;双形态全链 ctx),全仓 build/vet/单测绿);**2b 遗留待办**:①~~下载中停用/退出三场景 service 级集成测试~~ ✅ 已落(2026-09-23,ops 层全链集成 `TestDeactivateCancelsInFlightTxn`:真 Registry+真 journal,断言停用先取消、operation-cancelled 收口、无 succeeded 假账、drain 放行;退出场景由 ShutdownAll 同构 cancelBackgroundLocked 路径覆盖,真机冒烟见 ② 后补跑);②`go test -race` Windows 侧补跑(2026-09-25 本机实测:无 MinGW gcc、cgo 不可用,-race 本机不可行,维持 CI/容器覆盖;如需本机跑由机主装 MinGW 另行决定);③已知环境 flake 与本批无关:papertodo/instance(cmd.exe 秒退 #81 家族)、portscan(port+1 撞外部监听)(zip+Fetch 族为机械迁移;keyviz/piclite MSI、recordly/vscode 安装器、guoheview bespoke 需按各自取消边界处理,不伪造即时取消) | 迁移模板见 flclash service/manager 两文件;对比表见 [PLAN_P0_BATCH2_FIX](PLAN_P0_BATCH2_FIX.md) |
| 批 3 ✅ **已完成(2026-09-23,五子批 b3-a~e,计划见 [PLAN_P0_BATCH3_FIX](PLAN_P0_BATCH3_FIX.md))** | 4.1 stale 呈现(statusError/lastStatusAt+降灰灯)、4.2 本地未决不判空(localResolved)、4.3 清票走 sameVersionOf 互认、4.4 vscode/everything 自定义加载代次闸(状态/加载独立计数)、4.5 专属动作统一互斥(ddnsgo console/extras 卡/everything 写动作)、4.6 键盘可达(TabNav roving+方向键、progressbar 语义、重试转按钮)、§5-1 cancellable 诚实标注(仅 install/update 有 2b 真链)。**人工余项**:390px/200% WebView2 真机验收(归 P1 清单随 F6 重跑同场) | `frontend/src/components/managed/`、`adapters/vscode.ts`、`views/EverythingView.vue`、`ui/MainTabNav.vue`、`packages/go/operation/hub.go` |

- 每批开工前先形成正式修复计划确认范围;批 1、2 完成后补 `go test -race`(Windows 侧走 CI/真机;云侧已具备 hanxi-dev:2404 容器回路,43 个 Linux 可构建包可本地跑 race,Windows-only 包仍须 CI——配方与豁免清单见踩坑 #84)与组合级故障注入。
- 审查文档 §5 另有 9 项"已发现待复核/裁决"事项,随批次一并收口。

## P1:真机验收债

1. **F10 网页应用(webapp)冒烟**:task dev 开微信扫码、X 关窗后 WebView2 内存回落、收起 TTL 释放——唯一纯新未验项,成本最低,建议最先做。
2. **F6(存储目录)、F3(快照)、F4/F4b(MCP 与向导)、F7(OCR 托管)、F9(WSL USB)**:按 [PROGRESS.md](../PROGRESS.md) 既有人工清单补跑;**随 F6 加测批 3 人工余项**——共享托管面板/版本表在 390px 宽 + 200% 缩放下无溢出、TabNav 纯键盘(←/→/Home/End/Tab)可完成换选、NVDA/讲述人可读进度与"暂不可确认"标注。
3. **A 机 → NAS → 干净 B 机黄金路径**:先完整走一次手工验收(建立模块迁移等级:可携带/需重建缓存/需系统注册/需重填凭据/不适合临时电脑),据结果再决定机器绑定恢复提示与临时电脑模式是否立项。

## P2:文档与工程卫生

- [x] 三份未跟踪文档已随本排期一并入库:本文、CODE_QUALITY_REVIEW_PROGRESS_2026-09-19、FUTURE_FEATURE_RECOMMENDATIONS。
- [x] 卫生清理已执行完毕(2026-09-20,机主点名批准):28 个 worktree 目录(全 MERGED)注销删除,含 hubkit 旧名时代 3 个失效 locked 登记;已合并分支(`feat/f*`、`fix/q*`、`feat/r*`、`fix/nine-suite-quality`)与全部无主 `worktree-agent-*`  scratch 分支删除。现仓库仅存 `dev`/`main`/`pre-split-backup` 三分支、单一工作树。注:8 个 scratch 分支同指 hubkit v0.2.0 时期 release 合并点 `60aff0c`(不在任何 tag/远程/备份可达范围),删除后仅 reflog 可回捞(约 90 天窗口)。
- [ ] 事实漂移收口:README/PRD/PROJECT_STRATEGY 的 37/38 旧统计 → 41;旧数据根语义(`hanxidata` 标记、`%APPDATA%` 回退、旧 `data/` 兼容)→ 现行"exe 同级 + `hanxi.bind`",含 Release workflow 便携说明;BACKLOG 中 WebApp 状态更新。
- [ ] gofmt 闸门红在 8 个历史文件(`logging/everything/ocr/platform` 等):机主裁决——建议单开一次 `style(frontend)`/`chore` 性格式化提交,不与功能混淆。

## P3:已暂停批次(恢复与否由机主定)

- **前端打磨批次一**:审查与实施计划已完成,三路实现已停止且主工作区无本轮改动;恢复时直接重新并行实施,不必重做审查。

## P4:复进日新增(2026-09-20,机主点名,只登记不即修)

| # | 事项 | 现状与落点 |
|---|---|---|
| N1 | **snipaste 打开/退出异常**(机主真机报告) → **✅ 实施已落(4cfd83f,待真机复验)** | 模块无 OpenWindow 动词(仅 Launch);状态词表无 external——`noExternalProbe` 恒报"不在运行"(`internal/modules/snipaste/instance/instance.go`),管理边界=本会话启动的进程树。先真机复现定位,再定修复范围(含是否引入外部检测)。 |
| N2 | **everything 外部实例可检测但退不掉**(机主真机报告) → **✅ 实施已落(de27a5f,待真机复验)** | external 探测与信使唤窗可用;`Quit()` 在 external 态刻意不越权、只回指引(`internal/modules/everything/service.go` ~303 行,探针窗口类/互斥体通道拿不到 PID)。需补"外部 PID 取得(按进程名/窗口枚举)→ `-quit` 优雅退出或 WM_CLOSE"。 |
| N3 | **Wave 4X 复活评估:托管家族外部实例退出/唤窗策略统一** → **✅ 代码收口(2026-09-24 自主批 33d124a,真机补看)**:21 模块 focus/quit 普查底账→公共件 winfocus_windows(guoheview 标杆三要素:可见+标题过滤/IsIconic 恢复/SetForegroundForce 借权),litemonitor/rufus/flclash/rustdesk/subnetdesk 五族"Visible==0 才恢复"误判与裸置前台病灶根除,paseo 最大化误伤修正,rammap 注释口径订正(force-free 终裁,代码未变),snipaste 补 PID==0 守卫+无身份指引文案。余账已清:QuitExternal 四份胶水克隆已收编 QuitExternalOf(守卫入内核,漂移结构性杜绝);"17 保守档升档"需先补 Inspect 通道且属越权决策,W1 分档裁决维持。**原账**:唤窗两方案并存(单实例信使:ccswitch/everything;按 PID EnumWindows:rustdesk/litemonitor/subnetdesk/rufus/flclash/bcu/guoheview);外部实例退出一律只指引。已知隐患:①`focusWindowsByPIDs` 族以 `IsWindowVisible` 判恢复,最小化窗仍带 Visible 标志→不 SW_RESTORE、置前台无效,guoheview 的 `IsIconic` 方案为标杆;②托盘/后台唤起需 `platform/windows.SetForegroundForce` 借前台特权,族内多用裸 `SetForegroundWindow`。收口"是否越权强杀外部实例"决策后按模块推开,吸收 N1/N2 结论。 |
| N4 | **RAMMap64 托管集成** → **✅ 代码收口(2026-09-23,机主拍板路线 A 托管本体,待真机验收)**(微软 Sysinternals 内存释放) | 单文件绿色 exe,源 `download.sysinternals.com/files/RAMMap.zip`(非 GitHub,借用 integrate-github-tool 托管模式但换下载源);一次性"清空待机列表"语义、需提权、非常驻 GUI,形态近 recordly/paseo 单目录家族,提权与退出语义开工前正式计划确认。 || N4 | **RAMMap64 托管集成**(微软 Sysinternals 内存释放) | 单文件绿色 exe,源 `download.sysinternals.com/files/RAMMap.zip`(非 GitHub,借用 integrate-github-tool 托管模式但换下载源);一次性"清空待机列表"语义、需提权、非常驻 GUI,形态近 recordly/paseo 单目录家族,提权与退出语义开工前正式计划确认。 |
| N5 | **quickmenu 右键轮盘交互改进**(机主:"实在是不好用") → **✅ 代码收口(2026-09-25 自主批,C1 27159b3+C2 932eaaf+C3/C4 0da6207,真机补看)**：TrayItemsEditor 共享件抽出+内嵌 QuickMenuView(编辑不再跳设置页，勾选/exe/组排序/脏态/回滚逐字保留零行为改动，saved 钩子刷新预览)；isOptionAdded 顶层判重病灶与 tray:changed 事件化(涉契约)按纪律留后续；C1 编辑面共享件内嵌/C2 触发参数外化热更新/C3 点上格互换闭环(身份匹配定位,零新 RPC)/C4 容量软警示全落;**2026-09-26 第二波收口**：页布局 v3(788b95e 编辑主场置顶+轮盘舱 trim 推导+628 窄窗 spec)、N40③ 皮肤三预设×透明度×描边×跟随模块色全链(71f3052 基础盘→cdfdc57 双源→b944f40 后端账)、#50 几何同源清偿(55118d9 跨语言对账锁)、DPR 逐枚台账+红线件 rt 轨(19319c2/f575082/7e2c73b);轮盘皮肤并入 N40③ 拍板后批次;保留项(触发键可配/排除清单=N5b)按计划归档;isOptionAdded 顶层判重与 tray:changed 事件化两项已知病灶挂后续批次。**2026-09-26 追加(夜,双账批)**:011a60a 编辑宿主收官——TrayItemsEditor scope 取 'tray'/'wheel' 单点 Get/Set 成对切换(设置页零改动)、保存钮/脏标记整退改实时保存(结构即时+文本 800ms 防抖+blur 抢跑+在途尾随合流绝不丢最后一次)+草稿门(不完整分组暂缓落盘+页脚如实状态条,"删最后一个子条目该笔暂缓"副作用入文案);type=route 候选退役不再供选(存量行挂"不再供选"中性小标照常管理);isOptionAdded 病灶按交互重设计更新为组内判重口径、体量原因如实挂账未动;轮盘面 Get/SetWheelMenu bindings 待总闸再生(现走交叉型护栏,误接只 toast 不串账)。**原账**:借鉴 GitHub 开源项目 starpie(登记日检索未定位到确切仓库,开工先核实链接)。落点 `QuickMenuPopup.vue`/`useWheelRingState.ts`/`wheelGeometry.ts`/`internal/modules/quickmenu/`;既有欠账:前后端几何不同源(512 DIP vs 376 viewBox,见 FEATURES_OVERVIEW 与踩坑 #50)、触发时长/容差为编译期常量。先出改进分析+实施计划,再动代码。 |
| N6 | **msgboard 桌面留言板 UI 重设计**(机主:不喜欢、不友好) → **✅ 实施收口(2026-09-23,牌面观感待真机验收)** | 走 hanxi-workbench-ui 设计语言评审;落点 `MsgBoardView.vue` 与留言板组件族,`internal/modules/msgboard/` 后端契约尽量不动。先出方案给机主过目再实施。**2026-09-26 追加(夜,v4 牌桌)**:8a77c03(A 路:大牌面 1:1 舞台+右栏窄操作条+一屏零滚动;v3 定量复盘——牌真实占比<5%、控件与说明 45%,配重倒挂即"表单合集"病根)→6d1f14c(B 路:boardMetrics 单源收编 ×13/×0.42 换算系数双真相,BoardStage/BoardFullscreen 落库 58 例独立绿)→9e544d8(接线收口:视图切共享件、内联数学与挂钩族 -88 行、19 条断言字面零动)——A/B 并流"收编在途未 import"的合并事故由主会话裁决闭环;另 a839ced(审计路 A1 根治:热键冲突回滚路径漏播 changed 致牌面窗永挂旧文案,补播+publish 投递缝),该笔自报三条结构性观察挂账待解禁窗口(换屏 chip 闪断/pending 窗口/上限常量双写)。 |
| N7 | **飞牛同步客户端托管集成**(标准安装形态) → **❌ 机主终裁放弃(2026-09-25:"飞牛同步放弃")——本排期项正式关闭归档,侦查结论(§下行)留史不销;若未来以新需求重提,另立编号不复活本项** | 源 `fnnas.com/download?key=fn-sync-client`;机主明确不能免安装,须走安装型先例(recordly NSIS 静默安装、vscode installer 双形态)。开工前确认:静默参数、版本提取、升级语义(覆盖/先卸后装)、且本机正在履行同步义务的实例按 N3 终裁登记为 `confirm_force` 档(优雅退出优先,强杀须 UI 明示"传输中"并确认,不闷头杀)。缘起级用途(MOTIVATION:NAS 同步随身),优先级建议靠前。 | **侦查结论(离线,部分实证)**:①下载页 JSON 实证当前版 `0.2.3`,Windows 直链 `iso.liveupdate.fnnas.com/pc/fn-sync_0.2.3_x64-setup.exe`(另有 macIntel/macApple dmg,无 Linux 桌面包);②`fn-sync` 系 Tauri 应用(命名 `<name>_<ver>_x64-setup.exe` 是 Tauri v2 NSIS 打包器签名产物——非 Inno,与 recordly NSIS 先例同族但 Tauri 有自己的静默参数约定);③**版本提取无需 GitHub API**:直链文件名即含版本,或解析 download 页 JSON 的 `version` 字段(everything 网页解析+快照兜底先例);④**卡点**:该 CDN 对非国内/数据中心 IP 返 403(`X-Error-Info: typeC`,四种 UA/Referer/Range 全拒),离线无法下载二进制做静默参数实证——按 skill"无实证不猜参数"铁律不预写 NSIS 解码链。**需机主侧一步取证**:在你本机(能正常访问 fnnas)浏览器下载该 setup.exe 或 `curl -sIL` 存 headers,或授权我在你网络环境跑一次下载,再定 `-S /当前用户静默` 具体参数与退出码族。
| N8 | **WindTerm 托管集成** → **✅ W1 复核更正:无 7z 门槛;W4 实施再更正:上游全量无官方摘要,须无摘要降级链(非零改动)** | 仓库 `github.com/kingToolbox/WindTerm`,走 integrate-github-tool 托管模式。**登记时"7z 绿色包"有误:GitHub API 全量核对,Windows 资产均为 `*_Windows_Portable*.zip`**(仅 2020 v1.1 带过历史 7z)。稳定版已至 2.7.0(2025-03)+Prerelease 线(原"2.6.1 稳定/2.7.0 beta"表述过期),channel 适配按 paseo/recordly 先例;Qt 单 exe 无信使→唤窗按 PID EnumWindows(guoheview IsIconic 标杆)、退出 WM_CLOSE+JobObject 兜底不变。⚠️ `windterm.cn` 为第三方网盘站(自动化只走 GitHub releases);官方称 Apache-2.0 但仓库根缺 LICENSE 文件,集成文档留备忘。 |
| N9 | **Termora 托管集成** → **✅ 代码收口(2026-09-23,待真机验收)** | 仓库 `github.com/TermoraDev/termora`(机主指定),同技能同模式。jpackage 双发布型:zip(内嵌 JRE 绿色,优先收编版本目录)与 exe 安装器(若采则归 N7 安装型先例族);Java 应用注意子进程树收口。与 N8 同类,建议同批实施、互相抄验。 |
| N10 | **借鉴 MooTool 系统信息功能** → **✅ 代码收口(a3e2519,2026-09-23;N32 MCP 扩面/N21 显示缺陷另收)**:独立 sysinfo 模块已落——注册表+WinAPI+标准库八段只读采集,段级降级 errors,无 WMI/新依赖;前端系统信息页与目录契约已接。真机补看:网络/显示器与内存磁盘单位 | 参照 [rememberber/MooTool](https://github.com/rememberber/MooTool) 的系统信息工具:CPU/内存/磁盘/主板/显卡/OS/网络等软硬件档案一览。借鉴纪律沿 `docs/MOOTOOL_ANALYSIS.md`(学机制不抄实现);Go 候选 gopsutil 或 WMI/syscall 直采,开工定。划界:envcheck=开发工具链、litemonitor=托管第三方实时监控,本模块=静态软硬件档案,独立模块或并入 envcheck 家族开工定。 |
| N11 | **数据根占用可视化** → **✅ 代码收口(46119bb,2026-09-21;W3;W3-b/a5070f4 后续补齐)**:存储目录页已接数据根一级目录排行、15s 巨目录预算+3min TTL/force 穿透/换根失效、Partial 下限与错误计数、versions 按软件展开及删留建议徽章;真机补看:路径族与数值抽查 | 数据根解析走 `internal/settings/paths.go` 现成通道(同级 `hanxidata/` 或 `hanxi.bind` 绑定);每个一级子文件夹大小=每便携软件磁盘占用,一眼看清。要点:walk 统计缓存+手动/后台刷新、巨型目录(Everything 索引/快照库)限时渐进、紧凑排序列表走 hanxi-workbench-ui;挂设置页「存储目录」分区还是独立模块开工定。 |
| N12 | **litemonitor 简介补「释放内存」能力**(机主指出) → **✅ 已落(f41f1a7)** | 上游 LiteMonitor 支持释放内存,hanxi 模块简介未写。定位 catalog/视图简介文案,对照上游功能清单核实后补齐,顺带排查其他"已支持未宣传"缺项;纯文案低风险可插空。与 N4(RAMMap 内存释放)同主题,文案口径可同批收口。 |
| N13 | **托管版本列表显示全平台安装包并标注形态** → **✅ 代码收口(步骤1+2 已落,2026-09-23/24)**:hostfeed 分类器(273 真名 fixture 校准,平台×形态词表冻结)+20 家 GitHub 模块 Assets 注记链(adapter→ManagedVersionPanel「上游发布」列徽标 chips,+N 折叠,托管形态高亮);只标不点(非 Windows 行不进下载链路)照案执行。余:W6 非 GitHub 五源(dd 系)注记+徽标观感真机看。**2026-09-26 追加**:9b9acad(Snipaste/VSCode/QuickLook 三自绘方言表接形态徽标,chip 画法逐字对齐共享面板、取词 releaseFormWord 单源,66 例绿)+d9f763c(everything 补 hostedForm 断头收口——方言表自此四家齐活;其 typedef 欠总闸由 e486674 补收)+9da419b(adapter 方言名单按实扫四家如实订正、"rammap 自绘版本区"旧误注释修正);rustdesk/subnetdesk 一行双资产不合行级 form 语义、排除位如实;6617b75(W6 五源判定推进:snipaste 官网页 assets 矩阵投影为唯一有料源已补,rammap/vscode/translucenttb 无料即诚实缺席零改动,"dd 系五源"注记余账相应收窄)。 | 现 release 被过滤到只剩当前 Win 机器可用包(以 flclash 为例);改为展示 Windows/macOS/Android APK(Linux 可不要),逐包写清**便携版/标准安装版**。三处落点:①各模块 `version` 包平台过滤改"全量返回+平台标注";②共享契约 `ManagedReleaseRecord`(现仅 version/published/size/isPre)扩平台/形态/URL 字段,Wave 5 版本面板加平台与形态 chip——**跨模块契约变更,开工前出正式计划评审**;③非 Windows 资产只给"浏览器打开下载",不进 hanxi 下载/设使用链路。 |
| N14 | **envcheck 扩展:环境本体+依赖目录占用显示** → **✅ 代码收口(f384948,2026-09-21;W3)**:envcheck/diskusage 已按环境变量→工具自述命令→平台默认三级推导本体/依赖缓存目录,复用 dirstats 多目录总预算,Partial 下限/不臆造未落地路径;EnvCheckView「空间家底」本体/缓存分组、路径、大小已接;真机补看:AppData 路径族与数值抽查 | 现只报"装了什么/什么版本",看不到空间家底:①每个环境**本体安装目录**大小(Go SDK、Node、JDK、Python、.NET 各占多大);②依赖/缓存目录位置与大小(Go `GOPATH/pkg/mod`/GOCACHE、npm 全局包与缓存、pip/Maven `~/.m2`/Gradle/NuGet)。落点 `internal/modules/envcheck/` 检测器补双目录推导(env var→默认路径→工具命令三级回退)+`EnvCheckView` 展示"本体/依赖家底"两列。**与 N11 共用同一 walk 目录统计公共件,两项同批:先下沉公共包再各自接线**(实践"新功能第一天走公共包"纪律);UI 走 hanxi-workbench-ui。 |
| N15 | **评估:OCR 与 Snipaste 结合**(机主:"不能就算了") → **✅ 已定:可行,免费路径可落地,W1 调研收口** | 核心机制=Qt 单实例命令行通道(`Snipaste.exe snip -o 文件` 免费,外部实例同样可达),hanxi 目录监听落盘文件接现有 `ocr` 模块;PRO 另有 exec/`ocr_clipboard`/`--block` 增强。边界:目标提权运行被 UIPI 拦→降级指引;免费版无官方退出命令(`exit` 属 PRO)。详见 [PLAN_W1_DECISIONS §2](PLAN_W1_DECISIONS.md)。 |
| N16 | **随手记(memo)演进:路线已定 A→B→C**(首轮沟通 2026-09-20 完成) | 机主四类痛点全选(怕丢/记不快/找不回/用不上)→ 完整升级,三批次:**A** 文件库化+空闲静默 git 快照(无 git 降级普通备份,底稿 `PLAN_SNAPSHOT.md`,与 F3/N7 飞牛同步天然合流)→ **B** 全局热键快记+悬浮速记卡+全文/模糊检索 → **C** MCP/CLI 联动深化与导出(依赖 A/B 形态)。**2026-09-26 一日三批全收：A**=重设计+markdown+一键全删(2972811/a3bd50e/2ac9b3b 后 ClearAll 2972811 与整页 a3bd50e)；**B**=全局热键 Ctrl+Alt+N 悬浮速记卡+光标屏摆位+空闲真销毁(65aa380)，全文/模糊检索增强由 A 批检索条+ C 批 MCP 时间窗部分兑现，纯模糊匹配留观察；**C**=MCP 深化(hanxi_memo_stats+since/until 时间窗 d87654c，写工具经 ADR 复核否决)+导出(单条 .md/全库汇总 masked 恒排除 919c526)。**2026-09-26 追加(夜)**:c0da60d 全链更名"极客随手记→随手记"(Nav/Info/Catalog/MCP 文案/导出标题/联动提示零漏网,cataloggen 再生非手写;docs 历史账与 memory 按纪律不动);378d006 悬浮速记卡牌面重做(主角下划线行+胶囊标签+飞走残影,133px 预算内闭环;修入场动画白烧真 bug——mount 期窗口还 Hidden 改 opening 事件驱动);f61060c M4 组件库奠基(MemoCard/MemoToolbar/memoMetrics 单源,53 例);52ef2de 主界面 v2「摊开的笔记本」(列表进场成本 -66%、一页两面替代三级跳转,金标准 36 例语义零丢迁移)。**masked 严口径裁决定稿**:MemoCard 硬纪律(masked 态标题与正文两明文面皆不渲染、不挂含明文 title 属性、MD 徽标结构泄漏同熄)定为全链统一口径,视图旧"masked 仍显标题"退役,与 MCP 线"masked 整条不下发"同谱。接线收口 d745e70(18:31)当日闭环:f61060c 删除单全销——parseTags 双份收单源、📌 字形退役描边档、粘贴并条上收 flattenPastedLines 两面共吃、跨午夜分组分裂顺带修,五 spec 111/111 绿;主窗 Ctrl+Enter 裁量"存下但不抢阅读位"。随手记夜批五笔全线 ✅。 |
| N17 | **TranslucentTB 跨机环境脆弱**(机主真机报告 2026-09-23,只登记不即修) | 现象:旧机"之前合适、后来坏了",换本机(26200)即正常——二进制未坏,坏的是环境匹配。三条嫌疑待定位:① portable 依赖系统已装 WinUI 2.8/VCLibs 框架包,旧机可能被系统更新重置/精简掉(模块头注释已记此依赖);② OS 大版本更新(如 23H2→24H2)致所用 TranslucentTB 版本失效(上游任务栏 shell 变更有失效史);③ 旧机若手动双击起过外部实例(持单实例互斥体),接管语义与托管启动不同。**待机主补事实**:坏的具体症状(任务栏完全不透明/exe 没反应/面板与现态不符)、旧机破坏时段前后的 Windows 更新历史、两机托管版本号差。定位后:若①,启动前加框架包探针(`Get-AppxPackage Microsoft.UI.Xaml.2.8`/VCLibs.UWPDesktop)缺件如实预告并引导补装,不静默拉起注定不透明的进程;若②,沉淀踩坑并评估版本兼容下限提示。**2026-09-26 追加(双形态闭环,三笔+一补票)**:5947e25(msixbundle 下载缓存线:上游 releases 全扫 10/20 带 bundle、仅 2026.1/2026.2 有 GitHub digest——"无摘要不入列"硬口径实出列 2 版,四层完整性入 versions/translucenttb/packages,与便携线缓存根互不踩踏)+f43adb5(MSIX 系统生命周期五导出 GetMsixState/Install/Uninstall/Launch/RemoveCache;FamilyName 三源交叉实证常量化;共存语义只陈述不干预——便携/打包共用上游互斥体、打包进程游离 Job 按 external 感知)+0521bc5(前端「打包版(系统管理)」区块+远程行装打包版 chip+AV 对照优先钮;ManagedVersionPanel 加可选 #release-actions 槽零波面)+6f36b12(归因补票:0x80073CF6/0x80073D05 包注册基础设施缺失族→话术直指"开开发者模式重试,仍失败便携版即正解"——机主装机事故原形回归钉+failMsix warn 日志收口);bindings 三面再生 cffc2c8。嫌疑①(框架包被精简/重置)自此有官方出路:打包版走部署器自动解析依赖。 |
| N18 | **WSL 本机发行版页签界面问题**(机主报告 2026-09-23,只登记待细化) | 现象待补:机主报"本机发行版"页签界面有问题,具体症状未描述(布局溢出/状态错位/操作按钮异常?)。嫌疑落点近期改造面:32f3c15 本机发行版独立标签页与行内操作面重排、1287be6 W1–W9 交互整改与页签合并——多轮重排后回归风险高。待机主给症状与截图/屏录后定修复范围,纯前端问题优先随真机验收并段处理 |
| N19 | **设置「存储目录」分区界面优化** → **✅ 代码收口(2026-09-24 自主批,基于 N11 既有契约)**:占用卡重排为测量头/三项摘要(已测目录/已测占用/已测文件)/资源行三层,Partial 下限与错误计数语义不变;首次扫描 loading、零目录下一步空态;640px 以下动作换行/摘要单列/资源行纵向,pointer:coarse 控件44px,reduced-motion;不改后端契约。真机补看:390px/200%/长路径。**2026-09-26 三连胜**:子行归属标识(c766a25)→分区语境重组+DIR_INTENTS 九类目录用途词表铺四类行(20ef618)→系统操作钮位收编待复核(机主截图两钮实证为 RAMMap 旧构建沉底已修,存储页无此钮——如新构建仍见请带页指认) | 机主点名优化存储目录设置分区,具体痛点待补(疑与数据根占用一屏总览 W3/N11 观感相关)。落点 `frontend/src/views/settings/StorageSection.vue`。建议走 hanxi-workbench-ui 设计语言评审后重排,与 P3 前端打磨批次、W4 各真机验收同窗并流 |
| N20 | **OCR「识别历史」弹窗界面优化**(机主报告 2026-09-23:"不够友好") → **✅ 代码收口(2026-09-25 自主批 8c6d9d9,真机补看)**：读码实锤三硬伤(Esc 一键关两窗/确认框被弹窗垫底 z 序倒挂/零焦点管理)全修+说明行与视口滚动缓释；**排期前提修正**：弹窗本体就是公共 HistoryPanel 非自取数组件，冗余在 OcrView/PortKillView 两份壳。N20 追加：PortKillView 同源壳三病灶同批移植修复(b151d72)；N20b 公共件三项同日已落 95fe248(fmtDateTimeSmart 智能时间/标记 chips 中文化词表对齐后端记录点/分桶空态，三接入方同源受益)。双壳已抽共享 UiHistoryDialog（壳皮/契约单份，PortKill 皮微差统一为 Ocr 版待真机目检一一眼） | 落点 `frontend/src/views/OcrView.vue` 内自取数的历史弹窗(约 511/790 行起,Teleport 遮罩语系外壳,非公共 HistoryPanel 组件,改动无波及其他接入模块)。"不友好"具体痛点待机主补(条目密度?时间呈现?回填交互?空态?),建议走 hanxi-workbench-ui 重排;可顺带评审是否值得收编回统一 HistoryPanel 口径。纯前端,随真机验收并段 |
| N21 | **系统信息页(N10)两处显示缺陷**(机主报告 2026-09-23) → **✅ 代码收口(2026-09-24 自主批)**:①fmtSize 全局扩 GB/TB 档(纯高位扩档,KB/MB 锁定口径不动,波面核查全库无 GB 级 MB 断言);②真机 probe 实证采集面本含 IPv4(192.168.1.31 在场)——断点是观感非数据,新增 netaddrs 呈现排序 IPv4 前置/APIPA/链路本地垫底,机主实拍 fixture 锁死。真机补看:系统信息页内存磁盘显 GB、网卡列 IPv4 打头 | ① **容量单位恒 MB 不换 GB/TB**:根因实证——视图走共享件 `utils/format.ts::fmtSize`,上限即 MB(`>1MB → 一位小数 MB`),16GB 内存显示成 "16384.0 MB";**改共享件有波面**(BCU 等托管家族视图共用,且 `format.spec.ts` 明钉 "1024 KB/4.0 MB 现实现口径"),方案二选一:全局加 GB/TB 档(改契约,顺带全家族受益)或 sysinfo 私有 auto-scale 件(零波面);建议全局档+改钉规格,开工时机主定。② **网络接口不显示 IPv4**:后端 `collectNetwork` 用 `net.Interface.Addrs()`(理论含 IPv4)、前端已有「地址」列(`SysInfoView.vue` ~167 行 join 渲染)——链路看着都在,需真机复现断点:采集面真缺(如地址族/时序问题)还是 IPv6 淹没观感缺;若采集真缺,可下钻 `GetAdaptersAddresses`(Windows 原生含 IPv4 单播)或借 `Get-NetIPAddress` WMI 面补,并把 IPv4/IPv6 分列呈现 |
| N22 | **提权工具启动体验重做——双路并存+双向检测+友好话术**(机主诉求 2026-09-23,细化同日) → **✅ 代码收口(2026-09-24 自主批,RAMMap 首接,真机补看)**:①共享件——platform 目标-only `RunElevatedDetached`(PsQuote/取消/失败分态,不进 Job)+PE manifest reader(require/highest/asInvoker/unknown 四态,unknown 不误判免提权,740 兜底不变);②RAMMap `OpenWindowElevated` A 路(回执 `launchMode=external-elevated/managed=false/canQuit=false`,前端横幅+退出钮禁用明说外部实例)、`ElevationStatus.executionLevel` 静态复核;③RAMMapView A/B 双钮并排、话术大白话;④ElevateRestart 文案与 Detached 现实对齐。余:其他 requireAdministrator 模块推广(BCU/Rufus/LiteMonitor 按同 adapter 形状接)与 UAC 真机验收。**原现状**:非提权 hanxi 启 RAMMap 被 740 特判拒→引导条只有一条路「以管理员身份重启整个 Hanxi」(ElevateRestart)。**机主已确认:两条路都要**——A「双击式」runas 只提权目标工具(`ShellExecuteEx runas`,系统 UAC 弹窗,hanxi 本体保持普通权限),B「提权重启」维持 ElevateRestart 全家提权。缺的不是通道是**判断与呈现**:①**双向检测**——"hanxi 是不是管理员"基建现成(ElevationStatus/#17);"**这个软件需不需要管理员**"新做:安装/导入时解析托管 exe 内嵌 manifest(`requireAdministrator`/`asInvoker`/`highestAvailable`,纯 Go 读 PE RT_MANIFEST)**自动探测**,模块显式声明可覆盖,启动 740 特判**兜底**(探测失真不误报);②**四象限决策**——不需提权→照常入 JobObject 托管;需提权+hanxi 已提权→直接托管起;需提权+未提权→友好选择弹窗双按钮(场景话术:一次性看数据选 A,常用水工具选 B);③**诚实呈现**——A 路起的实例是外部提权进程,进不了 JobObject、UIPI 下不可控退出,琥珀徽标「运行中·已提权(外部)」+ tooltip"这个实例 Hanxi 管不到,关窗请手动",绝不说"启动失败/管不了"这类吓人词;④文案纪律:740/JobObject/UIPI/manifest 等术语全部退居「技术细节」折叠区,主界面大白话+一句话场景判据。**落点**:runas 通道做成平台共享原语(「按需提权拉起」),RAMMap 首接、litemonitor(#17 同族)/Rufus/未来提权工具与 **N23 自研释放**复用,守"新功能第一天走公共包"纪律;manifest 探测件同批下沉(托管家族共用)。**版本管理面不受影响**,只改运行控制呈现。**随批影响**:N4 真机验收清单第②条需按双路新形态改口径 |
| N23 | **自研「一键释放内存」能力**(机主诉求 2026-09-23:RAMMap 之外,自研一个) → **✅ 代码收口(2026-09-24 自主批,真机补看)**:①platform standby 能力件(SeProfileSingleProcessPrivilege+ERROR_NOT_ALL_ASSIGNED 双检、NtSetSystemInformation MemoryPurgeStandbyList=4、STATUS 失败如实);②一次性 UAC helper:主进程 runtime 目录预创建唯一结果文件、`--mode=purge-standby` 早于 GUI 分流、request ID/协议/大小/状态四闸、超龄收尸;③sysinfo `PurgeStandby` RPC(调用门+mutex;未提权走共享 helper，已提权直执行;前后 AvailPhys 快照差如实命名"可用内存变化"不谎称精确释放量);④系统信息页内存卡入口(确认/busy/失败/零值如实+完成后刷新);MCP 红线未触碰、无新工具。与 N22 共享一次性提权原语。**与 N4 关系**:N4 当年拍板只选了路线 A(托管 RAMMap 本体),**路线 B 当时已侦查完备**——方案/实施拆分/代码量估算(~400 行)全在 [PLAN_N4_RAMMAP §1/§3](PLAN_N4_RAMMAP.md),现按机主新诉求复活为独立排期项。与 A 不互斥:A 管看细节(完整版 GUI),B 管 hanxi 内**一键动作+释放量数字**。技术骨架(照方案):①`platform/windows/standby.go`——`SeProfileSingleProcessPrivilege` + `NtSetSystemInformation(MemoryPurgeStandbyList)` 单系统调用封装(数十年稳定,零第三方二进制);②提权走一次性子进程 runas + 临时文件握手回传(与 N22 的「按需提权拉起」共享原语同域,**两卡开工时并流复用,勿各造一套**);③sysinfo 服务挂 `PurgeStandby()` RPC(走调用门,返回前/后 AvailPhys+释放量+拒绝原因)+ 内存卡按钮 + 结果 toast。沿用 N4 旧拍板点 ②③:入口先页内还是同要托盘命令;释放前 UAC 弹窗属预期打扰的接受度确认。文案纪律:如实讲"清理可回收缓存换立即可用",不写"提速"营销话术 |
| N24 | **托管家族"导入本地目录"未对齐首装即立用**(机主报告 2026-09-23) → **✅ 已修(2026-09-23 自主批)**：17 家 ImportLocal 全补 `GetActive()==""` 守卫（rustdesk/subnetdesk 双值形、everything 通知前插），17 模块 52 包测试绿；service 级 DI 断言从略（守卫三连行同构，排期原文"可随批"非硬性） | 契约:"没有任何版本时,第一个到手的版本默认=使用版本"。核查实证:**下载链路 24/24 模块已统一**("未设使用版本时自动把刚下载完的版本设为使用",含 rustdesk/subnetdesk 双值形态);**导入链路只有新样本做了**——snipaste/termora/windterm/paseo/rammap 导入即立用,存量 **17 模块**(`bcu/bili23/ccswitch/ddnsgo/everything/flclash/guoheview/keyviz/litemonitor/mangodisk/piclite/quicklook/rufus/rustdesk/subnetdesk/translucenttb/vscode`)的 service.ImportLocal 直接 `return s.manager.ImportLocal(...)`,导入后不设 active(前端"使用中"徽标空,行为不一致)。markeron 无导入面、papertodo/frpc 无 active 版本概念(恒自动最新已装),均不涉及。修法:照 snipaste 样板在 17 个 ImportLocal 成功路径补 `GetActive()==""` 守卫(rustdesk/subnetdesk 用 `(info.Version, version.FormPortable)` 双值),机械改动一次扫过;行为无单测钉(现有测试未覆盖守卫层),可随批给 1~2 代表模块补 service 级断言 |
| N25 | **托管下载不吃系统代理——代理链下沉公共件** → **✅ 代码收口(2026-09-24/25 自主批,真机补看)**:G0 netx 下沉 packages/go/netx(wsl 孤例泛型化+DirectClient/LoopbackAwareProxyFunc/IsLoopbackHost 上收+非 windows 桩+回环短路/八形态/四态链测);G1 remoteversion 公共厂换链(envcheck 七探针一处修复);G2-G3 44 处出网字面量全量切链(25 家 apiClient/下载 manager/es/eartrumpet 补 15min 硬顶,build/vet 净);G4 语义定调(publicip 走链+代理出口诚实注释·wechat 隐性直连显式化 Proxy:nil·artifact fetchTransport 换混合闸消灭两份 isLoopback);G5 测试收编(回环绕代理断言·fileshare 集成靶直连化·vscode NO_PROXY 洞改注入式——链的 WinINET 兜底不认 NO_PROXY,原口径受执行序影响)。updatewatch 随 A 组自动受益;portscan/LAN 探测按协议豁免登记。余:真机验证 Clash 系统代理开启态下载成功、测试族残余裸 client 低风险遗留已录。**原账**:现状实证:托管家族的版本 API/资产下载用裸 `http.Client`(含 `packages/go/artifact` 内核,均 `ProxyFromEnvironment`)——**只认环境变量代理**;Clash/v2rayN 默认"系统代理"只写 WinINET 注册表,Go 不读→浏览器能上、hanxi 直连(用户体感"开着代理还是慢/挂")。现仅两条缓解:①21 模块 gh-proxy/ghproxy.net 镜像前缀降级(绕代理但非真走用户代理);②**wsl/netx 代理链(env→WinINET 兜底→直连)是孤例**,仅 WSL 模块与 ocr 注释引用。修法:按踩坑 #35 既定口径把 netx 泛型化为公共件(`packages/go/netx` 或 platform 层),托管家族 remote/downloader 与 artifact fetch 统一接入(PAC 形态不猜、如实直连;env 命中优先保持);收益:用户"系统代理开着"即全站(含 GitHub 直链)同进退,镜像降级退居真·兜底。注意 #35 坑中坑:`http.ProxyFromEnvironment` 按进程缓存环境,单测用注入链不碰真 env。**机主 2026-09-23 扩面(痛点实报:经常下载失败)**:凡 HTTP(S) 出网一律接代理链——托管下载/版本 API/更新感知、publicip 等在线查询、各模块网络探测的 HTTP 探针全量收编,不留裸连孤岛;如实边界:ICMP ping/端口扫描/LAN 发现属原始协议,HTTP/SOCKS 代理帮不上,探测器按协议决定能否走链并如实标注 |
| N26 | **下载任务用户可取消(界面缺最后一公里)**(机主诉求 2026-09-23) → **✅ 代码收口(2026-09-23 自主批)**:ops activeTxns 登记表+CancelModuleTxn(只发信号不代收口,ID 失配拒杀)+AppService.CancelOperation+横幅在途行「取消」钮(cancellable&&txnId 双闸,不可取消维持如实提示)+22 家 worker 取消话术统一(票面「托管下载已取消」不露 ctx 原文、主动取消不发失败系统通知);journal 收口/残件清理沿用 2b 既有纪律零改动,真机补看:取消后版本区即时回未装、banner 记录标注 | 现状:P0 批 2b 已把生命周期管道铺好——`ops.Txn.Cancel()`/`operation.Hub` 支持事务取消、停用/退出联动已落,批 3 §5-1 也做了 cancellable 诚实标注(仅 install/update 真链);**但面向用户的取消通道没出**:无 Cancel RPC 绑定、共享托管面板下载/安装中无「取消」按钮(核查 frontend/bindings 与 managed 组件零命中)。落地四件:①ops/模块侧暴露按事务取消 RPC(经调用门,鉴 operation ID,只取消自家在途 install/update);②共享托管面板下载/安装进度行挂「取消」按钮(与 §5-1 cancellable 口径联动,不可取消阶段不出按钮);③取消语义收口:journal 收口 operation-cancelled、半截下载残件走 2a 收尸器/黑户隔离不留脏数据、 MSI 场景 msiexec 不可强杀如实文案(keyviz/piclite 既有口径);④取消后状态即时回"未安装",不轮询等灯。纯接线+前端活,2b 地基已在,成本低 |
| N27 | **托管软件真实图标进 hanxi(导航/卡片/列表 + 托盘子菜单)**(机主诉求 2026-09-23) → **✅ 代码收口(2026-09-24 自主批,侦查+批A 5c36949+批B 1beb706+批C 9d3fca4+批B-2 f698a34 扩 12 枚+尾巴放行 litemonitor/果核看图 7417a2e（机主拍板"识别性使用+异议即摘"，在册 19；孤儿矢量 image 已随 b266501 清）(17 在册;litemonitor/guoheview 许可红线摘出待机主放行,bili23/rustdesk/termora/nanazip 已补提(d0d551a)→**2026-09-26 三连胜**：尾巡补录 ddnsgo/frpc/eartrumpet 至 26 枚(d77ba27)；全册 64px 封顶升切+sources.go 逐枚台账(1c7e3af，绝不放大凑数，ddnsgo/frpc 源天花板 48 如实)；托盘 16px 菜单变体档+提取脚本 PE RT_ICON 通道根治(19319c2)。**红线件运行期本机提取通道上线**(7e2c73b)：RAMMap/Recordly/VSCode 免入仓真图标，rt: 第三源+已装未停用两判据+三层缓存，许可"不入库"结论不变),真机补看,成果见 [PLAN_N27_ICON](PLAN_N27_ICON.md;2026-09-26 夜追加两笔卫生:e486674(bindings 总闸补收 IconPNG 服务面与 everything form typedef)、1a9ffb3(peicon 误入库调试勘察残件 TestDbgTmp 剔除))**:批C——internal/app/appicons go:embed 五枚 PNG 副本(dist 哈希/内联不可按名读取,双份系侦查定案)、托盘顶层与 group 子条目按 itemModuleID 挂 SetBitmap(Wails releaseMenuBitmaps 源码实证不漏 GDI 句柄)、sync_test sha256 逐字节防漂移、未内嵌模块恒 nil 回落文字菜单;观感不过删副本即整批降级,功能无损。**批A/B 回顾**:批A——AppIcon 扩 `app:` 第二来源(glob 静态表+generic 回落)、提取脚本五枚实证入库、许可逐条过 THIRD_PARTY_NOTICES(Snipaste 标异议即摘);批B——三轨解析收口公共件(appIcons.resolveIcon/appIconName,`startsWith('i:')` 散落判断全退役),六消费面接轨(rail/侧栏/命令面板/模块卡/详情/首页/轮盘双盘),五模块 ccswitch/keyviz/everything/snipaste/markeron 后端 Nav+MODULE_PRESENTATION 双源同换,契约升级为"svg|app 可解析"钉死。批前后端全仓绿、前端 1338 绿、exe 重建。**余批C 托盘 SetBitmap(独立可砍,Win11 深色菜单观感真机定)+真机看五枚图标显示**。**侦查结论**:①取材通道实证(cc-switch.exe→32×32 PNG 提取成功,定"一次性脚本+静态入库"否决运行时提取);④**硬不确定项解除**——Wails v3 beta.10 公开 `MenuItem.SetBitmap` 吃 PNG 走 SetMenuItemBitmaps(托盘 popupmenu 同通路,无需 owner-draw hack)，残余风险=Win11 深色菜单 alpha/尺寸真机定,不达标整批降级文字菜单;素材底账 24 版目分类(ddnsgo/frpc/rammap 落通用徽标+许可逐个过 THIRD_PARTY_NOTICES);实施拆 A 前端注册表+脚本/B nav 接线/C 托盘独立可砍三批。**原诉求**:拿托管工具自己的图标嵌进 hanxi,最快最直观认出软件;托盘右键子菜单最好也带。设计要点:①**取材通道**——集成时从官方包提取 exe 内嵌图标(PE RT_GROUP_ICON,纯 Go 解析或 `ExtractIconEx` 一次性脚本),规一化 32px PNG 落 `frontend/src/assets/` 并注册进现有 `i:` 图标注册表(AppIcon 家族,现 54 枚全是自绘矢量,真图标是第二来源需定命名口径如 `app:rustdesk`),**静态入库优于运行时提取**(版本目录可能未装、运行期提图标还要解决前端取图通道);②**展示面**——侧栏/导航 rail/首页模块卡/托管家族列表行,emoji→矢量→真图标三级回退;③**取不到的如实降级**:无 GUI 头件(ddnsgo/frpc 核心)与图标藏在伴生 DLL 的,先落统一"托管工具通用徽标",机主手头有官方 logo 可人工投喂(同"机主交卷"范式);许可注意:各上游 logo 版权状态入库前逐个过 THIRD_PARTY_NOTICES 纪律;④**托盘子菜单图标是硬不确定项**:Wails v3 beta 托盘菜单是否暴露 native menu 图标位需侦查(Win32 本体要 MFT_BITMAP/owner-draw,Wails 未必开口子)——侦查不过关就如实降级为无图标文字菜单,不为菜单去 hack 原生菜单。建议先侦查(半天出可行性+图标素材底账),再拆前后端两批实施 |
| N28 | **观察面操作卡进度与模块内进度不同步**(机主实报 2026-09-23,paseo 下载中:版本表 15%、模块中心横幅卡 0%) → **✅ 代码收口(2026-09-23 自主批,推荐案①)**:operation.Hub 加进度节流广播(800ms 窗口、事件驱动零 goroutine、锁外回调;装配根 SetChangeNotifier(ops.BroadcastChanged) 接线)，前端既有 operation:changed 重拉链自动受益;真机补看项保留:断流慢下载两面读数一致漂移 | 根因已定位:`ops.Txn.Progress`→`Handle.Progress` 写链**是通的**(2b 已接),但 Handle 进度更新**刻意不广播** `operation:changed`(避免下载逐块事件风暴),`BroadcastChanged` 只在登记/收口/相位迁移处调用;前端 `useOperations` 又**只在该事件驱动下重拉**、在途态无兜底轮询→横幅进度冻在上一快照(0%),直到切"解包"相位才跳一下;模块内 15% 走的是各模块 `*:version-download` 事件流,两通道本就异构。修法二选一:①ops 侧对 handle 进度变更做**节流广播**(500ms~1s 合并一次,推荐,单点收口全模块受益);②前端 `active>0` 时 1~2s 轮询 `ListOperations`(多端各写一份)。与 N26(取消按钮)同域,建议同窗实施;修后真机场景补看:断流慢下载时两面读数应一致漂移 |
| N29 | **桌面留言板支持多屏同时生效**(机主诉求 2026-09-23) → **✅ 代码收口(2026-09-24 自主批 8b3013b)**:主屏窗升级为窗组 manager——每屏一窗(主屏打头、首窗沿用旧名)、挂撤/失效拆组全程原子(杜绝半挂态);拔屏由挂期间 5s 对账协程比对在位屏集(planScreenTargets/sameDeviceSet 纯函数)整组重挂,不做单窗增删;KeepAwake 引用计数与热键/轮盘三通道语义不变;Config 新 EveryScreen 默认真(旧配置缺键即多屏同挂,显式 false 才回 F8 单屏旧口径、下拉只列副屏原样保留);前端模块页加勾,关勾现选择器;热键回滚补 EveryScreen 不再丢位。多屏/拔屏为窗口系统行为,真机补看:双屏各一牌、Esc 整组撤、拔副屏窗组收回 | 现状:挂牌为**全屏覆盖单窗**(F8 设计口径"副屏跟随主屏选择",即只落一块屏)。诉求:多显示器同时挂牌。落点 `internal/modules/msgboard`(挂牌窗生命周期)与 `MsgBoardPopup.vue`:主屏窗已走"真销毁/TTL 释放"纪律(N6 重设计后),多屏化=每屏一窗的集合管理(创建/销毁/显隐要整组原子,防"一块屏摘了另一块还挂着"的半挂态);注意屏数/分辨率/缩放热变化(拔掉副屏时窗口集合要收回)、KeepAwake 联动与"随 Hanxi 关闭"语义不变、挂牌文字各屏一致(内容单一来源、多窗同源渲染);实现按"主屏窗升级为窗组 manager"重构而非复制粘贴。中等活,纯 msgboard 域内 |
| N30 | **留言板控制台右侧预览窗问题 + 预览要全屏**(机主报告 2026-09-23,与 N6 重设计/N29 同域并批) → **✅ 代码收口(2026-09-25 自主批 5872649)**：①读码复现硬伤=固定 0.34 缩放遇大字号横裁→动态容纳(默认档逐像素不变)，其它猜测症状未见代码病灶、如仍存在需机主补截图；②「全屏预览」纯前端浮层同源 BoardCard 所看即所挂、零后端副作用(不走真挂牌：Toggle 翻转语义会撤真牌+惊动 N29 窗组账)；预览=同组件同数据源纪律照 WheelPreview 先例守住。**余真机**：浮层不含任务栏/不跨屏，要逐像素含须回谈真挂牌副作用方案 | ①"右边预览窗口有点问题"——具体症状待机主补(被裁剪?牌面不随编辑回显?玻璃/底色样式错?);②预览最好能**按全屏实际大小呈现**(现右侧小窗预览与真挂牌全屏观感差距大,所见非所得)。落点 N6 改造后的 `MsgBoardView.vue` 预览区与 `BoardCard.vue` 组件族;与 N29 同属留言板一批,建议一次沟通把两处症状+预期看感钉死再动。注意与轮盘 `WheelPreview.vue`(N6-2 同源预览先例)口径统一:预览=渲染同一组件实例数据源,禁止二次实现走样 |
| N31 | **usbipd-win 托管化**(F9 保守方案被机主推翻 2026-09-23:"目前这样不满意") → **✅ 方案A收口(机主 2026-09-25 二选一拍板A，65c3512)**：「⚡ 一键直通」主路径(目标自动解析/多发行版弹选按行记忆/UAC 事前明示/失败现态驱动分步重试)+高级操作折叠旧细钮全保留，零新增导出；病灶实锤=概念负担非功能缺失。**2026-09-26 机主终裁 B(推翻 09-25"留缺口")**：真·一键 winget 代装同步补齐——受控新导出(后端全字面量 winget 命令、winget 自提 UAC、已装不重装、CommandContext 超时收口),**已全链收口(同日 B 落地)**：受控导出 InstallUsbipdViaWinget 入库 1085172(取消码六形态归一/已装不重装/Runner MSI 固定落位自救看穿 PATH 定格，踩坑 #88)；真机一键实跑仍挂 F10 同场验收。 现状:F9 落地时"未装引导"取保守口径——探测 usbipd-win 缺席→只给官方发布页链接+winget 命令复制,**没接托管链**。机主裁决改为托管接入。要点:①源 `github.com/usbipd/usbipd-win`(integrate-github-tool 标准流程),Releases 形态需实查:MSI 安装版与(4.x 起是否有)portable zip——形态矩阵决定托管型/安装型或双形态(vscode/recordly 先例);②**特殊性如实**:它不只有 CLI,还有 Windows 服务(Usbipd)+内核驱动(ViPciBus)+证书——驱动/服务安装的静默语义与升级路径开工前实证,**无实证不猜参数**(N7 铁律);portable 形态即使存在也解不开"驱动一次性安装"依赖,`usbipd install` 类子命令能力需真机核;③提权全程走白名单提权通道(F9 bind 已趟路)+N22 新原语;④托管完成后 WSLView「USB 直通」页的引导卡收敛为 hanxi 安装/更新/卸载操作面;用户已自行 winget 装过的场景走 **SystemVersion 外部安装版互认**(rustdesk 先例),不装第二份;⑤LICENSE 核对(GPL 系?)入 THIRD_PARTY_NOTICES。工期:托管八件套 1~2 天+服务/驱动形态特殊侦查半天;真机闸门=有可直通设备环境实测 install→bind→attach 全链,无设备环境如实标注未验 |
| N32 | **系统信息(sysinfo)接入 MCP 工具面**(机主诉求 2026-09-23:设置→AI 接入"感觉系统信息也可以接入") → **✅ 代码收口(2026-09-24 自主批,与 N34 同批)**:`hanxi_sysinfo_report` 五→六件扩面——overview(默认,剥 BIOS/产品ID/MAC/环回)/full 两档+段级 errors 透传,后端取无头 registry 内 sysinfo 模块同一 service(零新采集逻辑);access.json 六键契约读写镜像同步(对拍矩阵 16→64 组合、guards 白名单、四键老档仍合法下次回写归一);AI 接入分区第六开关「系统信息」(含计算机名/网络地址直书)。真机补看:客户端 tools/list 见 6 件、overview/full 两档手测 | 现 MCP 只读工具面四件(envcheck/everything/ocr/memo),设置「AI 接入」分区四开关授权(access.json fail-closed 引擎已落,R6 写入口现成)。接入 sysinfo=薄封装第五件:`GetReport` 数据源直供 MCP 工具(软硬件档案,天然只读,符合 PLAN_MCP 只读纪律),零新后端逻辑。落点:①internal/mcp 注册第五工具(输出体量注意——整份 report JSON 偏大,考虑档位化:概览摘要/全量两档,学 ocr 工具 Q1 全文档位先例);②access.json 第五键 + mcpwizard 四开关转五开关(写引擎/只读呈现/对拍矩阵测试机械扩列,R6 真读方验收先例复用);③安装向导三客户端配置文案同步(授权清单描述加"系统信息");④敏感面如实评估一句:report 含计算机名/IP/MAC/序列号,经本地 stdio 喂给用户自己的 AI 客户端属自机信息自取,不新增外泄面,但工具描述里讲清返回内容。成本:半天~1 天 |
| N33 | **设置「历史版本」分区交互重做**(机主裁决 2026-09-23:"反正不友好,排期") → **🔄 施工中(机主 2026-09-25 批"全按推荐"；批A 已落 7f24431：热修被删文件恢复三层验证+文件轴 ListFiles/FileHistory/DiffFile 双闸三 RPC+备份模式解封浏览 P4+memo 标题 DI+拆 SnapshotSection git 门出文件轴最小可用版；申报偏离=engine 接口 1→3 支合理代价。批B 已落（行级 diff 前后缀裁剪+块状降级/折叠/组件拆分/中文名账 17+27/两模式口径，全量 1421 绿）；批 C 已落(640px 纵深+常驻徽标+计数整句);批 D 代码面已落(60f19e3：旧摘要 decorateLegacy 呈现层回改/双模式文案 P8-A 收口/容量句按模式分档)；余真机双模式验收 D1-D10 清单挂 P1；改名 AtPath 根治在 B。方案与 9 点回填见 [PLAN_N33_SNAPSHOT_UX](PLAN_N33_SNAPSHOT_UX.md))**:实读代码修正两条排期诊断——①备份模式"降级像坏了"根因是后端能力全量在位、前端 SnapshotSection mode==='git' 门+当年 Q3 旧裁决主动隐藏(拆门即改,成本远低于字面预期,推翻 Q3 列 P4);②新发现功能洞:**已删文件当前恢复不了**(预览弹窗对 status=D 无恢复钮+PreviewFile 必失败,而"删错的便签能找回"恰是快照 Q1 头号场景)——零新 RPC 修法已并入方案。文件为轴双栏 IA/memo 标题映射现成/state 中文名表/config 键 v1 只做 15 顶层(逐字段需另立注册表工程 P5)/diff 前端纯函数件。分批 A-D 含验收风险,实施等拍板。**原背景**:机主看不懂/不会用,即便按"什么时候的哪份文件+预览+恢复"三步讲解仍判不友好——问题在界面概念模型,不在文案。病灶诊断:①"版本=全家福"是**以快照为轴**的心智,用户脑子里是**以文件为轴**("我这篇便签/这个配置之前什么样");②变更摘要是文件路径串(memo/xxx.md),不对非技术用户;③git 模式与"本机备份模式"两套 UI 落差大,降级时只剩一个按钮像坏了。改造方向(先出 hanxi-workbench-ui 方案给机主过目):以文件为一级入口(左侧受保文件清单:便签名/设置项中文名,右侧该文件时间线),每版直接 diff(改了什么一目了然,替代"内容"盲看),恢复按钮长在文件行上;自然语言摘要("删除了便签《购物清单》");备份模式降级态讲清"你的历史在这里,打开文件夹找"。落点 `SnapshotSection.vue`+snapshot 服务面(如需 per-file 历史查询 RPC 则补,学 DataRootSubUsage 单段名校验纪律)。中活 2~3 天 |
| N34 | **运行日志接入 MCP 工具面**(机主诉求 2026-09-23:AI 接入"可以直接把运行日志接入") → **✅ 代码收口(2026-09-24 自主批,与 N32 同批)**:`hanxi_log_read` 按日期 tail 只读工具——date/level/contains/lines(硬顶500) 四参、256KiB 起窗口×4 回读到 64MiB 封顶(不整库吞入)、半行不出机、缺档给现存日期指引、非法日期钉死;Windows 共享读不抢 GUI 写者锁;逐行 `logging.RedactPII`(Redact 全量+IPv4/邮箱/sk-类前缀密钥,磁盘窄口径不动);logs 键无背后模块走 registryGate 空门(access 即唯一闸)。真机补看:AI 客户端问答"昨晚 xx 怎么断的" | 诉求:AI 客户端经 MCP 查 hanxi 自己的运行日志(hanxi 数据根 `logs/`,MultiWriter 落盘件),问答式排障("昨晚 frpc 怎么断的")。落地:①新只读工具 `logs`——参数 行数上限/级别/module 关键字过滤,**硬顶截断**(学 ocr Q1 档位与 PLAN_MCP"合法 JSON 截断"纪律,超限仍须合法 JSON,#82 先例);②读源走只读 tail 解析(UTF-8 行协议,勿用 windowsgui stderr 假通道,#7e33ef0 教训);③**敏感面如实**:日志行会带机器名/IP/路径/微信昵称级信息,memo 工具"敏感遮罩条目不下发"先例在此升级为**行级脱敏规则(正则族:IPv4/邮箱/token 样式)**,脱敏口径写进工具描述,接入设置「AI 接入」第五开关(与 N32 并批,access.json 一次扩两键省一轮契约 churn);④轮转/占用:读当前活跃文件走共享行读句柄,不抢写者锁。成本:1~1.5 天(N32 同窗) |
| N35 | **设置「工作台入口」打开日志/关于后左侧菜单跳变**(机主报告 2026-09-23) → **✅ 代码收口(2026-09-24 自主批)**:复现钉死疑点②——设置语境全靠 settingsSectionOf(activeRoute) 派生,/logs /about 双信号(routeGroup/分区)全空致面板跳首页态;修法=契约单点归判(两页判归 workbench 分区语境,入口跳转只换内容区、结构稳定高亮跟随,'/' 显式回台不受影响)。三测例锁死。真机补看:设置→工作台入口→日志/关于左栏不变阵 | 症状:设置→工作台入口分区(8708953 改造:日志/关于迁入该分区+rail 收敛折叠)点开「日志」或「关于」,左侧菜单位/分组跟着变了(观感=入口不该动的东西动了)。疑点方向(实施时先复现钉死):①跳转后 AppSidebar/AppNavRail 的分组归属或激活态随路由重算,把用户停留的展开态冲掉(折叠持久化 8708953 只存"折叠",激活切换是否连带换组未核);②日志/关于的 `Section/Group` 归属与"设置"上下文不同源,路由门禁链(navGrouping)按新路由重排面板;③若为窄屏 flyout 场景另查浮层关闭后的落位。修法预期:入口跳转只换内容区,左侧菜单的分组/展开/滚动位置保持稳定(菜单状态与路由激活解耦,激活高亮可以变、结构不许蹦)。纯前端小活,半小时~半天,归 W5 体验债同窗 |
| N36 | **界面字体多套方案实装,机主 App 内亲测后终选**(机主裁决 2026-09-23:"你定几套方案做进去,我最后选") | 前置调研已定调:苹果系(苹方/SF Pro)授权禁嵌入分发,合法平替走可变字体+子集化。**方案组(全部免费商用/OFL,可变版单文件覆盖全字重轴)**:A 现状基线(系统栈,零内嵌,对照组);B MiSans Variable GB2312 子集(~1 MB,最像苹方);C Noto Sans SC Variable 子集(思源系,OFL 授权最硬);D Inter 拉丁子集 + B/C 中文混排栈(SF 味西文);E 等宽升级位(JetBrains Mono 子集,对照现栈 Cascadia 系统自带零成本)。工程要求:①子集化管线进构建(Go Toolchain 纪律不破——用 `github.com/nicholasjackson/...` 不可,选**纯 Go 子集器或 go:generate 调 fonttools 的 vendored 容器脚本**——坑 #65 环境差异先例,开工定实现路径并如实说明);②`unicode-range` 分片+FOIT/FOUT 加载策略;③tokens.css 字体三 token 扩为可切换命名栈,**设置→外观加字体选择器**,App 内热切换让机主同屏对比;④**体积硬预算**:字体资产总增量 ≤2 MB 入构建闸门断言,超标即方案降级;⑤缺字尾追微软雅黑兜底,永不黑洞;⑥终选后处置待定拍板:一把梭删其余(体积洁癖),或保留选择器长期给用户(多 1 MB 常驻)。工期 2~3 天(含管线一次性建设) |
| N37 | **双轴主题按钮配色返工(浅字发黑/深字光刺眼)**(机主报告 2026-09-23) → **🔄 分析+样张已出待拍板([PLAN_N37_ONCOLOR](PLAN_N37_ONCOLOR.md)+specimens/n37-oncolor-matrix.html,7 项拍板点)**：实测修正病根——--color-on-primary 其实早已逐板定义且全局在消费，真凶是 teal 浅色 4.12:1 唯一硬违规(数学证明该底色无干净前景，须压暗底色，A/B 两案待选)+danger/positive 借值 15 处无契约看守+"随手取 text"系结构隐患非既成事实(全库零实例但按钮基座 color:inherit 一踩十爆)；glow 18 处 A-D 分类降级清单。**2026-09-26 追加**：N38 按钮家族八项治理已入库(609479a：disabled 灰件形/十主题焦点环提档/层级描边/按压反馈/danger 递进/全局 btn-danger 收编/死 hover 修)——但本卡核心两拍板点(teal 浅色 on-color 压暗 A/B 案、glow 18 处降级)仍待拍板未动。**2026-09-26 追加(夜,六路终态——上句已被当日事实追平)**:机主已拍板执行——teal 压暗定案 A(#0d8284/hover #0b7577,白字 4.12→4.62 达标)、glow 全线废除仅留 live-pulse/snip-pulse 呼吸;六路全落:96ed8ee(一路·原子层 on-* 契约成文)→81d9cb6(三路·五页 scoped 危险钮副本三处退役+frpc 选中环去彩色发光)→b9f9785(四路·壳族 .mod-run halo 改实底描边,HomeView 零改动判定留档)→a680fd1(五路·设置族死档回落剥除+AboutView 自调色底转正源)→c98a469(tokens 终态:on-primary+on-accent 双枚×十块、110 组 WCAG 实算全过、同值兼容档整删封死双头真相)→7675631(终结位:frontend/src/styles/__tests__/buttonColorContract.spec.ts 守卫三则+样张定案执行版+PLAN §5 七项逐项标态);9d0367f(模块中心长条蓝钮降 micro 次级档)搭车。**三笔挂账待拍板**:§2c selected 三处主色字(sky 深 3.51/sky 浅 4.30/teal 浅 3.55)、onyx AAA 续压否(现盘据实推定 AA 即止,需 AAA 另启)、轮盘 QuickMenuPopup.vue:753/896 两枚 drop-shadow(--color-primary-glow)借值(§3 盘点未列、N40 域,是否纳入全线废除待机主明示);**另两笔在途**:AppNavRail .rail-dot 旧 6px halo 待壳线收编、拍板点 2"不拆"系 token 线终裁、待机主追认名义未销。**原症状**:①**浅色模式**——实底按钮(尤其启动/运行类 primary)上文字发黑发沉、看不清;②**深色模式**——部分启动按钮白字+按钮外发光(glow 投影),看久了不舒服。病根初判:主题双轴(9adb7cb 五套色板×明暗)落地时只收了"状态色 AA",**"色板彩底上的前景色(on-color)没有逐板逐模式定义**——彩底配黑字/配白字靠各组件随手取 `--color-text`,五板两模式十组合必然顾此失彼;glow 阴影同理在深色底上对比过载。返工要求:①tokens 层为每板×明暗补 `--on-primary`/`--on-accent` 字色 token(按 WCAG AA 4.5:1 逐对核,复用 9adb7cb 的对照表工具口径),全组件族(`.btn` primary/secondary/danger、启动/运行按钮、徽标 chip)字色一律取 token 禁自取;②深色模式 glow 降级为**无发光语义**——实底色+1px 提亮描边或纯色底,彩色投影只留给"进行中"脉冲一处;③产出物=五板×明暗的按钮矩阵对照稿(Artifact 或 HTML 样张),机主过目再合码;④走 hanxi-workbench-ui 设计规范同步补"on-color 条款"入 skill。纯 token+组件皮活,1 天含矩阵稿 |
| N38 | **softver 微信支持下载安装包(明确不托管)**(机主诉求 2026-09-23) → **✅ 代码收口(2026-09-25 自主批 b4591e1)**：netx 链下载引擎(仅 HTTPS/1GiB/MZ 断言/同名避让/临时件即清/ctx 可取消)+三门后 RPC+进度事件(契约 83→84)、KnownFolder 还原真实下载目录、页内诚实文案"无校验值不代安装"。**余真机**：实连 dldir1 时效/OneDrive 落位/Clash 代理链。 |诉求:系统管理→软件版本页,微信条目给"下载安装包"能力;边界与现成先例完全对齐 **douzy「仅版本+下载」型**(a6d4fd9:不做版本目录、不 JobObject 托管、不接更新感知设使用——拿完包走人,安装由用户点自己)。实现要点:①**版本与直链取真值**:softver 已有微信官方页解析链(F5),扩展为同时提取当前版号与 `dldir1.qq.com` 系安装包直链——直链时效/参数形态开工实证,**无实证不猜**(微信 PC 官网长期是单一稳定直链,4.x 改版后页面结构需实查);②下载走 artifact 安全下载件复用(进度/断流收尸),但微信无官方 sha256 旁挂→**无摘要降级链如实标注**(WindTerm 先例三层里的可用层:字节数+PE 魔数,UI 明说"官方未提供校验值");③落位=用户下载目录或数据根 `downloads/`(开工定时口径),完成后行内给「打开位置」,可选「运行安装器」按钮走 ShellExecute **用户态拉起**(等价双击,不静默不托管);④旧版检测与新版并列展示,让用户判断"要不要更新"——升级动作仍是用户跑安装器(微信覆盖安装保数据,如实文案)。合规:只做官方源搬运,不入库不分发安装包。成本:半天~1 天(直链实证顺利的话) |
| N39 | **主题明暗切换后轮盘弹窗不跟色**(机主报告 2026-09-23:深色→浅色后,右键轮盘仍是深色) → **✅ 代码收口(2026-09-24 自主批,照①案零后端契约扩面)**:useTheme 双轴动作经 Wails 总线广播 theme:changed(前端 Emit→Go EventManager→全窗含自收,v3 源码实证),各窗 initTheme 订阅只回写 ref、应用统一走既有 watch(同值不触发=无回环),启动校正不广播防风暴;useTheme.spec 六例锁死发起/收端/自收/垃圾载荷。真机补看:深色→浅色后轮盘/挂牌/结果卡即时换肤 | 症状:主窗主题已切,quickmenu 轮盘浮窗残留旧(深色)配色。病根推断:轮盘弹窗走"按需创建+空闲 TTL 真销毁"(1822a39),**TTL 内存活的窗体不会响应主题变更**——主窗换主题只重设自己的 DOM tokens,无广播给浮窗 webview;浮窗创建时读持久化主题,但创建后主题再变即脱钩。修法(实施先复现钉死):①主题切换处(双轴 9adb7cb 落点)广播 `theme:changed` 事件,**所有活着的浮窗/挂牌窗/预览窗 webview 即时重应用 tokens**(Wails Events 天然可达,QuickMenuPopup.vue 订阅重套);②连带核浮窗族同源病灶:留言板全屏挂牌(N6 玻璃牌面)、OCR 悬浮结果卡(Acrylic 9c1e288)、轮盘实时预览盘、SnipCard——一处广播全员响应,勿逐窗打补丁;③Win11 Acrylic/强调色若随明暗变(dark-mode acrylic tint)需同步重设,不只 CSS 变量;④DWM 标题栏同色(c26e4e7)逻辑对浮窗是否生效一并核(轮盘 frameless 应无标题栏,确认即可)。兜底策略:若浮窗即时重套在某些窗体不干净,退为"主题切换即销毁全部在途浮窗,下次唤起重建"——TTL 架构下销毁重建是现成语义,更诚实。小活半天,归 W5/真机验收并段 |
| N40 | **轮盘命中区全扇面化 + 圆心静区去闪烁 + 对标 StarPie 美化**(机主诉求 2026-09-23:"现在感觉有点不舒服") → **🔄 ✅ 代码收口(①② 2026-09-24;③机主批"全B"已落 2026-09-25 P2 1f07984+P1 29dafa1——VIS 观感常量与命中物理隔离、六条全改、红线自查通过;真机验收点五条已录)**:①②命中改几何单点路由——`onPointerMove` 内 `resolveHover` 按名义角域 `slotOf`(入 wheelGeometry 纯函数,前后端几何同源)极坐标归属,牌面任意落点(含 pad 缝隙/盘缘死区)点亮唯一槽、圆心 `r<rSecIn-迟滞` 清全部悬停、弃 `<path>` DOM mouseenter/leave 根治楔形缩放+button 抢命中的交错闪烁;±4 迟滞、楔形→帽带无缝穿越;高亮与 dwell 开收解耦、键盘触屏不动。真机补看全扇面/圆心去抖。**③对标 StarPie 美化样张已出([PLAN_N40_WHEEL_SKIN](PLAN_N40_WHEEL_SKIN.md)+specimens/n40-wheel-starpie.html,6 差异点 7 拍板点)待机主选向**——需先出现牌面 vs StarPie 化对比稿给机主选向再动皮(与 N5 编辑器内嵌建议一次收口)。**2026-09-26 追加**:双账批三笔见 N5/N41 行;编辑器内嵌一议已被 011a60a scope 化方案兑现;N37 延入本卡域挂一笔待拍板——QuickMenuPopup.vue:753/896 两枚 drop-shadow 借值是否纳入 glow 全线废除范围,待机主明示 | 三条:①**全扇面触发**——鼠标落在扇形任意位置(含牌面边缘空白)都要点亮+可点,现状疑似只有图标/文字热区响应(元素级 hover),牌面大片区域是死区;②**圆心静区**——指针移入中间圆心底座范围应清除一切选中/悬停态(回 idle),现状圆心区边缘与扇区内端交界处出现闪烁(hover 态来回抖动);③**样式美化**——对标 StarPie(Star-Pie/StarPie,N5 已核实的高置信参照,PLAN_N5_QUICKMENU v2 在册)。落点 `QuickMenuPopup.vue`+`useWheelRingState.ts`+`wheelGeometry.ts`:命中改**几何判定统一路由**——pointer 坐标→极坐标(半径 r/角度 θ)→环号+扇号,牌面渲染 hover 态由命中结果驱动(pointermove 单点权威,弃 DOM hover;与 1822a39 左键态轮询双通道收口对齐);**前后端几何必须同源**(r 阈值/扇区边界两侧共用一份函数)——#50"512 DIP vs 376 viewBox 不同源"欠账与 N5 既有改造同批清偿,否则命中永远比视觉差一圈。圆心判定= r<内半径 强制 hover=-1;闪烁排查补 hysteresis(交界处 2~3px 迟滞)防抖动。美化先出对比稿(现牌面 vs StarPie 化)给机主选向再动皮。与 N5 编辑器内嵌同模块同窗,合计建议一次收口 |
| N41 | **轮盘与托盘配置收敛单一来源** → **✅ 对账收口(现状已满足,2026-09-24 自主核查;无需重复迁移)**:历史登记把「轮盘配置」与「托盘配置」误写成两套账；当前源码实证 `QuickMenuService` 与原生托盘都读 `settings.Store.TrayMenu`，分发共用 `internal/launcher`，`QuickMenuView` 明确只读、唯一编辑面是设置→托盘菜单 `TraySection`，不存在第二份轮盘账本或待迁移文件。保留的真实余项归 N5/N40：编辑器体验/展示开关若要增加，需在这份 `TrayMenuItem` 单源模型上扩字段；模块停用后的旧引用过滤与重复引用提示另行补强，不把它们伪装成双账迁移。**2026-09-26 追加(夜,旧口径被机主推翻——双账正式落地)**:"不存在第二份轮盘账本"曾为事实,现产品裁决改为**轮盘/托盘刻意分账**,上文核查结论作历史保留、现行口径以此注为准:5964984 新 AppSettings.WheelMenu 账(缺键/null 旧档一次性深拷贝 TrayMenu 派生——出厂空切片使 nil 判据永不成立的坑走二次 decode 指针探测、注释写透;显式数组含 [] 系用户独立意图永不复活;SetWheelMenu 不触发 trayRebuild 互不干扰;热更通道=弹出时现读 store 零新事件;DeleteWebAppEntry 死引用双账各清)→ade2338(消费端各归各账:Dispatcher.WheelItems 新取账点、FilterDisabledModules 现读过滤——停用模块条目不占扇区,与托盘 Wave 1 对齐的行为变化注记在案;itemModuleID/filter 可见性收口自 tray.go 上收 launcher 共享;双向隔离测试焊死"改一盘另一盘纹丝不动")→011a60a(编辑宿主 scope 切换、双宿主"共用本配置"文案全改"各记各账互不影响")。本卡"真实余项"之旧引用过滤已由 ade2338 交付;重复引用提示=轮盘组内判重转挂 N5 批次。 |
| N42 | **OCR 轮盘识别悬浮结果卡支持缩放**(机主诉求 2026-09-23) → **✅ 代码收口(2026-09-24 自主批,两解全做)**:取证订正登记语境——"轮盘卡 vs snip 卡两副面孔"不实,两者同一张卡(SnipCardView),dce4be3 拖拽把手本就在,①无需做;真缺口补齐:②窗口缩放=frameless 新原语 QueryWindowRect/ResizeWindowTo(物理像素直连 user32)+startCardResize 会话(与拖拽同构三重退出)+收口按 ScaleFactor 换算 DIP 回写记账并 ocr.json 持久化,下次弹出复现+工作区钳位;③字号缩放=A∓ 步进/Ctrl+滚轮(12~32px 钳制,localStorage 记忆,默认档=--text-md 零变化,Number(null)=0 坑测试钉死)。两态正交独立记忆。真机补看:右下角拖大小、跨重启记忆、Acrylic 无白边 | 现状:轮盘框选识别的悬浮结果卡(b99a41c 落地的"系统截屏接图+悬浮结果卡+自动复制")**不可拖拽也不可缩放**——注意它与截图 snip 卡是两副面孔:跟手拖拽把手(dce4be3)只长在 snip 卡上,轮盘卡没接过;尺寸固定,长文本只靠内部滚动,读小字费劲。修法:①**拖拽**——把 dce4be3 的把手移动原语(`GetWindowRect`/`MoveWindowBy`+左键态轮询双通道收口)收编为共享件,两卡同装,别再各写一份;②**缩放**两解一并做:①**卡片窗口缩放**——右下角/边缘 drag resize(FramelessWindow 原语补 WM_NCHITTEST 边界命中或把手同族),尺寸持久化记忆(下次唤起跑上次大小),设最小/最大钳制与出屏回收;②**内容字号缩放**——卡片内 `Ctrl+滚轮`/± 按钮步进字号(结果文本 CSS 变量一处控),两态正交独立记忆;浮窗纪律连带核:缩放后仍守真透明无边框(98f3bce 白边教训)、TTL 空闲释放不因拖拽缩放误触发(1822a39 交互态刷新活跃)。"缩放"具体指哪种待机主一句话钉死,登记按全做(工作量都不大,合计一天内) |
| N43 | **状态真相补刀:未安装的不得谎报"未运行"**(机主报告 2026-09-23) → **✅ 代码收口(2026-09-23 自主批,真机复验随 P1 并段)**:notInstalled 判定+文案+hint 收在 managed store 契约层单源(词表接 SUMMARY_META['not-installed'] 冻结同源);primary 未装直落版本页(Shell goVersions 接线,独立挂载降级 toast 指路);running/external/quitting/failed 运行事实优先不遮盖(④口径);七视图夹具按病灶语义补已装记录、真未装用例显式传 [] 不受波及;store 层新 spec 钉三态。**2026-09-26 追加**:a9c2265 幽灵健康账四环根治——同主题从"运行态不得谎报"延伸到"健康账不得骑幽灵"(机主实证 ddns-go 卸载后仍挂"运行中·有更新",tasklist/sc/端口三路取证确无残留,纯账目不联动):①Uninstall 成功即 SetHealth 清零(同会话重装不复活);②Restore 回灌前按在册+installed 对账剔账回写(CheckedAt 不伪造);③sweep/merge 落盘前逐条复查 installed 盖住"判定后落盘前"竞态;④projectState 账目门 absent 一律按 current 出投影——updatewatch/Registry 层一处治同构家族病,不在单模块打补丁;踩坑 #93 四段式;残留已知窗口如实:仅删版本目录不卸模块时角标停留到下一轮感知(缓存时效语义非账目病) | 症状:多个托管软件从未下载安装,界面却显示"未运行"——机主被迫逐个点启动才发现根本没人装过;"未运行"与"未安装"是两个事实,前者暗示"可以起"。背景:P0 批 3 已做状态真相三收口(stale/localResolved/版本互认,25d0ce2)+四维状态统一词表(1831ed9)——**词表与契约都在,是消费面渲染短路**:把"无 running 态"直接兜底成"未运行",未先过 installed 维度。修法:①排查全部消费面(首页模块卡/模块中心/托管家族视图状态条/rail/托盘菜单),展示运行态前先过安装态闸:**未安装→"未安装"中性徽标+引导下载安装;已安装未启动才叫"未运行"**;②启动按钮联动:未安装态点击直落版本页,不发起启动 RPC 吃错;③收口在共享契约层(adapters 家族/managed console 契约 statusOf 纯函数)单点判定,禁 25 视图各修;词表若缺"未安装"档则 Go/TS 双端补档(1831ed9 门禁会逼齐);④连带核 N17 嫌疑③同源:运行探测失灵的 external/提权实例不得混入"未运行"桶。半天~1 天,真机验收并段顺路复验 |
| N44 | **piik 托管集成立项**(机主点名 2026-09-26) → **⏳ 在途(五线:档案+A/B/C/D;档案线已入库 9b17d27,实施四线 18:3x 已铺满工作区未提交)** | 上游 `TNTcraftHIM/Piik`(MIT,LAN 协作工具;22 版/14 天日更风暴、裸 zip 资产名 19 连版稳定、GitHub digest 覆盖率 100%)。决策档案 [PLAN_PIIK_HOSTING.md](PLAN_PIIK_HOSTING.md):托管家族首个 **headless 服务型骨架**全量裁决——无窗无信使(OpenWindow 重定=系统浏览器开 8787)、官方 stdin 优雅停+Job 兜底、8787 端口绑定事实互斥(外部占端口者可感知不抢停)、钉版手动追(followLatest 不接)、孙进程 cloudflared/piik-capture 归 Job 闭包、公网隧道 hanxi 不代管、空闲自动退出禁用、asInvoker 无 #17 三重契约、Gitee 官方镜像入回退链首位。合入序 A→B→C→D+总闸批次(THIRD_PARTY_NOTICES"全部 25 款"→26、cataloggen、bindings、路由/枚举计数锁 47→48)列档案 §⑥。**工作区实证(18:33 复核)**:A(version 七件含 check_update——第一天挂雷达契约照 gonavi/dbx 纪律)/B(instance 端口+镜像路径双探测)/C(service/store/PiikView/adapter)/D(app.go 预铺 piik.New+两事件+versionTrees、contract-enum 计数锁 47→48、vitest "缺失绑定解析缝"插件)五面全见而**零入库**;曾现的绑定探测残件 `__probe.spec.ts` 已自清;前端全量现 5 红=piik 在途半件所致(d745e70 如实自报"非本批"),合入序与总闸批次(档案 §⑥)为唯一收口点 |

> N1/N2 与 N3 同为"外部进程也能被 hanxi 退出/唤窗"诉求的具体症状:N1/N2 先真机复现修点,N3 决策收口成面。**2026-09-21 W1 已裁决收口(采纳 C+风险分档),三项调研结论与 W2 实施方法全部见 [PLAN_W1_DECISIONS](PLAN_W1_DECISIONS.md)。**

### P4 执行波次排序(2026-09-20 复核重排;执行以本节为准,上表保留原始登记与详情)

复核实测三条硬事实(原登记未覆盖,直接影响排序):

1. **artifact 包只支持 zip**:`packages/go/artifact/unpack.go` 仅有 `UnpackZip`(全套 zip 安全闸门),无 7z 解包能力。**但 W1 调研已推翻本条对 N8 的前提**:WindTerm Windows 资产实测全为 zip,门槛不存在;7z-only 场景(如未来 mpv)已归档选型 `bodgit/sevenzip`(纯 Go/BSD-3,见 [PLAN_W1_DECISIONS §4](PLAN_W1_DECISIONS.md)),触发时再实施、当前不预建。
2. **版本契约在 Go 侧并不共享**:21 个模块各有一份 `version/models.go`(`isPre` 字段重复 21 次),N13 实际成本="21+ 处字段扩散 + 前端共享层 + 版本面板";且新托管模块每先落地一个、N13 就多一处返工点。
3. **N1/N2 的卡点在决策不在代码**:`noExternalProbe` 恒报不在运行、`Quit()` external 态只回指引,均为刻意设计(管理边界=本会话进程树)。动代码前须先裁决"hanxi 是否有权控制自己未启动的进程",即 Wave 4X 的收口决策(从 N3 中拆出前置)。

| 波次 | 内容 | 成本 | 排位理由 |
|---|---|---|---|
| **W1 决策与前置调研**(半天,不写码) → **✅ 全部收口(2026-09-21)** | ①N3 决策(**已裁:采纳 C+风险分档**——`force_free` 档低损外部实例优雅后直接强杀、`confirm_force` 档打扰类须明示确认后杀、未声明默认保守档,详见 [PLAN_W1_DECISIONS §1](PLAN_W1_DECISIONS.md));②N15 查证销项(→已定可行,免费路径);③N5 核实 starpie 仓库(→Star-Pie/StarPie,高置信);④N8 的 7z 方案选型(→前提反转,WindTerm 全 zip,选型无需,归档见 §4)。成果载体:[PLAN_W1_DECISIONS](PLAN_W1_DECISIONS.md) | 小 | 四项全是后续波次的门闸,一次沟通全部裁决 |
| **W2 外部实例治理** → **代码侧收口✅(2026-09-22,余真机验收)**:公共件 `packages/go/externalquit`(5f12779,force-free/confirm-force 双档执行器,未声明默认保守档已入包文档)→ snipaste(4cfd83f:external 按名取 PID、OpenWindow 重定为官方 toggle-images 显隐贴图、Quit 分档+UIPI 提权降级)→ everything(de27a5f:-quit 信使优雅→验证→强杀兜底)+ N12(f41f1a7)。**真机验收项**:①snipaste 外部实例探测/显隐/退出;②everything 外部实例退出(优雅与强杀两路);③提权实例降级指引。catalog 级档位字段未预建(现两模块服务层显式决策即终裁口径,W4 新托管接入时如需声明再加) | N1+N2 同批按 W1 裁决(C+分档)实施:平台层"外部 PID 取得+分档退出执行器"公共件 → snipaste(external 探测+toggle-images 唤窗+`force_free` 退出)、everything(按名取 PID+优雅信号+`force_free` 强杀兜底);N12 纯文案 30 分钟搭车 | 中 | 机主真机报告的在用功能故障,痛感最高;与 P1 F10 冒烟同场跑,合并真机验收 |
| **W3 空间可见性** → **✅ 全部收口(2026-09-21)**:公共件 `packages/go/dirstats`(7bf1b3a)→ N11 数据根占用一览(46119bb)→ N14 envcheck 空间家底(f384948) | 先下沉 walk 目录统计公共包 → N11/N14 各自接线。真机验收项:家底路径族(AppData/GOROOT 推导)与占用数值抽查 | 中 | 原登记已自判同批;"新功能第一天走公共包"纪律;纯本地可开发,适合真机等待期填充 |

#### W3 真机反馈(2026-09-21 机主实测;三件当日实施完毕,待真机复验)

| # | 反馈 | 定位与方案 |
|---|---|---|
| W3-a ✅ | **重新测量时黑窗连闪** | 已修:`defaultRun` 套 `windows.HideConsole`(detect 家族本就 Windows-only,无需 stub);**并查出更深一层**——`.cmd/.bat` 分发器未如 `detect.runVersionCommand` 做 `cmd /C` 包装,命令实际可能静默走默认路径回退,已同型补齐 |
| W3-b ✅ | **versions 目录要再展开一层** | 已实现:`AppService.DataRootSubUsage(sub, force)`(安全单段名校验,扫描面锁数据根内)+ `groupBySoftware` 按首个 `_` 前缀聚合(纯函数带单测)+ `StorageSection.vue` versions 行"展开到每软件",N 版本 chip 悬停列版本目录清单 |
| W3-c ✅ | **占用行直接标删留建议** | 已实现:后端 `DirUsage.Verdict` 四档词表(keep/recommended/redownload/caution,静态建议非删除入口)+ `EnvCheckView` 徽章行;数据根一级行 `logs/memo/.snapshots/versions` 前端静态徽章(未列名目录不戴章,宁缺毋滥)。**一键清理动作仍未做**(待裁决) |
| 附注 ✅ | 小瑕疵两处 | 随 W3-a 顺收:`normalizePath`(Clean 去尾分隔符+盘符大写)统一所有推导路径;整串大小写如需彻底规一(GetLongPathName)另议 |
| **W4 托管集成批** → **N8 ✅ 代码收口(2026-09-23,待真机验收)**:windterm 模块全链接线(version GitHub 链+无摘要降级三层〔vscode 先例,实测上游 32 release 全量无 digest——登记时"现成 zip 管线零改动"不成立,artifact.Fetch 摘要必检须走 bespoke 分治〕/instance 多实例引擎+confirm-force 外部档〔N3 终裁点名〕/共享壳前端+图标+路由+契约锁)。**N9 ✅ 代码收口(2026-09-23,待真机验收,与 N8 互为抄验)**:termora 模块全链接线(version GitHub 官方摘要主链〔与 WindTerm 相反,34 release 全量带 digest,走 markeron 必检口径〕/tag x.y.z[-beta.N] 双收且 beta 即主干如实全列〔recordly 先例,不做通道藏行〕/instance 单实例信使引擎〔ApplicationSingleton CreateMutex+二次拉起 tick,ccswitch/snipaste 混合〕+confirm-force 外部档〔N3 终裁点名 Termora〕/portable data 激活器补齐/共享壳前端+server 图标+路由+契约锁)。**N4 ✅ 代码收口(2026-09-23)**:rammap 模块——日期版模型(Last-Modified 唯一诚实版本语义)+单源降级三层+提权三重契约(#17 litemonitor 同族:740 广播暂扣改写含「管理员」关键词喂 ElevateRestart、ElevationStatus 预告 RPC、UIPI blocked 如实降级)+force-free 外部档(零状态观察工具)。**W4 全部收口,余 N7 暂停件待机主取证** | 大 | N7 缘起级用途、N3 裁决落地后即可开工(先确认静默参数/升级语义);N8/N9 皆为 zip 绿色形态、同技能同管线,互相抄验效率最高;N4 提权+一次性语义开工前正式计划 |
> **N9 真机验收清单(W4)**:①远程列表 stable+beta 全量呈现、预发布徽标在位(上游 2.x 线 beta 即主干,无藏行);②下载安装全链(官方 sha256 四层校验→jpackage 嵌套根落位→data\ 激活器自动补齐)→启动/唤窗/退出三动词;③自带 JRE 确认:干净 PATH(无系统 java)也能冷启动;**单实例互斥验证**:自行打开一个 Termora 后点「启动」应被门禁如实拒绝(而非谎报成功);④外部自行启动的 Termora:external 黄标、「退出」弹风险确认后走优雅/强杀两路、提权实例降级仅指引;⑤导入本地解压目录(含 data\ 会话数据)→ 导入件 verifiedHash 如实 false、卸载运行中/使用中拒绝;⑥活动 SSH 会话下退出:关窗确认框挂起→宽限 3s→强杀兜底文案如实;⑦"随 Hanxi 关闭"开关双向。
> **N4 真机验收清单(W4)**:①远程列表恒一条"最新版(日期)"、下载安装全链(降级三层,已装未验官方哈希如实标注);②**未提权 Hanxi 启动**:应被 740 特判拒并以引导条呈现「以管理员身份重启 Hanxi」+一键 ElevateRestart 挂载;③提权态 Hanxi 启动/唤窗/退出三动词与外部实例 force-free 直退(多实例并看合法);④外部"自启且已提权"的 RAMMap:退出如实回 blocked 降级指引不谎报;⑤导入已解压目录→卸载保护;⑥上游改版(日期变)后 CheckUpdate 点亮、再装落新日期目录。
> **N8 真机验收清单(W4)**:①远程列表只显示稳定版 x86_64 zip(2.7.0 起,无 prerelease 混入);②下载安装全链(进度→落位→verifiedHash 徽章如实为"未验官方哈希"文案)→启动/唤窗/退出三动词;③退出含活动会话场景:关窗确认框挂起→宽限→强杀兜底文案如实;④外部自行启动的 WindTerm:external 黄标、「退出」弹风险确认后走优雅/强杀两路;⑤管理员权限运行的 WindTerm → 降级仅指引不谎报;⑥导入本地目录(含会话数据)→ 卸载保护(运行中/使用中拒绝);⑦"随 Hanxi 关闭"开关双向。

| **W5 体验债** | N6 msgboard 方案先行→实施(纯前端、盘子较小先做);N5 轮盘改进(前后端几何同源改造,大活,含踩坑 #50 欠账);P3 前端打磨批次一可同窗并流 | 大 | 属重设计而非修 bug,须先出方案给机主过目;出方案可与 W3/W4 并行,动代码排队 |
| **W6 契约收口** | N13 全平台安装包+形态标注,一次扫过 21+ 模块 models 与前端契约 | 大 | 后置于全部新托管落地,避免逐模块返工;与 P0 批 3(managed 前端)同域,排在批 3 之后;开工前正式计划评审 |
| **W7 长线** | N16 随手记 A→B→C;N10 系统信息新模块(独立无依赖,可作任意波次并行候选) | 大 | A 批同步语义宜等 N7(及 F3)收口后设计,与飞牛合流;N10 为借鉴型痒点,痛感低于其余,不占主线窗口 |

与原登记的关键差异:N3 拆"决策前置 W1 / 族推广后置于 N1、N2 验证";N8/N9 曾按"7z 门槛"解绑为 N9 先行,**W1 调研推翻前提(WindTerm 发布全为 zip),已恢复原"同批互相抄验"**;N12 从"与 N4 同批"改为搭 W2 便车(现成功能文案缺漏没理由等 RAMMap);真机类任务并段(N1/N2+F10),控制 Linux 开发环境与 Windows 真机的往返成本;与 P0 质量批的并行边界——W2 在实例管理层、P0 批 1/2 在 registry/租约层不冲突,仅 N13(W6)与 P0 批 3 同域需后置。

#### 机主实时反馈与页面重设计落账(2026-09-25/26 自主批)

- **版本"大小"口径**:✅ f20969d——dirstats 新增 MeasureBudgeted(2s 预算,超预算回 Partial),19 个托管模块 ListInstalled 改报解压目录实测体积(修 Termora 显示 500KB 实为 71.6MB),失败回退 zip 口径并记 Debug
- **Termora 首下不自动设为使用**:✅ 2ac9b3b——根因是 done 事件先于 SetActive 落账的竞态(SetActive 通道自模块诞生即在位,原"缺设置"诊断系误),改先落账后广播,service_test 3 例锁死
- **末版卸载死锁**:✅ 2ac9b3b——termora/windterm/snipaste/rammap 四件后端 guard 放行末版卸载并 SetActive("") 清账,前端 soleVersionUninstallNote 确认框如实预告(家族扫描:其余模块本就放行)
- **存储目录页展开子行分不清**:✅ c766a25——34px 缩进+2px 归属左线+单版本 chip(N19 追加)
- **WSL 本机发行版「更多」下拉被裁**:✅ 6f1f7db——行菜单 Teleport+fixed 翻转定位,表格 min-width 760px 横向滚动兜底(踩坑 #87 口径)
- **快捷菜单模块页重设计**:✅ 9b67179——状态参数 chips+编辑主场双栏+次级折叠
- **留言板模块页重设计 v2**:✅ 6306617——挂撤动作升唯一主角(N6 追加)
- **轮盘命令格全是通用方块**:✅ efe3e31——命令扇区图标解析归属模块真图标
- **开源上游真图标四枚**:✅ d0d551a——rustdesk/bili23/termora/nanazip(N27 追加);recordly/vscode 许可红线不入库已向机主说明
- **当日午后并流收口**:N31-B(见 N31 行)、随手记 A/B/C 三批(见 N16 行)、按钮配色 N38(609479a)、轮盘 v3+皮肤(788b95e/55118d9)、留言板 v3(396bd9c+文案人话化 4682878)、N36 字体三件套(b6e07e2/a256a9c/fcdb5af 切换)、竞态 21/21(41c6668/717a543/2ac9b3b 系)、TTB 自动诊断(068f01f/72ca148 踩坑 #90)、softver 雷达接入(b3cc7e7)、git 配置查看(50fadf8/c456ae1)、图标升切+变体+红线运行期(1c7e3af/19319c2/7e2c73b)、§5 九项复核+F-a/b(3d81940/78df697)

#### 2026-09-26 午后并流批余账与移交（第二批落账）

- 新增工具面尾账：**F-c**（release.yml 传 APP_VERSION+Verify 补前端闸+info.json 动态版本）涉 CI/CD 配置=红线待授权；**F-d**（托管在途操作+强杀→进程树零残留）挂 P1 真机；竞态家族 `-race` 补跑欠（本机无 gcc/cgo=0，happens-before 设计兜底）。
- 观察票余档：随手记 B 批"纯模糊匹配"未做（时间窗+检索条已兑现大半）、memo:changed 增量游标、EnvCheck 日志帧批量收编、可点 chip 五份形近副本统一、软底输入 N3x 立项建议、模态焦点陷阱、public/Inter-Medium.ttf 308KB 死重删否（待机主一字）。
- N31 行"已装不重装"与 N16 行"C 批写工具 ADR 否决"两处为设计终态非欠账，勿再当缺口复查。

#### 2026-09-26 晚间并流批第三批落账(本件只动本文档;PRD/FEATURES 归并行线未碰)

> **账至** `d745e70`(18:31)。788b95e 以来共 60 笔:截至 f9d3c36 的 19 笔第二批(133 行+余账块)已销其十三、漏网六笔中 9b9acad/d9f763c/9da419b/e486674/1a9ffb3 由本轮行内追加吸收(e511555 属审查文档自带落账);f9d3c36 之后 **41 笔全部本轮落账**。撰写期间并行线仍在推进,44efe65/a0b39e9/f6ee44b/9b17d27/6617b75/d745e70 六笔均属账至前最后一小时入库,一并收清;晚于 d745e70 的账归第四批。module catalog 计数 45→47(gonavi/dbx,b8231c2/5b34ba6);bindings 三面再生 cffc2c8(gonavi 21 法+dbx 18 法+TTB 五导出)。

- **① N37 配色六路** ✅ 全线终态(96ed8ee/81d9cb6/b9f9785/a680fd1/c98a469/7675631+9d0367f 搭车)——守卫三则(`frontend/src/styles/__tests__/buttonColorContract.spec.ts`)+样张定案执行版+§5 逐项标态;余账三笔待拍板+两笔在途,详见 N37/N40 行追加。
- **② 留言板 v4 牌桌** ✅ 闭环(8a77c03 A 路+6d1f14c B 路组件库+9e544d8 接线 -88 行),"收编在途未 import"的并流事故由主会话裁决收口;审计路 a839ced 根治+三观察挂账,详见 N6 行追加。
- **③ 轮盘/托盘双账** ✅ 三笔入库(5964984 数据/ade2338 消费/011a60a 编辑宿主),第四路 = bindings 总闸再生**未跑**——Get/SetWheelMenu 暂走交叉型护栏;N41"不存在第二份账本"旧口径被机主推翻、分账正式落地(本批最大口径反转,详见 N41 行追加);isOptionAdded 组内判重挂账(011a60a 自报体量未动)。
- **④ MCP 扫描族八键** ✅(b9f7fec):六键→八键读写对拍 256 组矩阵、有界三层(≤256 端口/≤1024 地址/60s 墙钟 completed:false"未列出≠关闭")、单飞独立闸、instructions/ADR 决策1 诚实修订、真件 e2e tools/list 九工具+默认关闸零发包;踩坑 #94/#95 沉淀。**端口查杀=按 #94 预裁未注册**;晚间工作区已见 E 路在途(tools_portkill+destructive.json 机主总闸、portkill 键刻意不进 AI 面板、mcpwizard 面与 bindings 局部再生)——未入库,未注册态维持。
- **⑤ 随手记夜批** ✅ 五笔全收(c0da60d 更名/378d006 悬浮卡/f61060c M4 组件库/52ef2de 主界面 v2/d745e70 接线收口)——masked 严口径(MemoCard 双明文面不渲染+title 属性不挂)裁决定稿全链贯通、f61060c 删除单当日销尽,详见 N16 行追加。
- **⑥ webapp 默认打开形态** ✅ 已入库 44efe65(18:12):DefaultOpen 空=window 存量零迁移、轮盘/托盘点击现读分派(写侧严格读侧宽松)、SetEntryDefaultOpen 受控导出、e2e 双钉(含"Ref 一份两账同效"形状);任务口径"在途"已被事实追平改记 ✅;PRD 网页应用行随该笔自增补(并行线自有账);bindings 总闸 +1(SetEntryDefaultOpen)未跑。
- **⑦ 新托管双模块 + TTB 双形态** ✅:gonavi(04a6d9e/9f2cb85/1e82b0a+b8231c2 夹具收口)+ dbx(b0b0499/5b34ba6/5ebf41b)——**漂移护栏先例首现**(落位记实测 sha256+mtime 闸控复算 hashDrifted/driftNote,上游自更新原地换 exe 如实显形不代处置)、UpdateChecker"第一天挂契约"自本批起成铁规(softver 教训口径)、9f2cb85 自报战线遭伪装注入指令未从(纪律留史);TTB 双形态三笔+归因补票见 N17 行(5947e25/f43adb5/0521bc5/6f36b12+068f01f/72ca148 话术面);7db423a(gofmt 欠账)/500d697(NanaZip 接线补——断点在接线非能力)卫生搭车。
- **⑧ 幽灵健康账** ✅ a9c2265 四环根治 + 踩坑 #93(详见 N43 行追加)。
- **⑨ Piik 立项** ⏳ 新立 **N44** 行;档案线 9b17d27 入库、实施四线满在途未提交。
- **⑩ 更新雷达口径复核**(实扫 `internal/app/updates_service.go:37` collectUpdateCheckers 面,取代 #90 的历史快照口径):HEAD 实数=**26 收集器**(托管 25 家+softver);托管 version 线家族 **30 家**、未挂契约 **5 家**——douzy 系设计排除("仅版本+下载"a6d4fd9 口径不接雷达),**frpc/nanazip/rustdesk/subnetdesk 四家契约缺席且无因由注释=真欠账**(承 #90 避坑令:不实现=静默缺席无编译提醒);webapp 非 version 线模块、雷达面无此项;piik 落库后应为 31 家/27 收集器(A 线在途 check_update.go 已见,第一天口径照挂)。

**本轮其他落账(133 行漏网吸收)**:f6ee44b 快照容量口径双写对账 ✅(internal/snapshot/capacity.go 单源 ↔ snapshotLabels.ts REVISION_WINDOW/BACKUP_KEEP,跨语言 contract spec 焊死、裁决不为两数新开 Wails 导出——N33 批 D 容量句之数字卫生);a0b39e9 envcheck 日志帧观察票**销案**(同病前提不成立:单 pre 节点非逐行 v-for、实测 23µs;顺手补"封顶200"名不副实用例缺口)——第二批"观察票余档"首项清零;b82c87e/3dd5fd8 为 #90 成对落账(见⑩);e486674/1a9ffb3 见 N27 行;d9f763c/9b9acad/9da419b/6617b75 见 N13 行;e511555 审查状态头订正属 CODE_QUALITY 文档自有落账、本文不重记账。

**在途线(工作区未提交——只登记不销账,以各线自身合入+总闸批次为准)**:① piik 实施四线(version/instance/service/view/adapter/app.go 预铺/计数锁 47→48/vitest 缺失绑定解析缝,前端全量现 5 红=piik 半件所致,d745e70 自报);② 端口查杀 MCP E 路(mcp/mcpwizard 全面+bindings 局部再生)。

**欠账与待拍板总表(收口)**:**F-c**(release.yml 传 APP_VERSION+Verify 补前端闸+info.json 动态版本,涉 CI/CD=红线)待机主授权名义不动;**frontend/public/Inter-Medium.ttf 308KB 死重**仍待机主一字裁决(晚间实扫未删);**bindings 总闸一跑合四欠**——wheel Get/SetWheelMenu、webapp SetEntryDefaultOpen、piik 面(档案 §⑥)、portkill E 路(若入库),everything form typedef 已由 e486674 收清不算欠;N37 三笔待拍板+两笔在途;留言板三观察(chip 闪断/pending 窗口/上限双写)待解禁;轮盘 isOptionAdded 组内判重;雷达契约缺席四家(frpc/nanazip/rustdesk/subnetdesk);THIRD_PARTY_NOTICES"全部 25 款"计数待 piik 总闸改 26;第二批其余观察票(纯模糊匹配、memo:changed 增量游标、可点 chip 五份形近副本、软底输入立项、模态焦点陷阱、竞态家族 -race 补跑)状态原样维持未动。

**账目勘误(只登记不改他人笔迹)**:① #90 与 b82c87e 的"达标 16/19"——所据"§5 #8 复核"实文本系进程树残留议题,数字与任一时点盘面均无法对上(同日 14:38 实扫应为托管 23/28+softver),以⑩复核口径为准,TROUBLESHOOTING #90 补注属他人文档本件未碰;② N37 行与 133 行两处把 609479a 写作"N38 按钮家族/按钮配色 N38"——N38 是 softver 微信下载安装包卡,609479a 实归 N37 域,待并批订正;③ 任务口径"双账四路"盘面为三笔,第四路(总闸)未跑,按"三笔入库+一路挂账"如实落;④ 任务口径"webapp 在途"——44efe65 已入库,升 ✅;⑤ 各笔 commit 体内"19 家/21 家"等家族计数皆为当日快照口径,托管总账现值以 catalog 47 项与本行⑩为准,勿互引为据。

## 路线参考(未排期)

- 中长期三阶段(搬家与数据真相 → 统一治理 → 灾备与恢复)见 [FUTURE_FEATURE_RECOMMENDATIONS](../FUTURE_FEATURE_RECOMMENDATIONS.md);仅方向,非承诺。
- Wave 4X(外部实例控制)维持延期;manifest、签名 Catalog、物理拆包为 N/A 条件路径(ADR-0003),非默认欠项。

## 复进建议顺序

1. 提交三份文档、执行 worktree 清理(收尾本次暂停);
2. P1-1 F10 冒烟(半小时级,先清最便宜的债);
3. P0 批 1(竞态 + receipt 设计)→ 批 2(租约/journal/Commit)→ 批 3(前端真相);
4. P1-2/3 真机与黄金路径验收;
5. P2 文档漂移随各批提交顺带收口;P3 视意愿插空恢复。
6. P4 复进日新增项按上方"P4 执行波次排序"W1→W7 执行,真机类(W2/F10)并段验收。
