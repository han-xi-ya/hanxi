# 供应链冷却策略说明（对应 docs/PROJECT_REVIEW_2026-09-08.md E05）
#
# 此前此处配置 minimum-release-age=10080，但 npm 不认识该键——它不会执行、
# 不会尊重，还会在每次调用时告警，构成「纸面门禁」，故予以移除。
#
# npm 工具链下的实际防线：
#   1. package-lock.json 随仓库提交，版本与 integrity 哈希全量钉死；
#   2. CI 使用 `npm ci` 按锁文件精确安装，不现场重新解析版本；
#   3. 本地更新依赖时，用 npm 原生 --before 手动套用 7 天冷却：
#      PowerShell: npm install --before="$((Get-Date).AddDays(-7).ToString('yyyy-MM-dd'))"
#
# 若将来切换 pnpm（>=10.16），可恢复配置 minimum-release-age=10080 获得原生冷却。
