// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

import * as McpWizardService from "./mcpwizardservice.js";
export {
    McpWizardService
};

import * as $models from "./models.js";

/**
 * AccessInfo access.json 只读呈现（写入口归 F4a 引擎，本服务绝不写）。
 * @typedef {$models.AccessInfo} AccessInfo
 */

/**
 * AccessTools 授权四开关（PLAN §6 固定键名）。
 * @typedef {$models.AccessTools} AccessTools
 */

/**
 * ClientState 单个客户端配置文件的状态行。State 字面量：not-installed /
 * installed / needs-repair / conflict / blocked。
 * @typedef {$models.ClientState} ClientState
 */

/**
 * DiffLine 预览差异行（绑定 DTO）。Kind：keep / add / del。
 * @typedef {$models.DiffLine} DiffLine
 */

/**
 * OpResult 写操作结果（含回滚信息）。
 * @typedef {$models.OpResult} OpResult
 */

/**
 * ServerInfo MCP server 启动命令（写入客户端配置的 command/args）。
 * @typedef {$models.ServerInfo} ServerInfo
 */

/**
 * WizardPreview 安装/卸载预览。Allowed=false 时看 Reason 与 ManualSnippet；
 * Token 供确认调用回传（预览后文件被改则确认整体拒绝）。
 * @typedef {$models.WizardPreview} WizardPreview
 */

/**
 * WizardStatus 分区页初始状态整包。
 * @typedef {$models.WizardStatus} WizardStatus
 */
