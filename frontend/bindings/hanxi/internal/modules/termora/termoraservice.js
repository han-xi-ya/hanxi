// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * TermoraService 面向前端的 Termora 托管服务：GitHub releases 版本管理、
 * 本地导入与会话内启停。外部实例感知为拉取式（GetStatus 读取前复探，
 * snipaste/windterm 同型——引擎状态回调即时推送，无需后台轮询 goroutine）。
 * 所有业务 RPC 方法经 holder.Enter() 接入统一调用门（Wave 3）：
 * 未安装/停用/阻止模块的任何方法调用被拒，且调用在途期间停用会等待 drain。
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as instance$0 from "./instance/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as version$0 from "./version/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * DownloadVersion 异步下载：立即返回 "started"；已装返回 "already-installed"、
 * 同版本下载中返回 "in-progress"（不报错，前端按状态渲染）。进度经
 * "termora:version-download" 事件推送；全程持后台租约事务（P0 批 2b）——
 * 模块停用/应用退出即时取消，journal 以 operation-cancelled 收口。
 * 首个版本装完自动设为使用版本。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function DownloadVersion(targetVersion) {
    return $Call.ByID(2413007879, targetVersion);
}

/**
 * GetActiveVersion 返回使用版本号，未设置时为空串（error 恒 nil，统一前端签名）。
 * @returns {$CancellablePromise<string>}
 */
export function GetActiveVersion() {
    return $Call.ByID(544575927);
}

/**
 * GetFollowOnExit 返回"随 Hanxi 退出一起关闭"开关值（默认 false）。
 * @returns {$CancellablePromise<boolean>}
 */
export function GetFollowOnExit() {
    return $Call.ByID(544246391);
}

/**
 * GetStatus 返回引擎状态快照；读取前做一次外部探针校正（瞬时枚举，
 * 内核在运行/启动/退出中自行短路——external 感知为拉取式，前端定时刷新即得）。
 * @returns {$CancellablePromise<instance$0.Snapshot>}
 */
export function GetStatus() {
    return $Call.ByID(2227077565);
}

/**
 * ImportLocal 导入本机已有 Termora 便携目录为托管版本（data\ 会话与密钥
 * 数据随目录收纳）；未设使用版本时自动激活导入结果。
 * @param {string} srcDir
 * @returns {$CancellablePromise<version$0.TermoraVersionInfo>}
 */
export function ImportLocal(srcDir) {
    return $Call.ByID(457587691, srcDir);
}

/**
 * ListInstalledVersions 扫描本地已装版本目录。
 * @returns {$CancellablePromise<version$0.TermoraVersionInfo[] | null>}
 */
export function ListInstalledVersions() {
    return $Call.ByID(2934614328);
}

/**
 * ListReleases 拉取 GitHub releases 列表（10 分钟缓存；stable+beta 全量，
 * 预发布行由面板徽标如实呈现）。
 * @returns {$CancellablePromise<version$0.TermoraRelease[] | null>}
 */
export function ListReleases() {
    return $Call.ByID(2628767847);
}

/**
 * OpenDir 用资源管理器打开版本目录（先校验存在，explorer 自身不报错）。
 * @param {string} dir
 * @returns {$CancellablePromise<void>}
 */
export function OpenDir(dir) {
    return $Call.ByID(2661206564, dir);
}

/**
 * OpenRepository 在默认浏览器打开上游仓库。
 * @returns {$CancellablePromise<void>}
 */
export function OpenRepository() {
    return $Call.ByID(1384573669);
}

/**
 * OpenWindow 窗口唤起编排中枢（单实例信使家族契约，见 instance 包注释）：
 *   - running/external：无参二次拉起信使（上游激活通道，自有/外部通用），
 *     取在跑实例实测路径；路径不可得时降级按 PID 聚焦（自有）或如实指引；
 *   - 静止态：先过外部在场门禁（信使语义下冷启动撞外部实例只会"抬轿后
 *     自动退出"，托管根本建立不了——如实拒并给处置路径），再补 portable
 *     激活器目录、解析 active 版本冷启动托管实例。
 * @returns {$CancellablePromise<$models.ControlOutcome>}
 */
export function OpenWindow() {
    return $Call.ByID(4005159725);
}

/**
 * Quit 退出实例（confirm 为 confirm-force 档授权位，vscode 安装闸同款往返）：
 *   - 外部实例：首入返回 confirm-required + 风险文案，前端全局确认框后携
 *     confirm=true 重入执行（优雅 WM_CLOSE → 验证 → 同意后强杀）；提权目标
 *     UIPI 拦截如实降级指引；身份不明（探针无 PID）保守回指引；
 *   - 自有实例：分层退出（WM_CLOSE → 宽限 → JobObject 兜底）——退出钮本身
 *     即用户第一人称"明确要关"，不再二次弹窗。
 * @param {boolean} confirm
 * @returns {$CancellablePromise<$models.QuitOutcome>}
 */
export function Quit(confirm) {
    return $Call.ByID(3006940666, confirm);
}

/**
 * RemoveVersion 删除本地版本；本会话正在运行该版本时拒绝（优先级最高）。
 * "当前使用版本"通常不可卸载（请先切走再卸），但它是唯一已装版本时放行——
 * 否则用户被 guard 死锁（卸掉即清空、不卸又不同意），卸载成功后清空 active
 * 回到"未指定"（与 bcu 家族"卸载清空、冷启动回退最新已装"语义对齐）。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<void>}
 */
export function RemoveVersion(targetVersion) {
    return $Call.ByID(2755062691, targetVersion);
}

/**
 * RepositoryURL 上游仓库地址（error 恒 nil，统一前端签名）。
 * @returns {$CancellablePromise<string>}
 */
export function RepositoryURL() {
    return $Call.ByID(1234854074);
}

/**
 * SetActiveVersion 切换使用版本；ResolveExe 确认 payload 在场后才落盘。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function SetActiveVersion(targetVersion) {
    return $Call.ByID(4134275435, targetVersion);
}

/**
 * SetFollowOnExit 设定开关（下次启动生效）。
 * @param {boolean} next
 * @returns {$CancellablePromise<string>}
 */
export function SetFollowOnExit(next) {
    return $Call.ByID(2979085099, next);
}

/**
 * Shutdown RPC：经调用门取 operation lease 后执行收尾（与内部版 shutdown 同语义）。
 * 停用/阻止态下被门拒属预期——OnDestroy 路径走内部版，不经本入口。
 * @returns {$CancellablePromise<void>}
 */
export function Shutdown() {
    return $Call.ByID(259830655);
}
