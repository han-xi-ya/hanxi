// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * LiteMonitorService 向前端暴露 LiteMonitor 版本管理与实例控制能力。
 * 监控条/主题/插件等全部配置操作不内嵌：打开 LiteMonitor 自有界面完成
 * （纯托管决策——其桌面常驻形态即产品价值，内嵌重做性价比为零）。
 * 
 * 与 ccswitch 引擎的适配差异（上游契约侦查结论，详见 instance 包注释）：
 *   - 单实例互斥体名随安装路径派生 → 探测/唤窗全部走进程枚举 + Win32 直操作；
 *   - manifest requireAdministrator → 未提权拉起特判文案指引；
 *   - 常驻监控定位 → 不做空闲自动退出（与 FlClash 同产品约束：后台监控
 *     正是其用途，空闲即退语义不成立）。
 * 
 * 所有业务 RPC 方法经 holder.Enter() 接入统一调用门（Wave 3）：
 * 未安装/停用/阻止模块的任何方法调用被拒，且调用在途期间停用会等待 drain。
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as instance$0 from "./instance/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as version$0 from "./version/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * CreateDesktopShortcut 在桌面为当前使用版本创建快捷方式（同名覆盖）。
 * @returns {$CancellablePromise<void>}
 */
export function CreateDesktopShortcut() {
    return $Call.ByID(3984294413);
}

/**
 * DownloadVersion 后台下载指定版本：立即返回，全程经事件 litemonitor:version-download
 * 推送进度；同时开一笔 journal 托管事务（install 首装 / update 向已托管工具链
 * 追加版本，managed-declarative 资产形态）——journal 先落盘再副作用，进度阶段
 * 迁移逐步 Advance，收口经观察面 Handle 自动落账并广播 operation:changed
 * （与既有模块事件双通道并行，Wave 4-B 接线，markeron/ccswitch 同构）。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function DownloadVersion(targetVersion) {
    return $Call.ByID(100762295, targetVersion);
}

/**
 * GetActiveVersion 返回当前设定版本（空字符串 = 未指定，冷启动自动用最新已装）。
 * @returns {$CancellablePromise<string>}
 */
export function GetActiveVersion() {
    return $Call.ByID(2606773447);
}

/**
 * GetFollowOnExit 返回"随 Hanxi 退出一起关闭"开关值（默认 false）。
 * @returns {$CancellablePromise<boolean>}
 */
export function GetFollowOnExit() {
    return $Call.ByID(3917559527);
}

/**
 * GetRuntimeStatus 探测系统 .NET 桌面运行时（框架依赖版 LiteMonitor 的可运行性判断，
 * 前端据此展示"需安装 .NET 8 桌面运行时"条件提示条）。
 * @returns {$CancellablePromise<$models.RuntimeStatus>}
 */
export function GetRuntimeStatus() {
    return $Call.ByID(2694319431);
}

/**
 * GetStatus 返回引擎当前状态快照（先做一次静止态外部校正，弥补 5s 轮询间隙的即时性）。
 * @returns {$CancellablePromise<instance$0.Snapshot>}
 */
export function GetStatus() {
    return $Call.ByID(2722165677);
}

/**
 * ImportLocal 导入本地已解压的 LiteMonitor 便携套件（settings/themes/plugins 整套迁移）。
 * 运行中的实例拒绝导入：Windows 下运行中的 exe 文件被独占，拷贝必然失败。
 * @param {string} srcDir
 * @returns {$CancellablePromise<version$0.LMVersionInfo>}
 */
export function ImportLocal(srcDir) {
    return $Call.ByID(153618811, srcDir);
}

/**
 * ListInstalledVersions 获取本地已安装版本列表。
 * @returns {$CancellablePromise<version$0.LMVersionInfo[] | null>}
 */
export function ListInstalledVersions() {
    return $Call.ByID(2133790696);
}

/**
 * ListReleases 获取远程可用版本列表（多镜像回退，10 分钟缓存）。
 * @returns {$CancellablePromise<version$0.LMRelease[] | null>}
 */
export function ListReleases() {
    return $Call.ByID(3331990999);
}

/**
 * OpenDir 在资源管理器中打开版本隔离目录（"打开位置"按钮）。
 * 收口至 windows.RevealDir：非空与目录存在性校验及中文报错内置，explorer.exe <dir> 直启；
 * 刻意不走 explorer.exe <file> 的"执行"语义（markeron「打开安装目录」按钮的事故教训）。
 * @param {string} dir
 * @returns {$CancellablePromise<void>}
 */
export function OpenDir(dir) {
    return $Call.ByID(4032724884, dir);
}

/**
 * OpenRepository 用默认浏览器打开上游仓库页面。
 * @returns {$CancellablePromise<void>}
 */
export function OpenRepository() {
    return $Call.ByID(1367475605);
}

/**
 * OpenWindow 窗口唤起编排中枢：
 *   - external：进程枚举拿外部 PID → EnumWindows SW_RESTORE+SetForegroundWindow
 *     直接唤窗（LiteMonitor 二次启动静默退出不唤窗，不能走信使）；
 *   - running：自有实例同样直操作窗口；
 *   - stopped/failed：解析 active 版本直接无参启动（启动即显示监控条）。
 * @returns {$CancellablePromise<$models.ControlOutcome>}
 */
export function OpenWindow() {
    return $Call.ByID(3527545309);
}

/**
 * Quit 退出引擎托管的 LiteMonitor。
 * external 状态不越权强杀（进程枚举不持句柄）：仅返回人性化指引。
 * @returns {$CancellablePromise<$models.QuitOutcome>}
 */
export function Quit() {
    return $Call.ByID(1147617450);
}

/**
 * RemoveVersion 卸载指定版本（正在运行的版本拒绝卸载）。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<void>}
 */
export function RemoveVersion(targetVersion) {
    return $Call.ByID(900202675, targetVersion);
}

/**
 * RepositoryURL 上游 GitHub 仓库地址（页面展示与复制）。
 * @returns {$CancellablePromise<string>}
 */
export function RepositoryURL() {
    return $Call.ByID(460718634);
}

/**
 * SetActiveVersion 设定使用版本（先校验已安装，再持久化）。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function SetActiveVersion(targetVersion) {
    return $Call.ByID(3340363739, targetVersion);
}

/**
 * SetFollowOnExit 设定开关（下次启动生效）。
 * @param {boolean} b
 * @returns {$CancellablePromise<void>}
 */
export function SetFollowOnExit(b) {
    return $Call.ByID(3660683643, b);
}

/**
 * Shutdown RPC：经调用门取 operation lease 后执行收尾（与内部版 shutdown 同语义）。
 * 停用/阻止态下被门拒属预期——OnDestroy 路径走内部版，不经本入口。
 * @returns {$CancellablePromise<void>}
 */
export function Shutdown() {
    return $Call.ByID(3450473807);
}
