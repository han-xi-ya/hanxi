// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * SubnetDeskService 向前端暴露 SubnetDesk 版本管理与启停/唤窗能力，双形态纳管：
 *   - 便携版（隔离目录 packer exe）：全托管，JobObject 启停联动；
 *   - 安装版（MSI 系统安装）：Hanxi 负责取包校验、发起上游安装向导、注册表
 *     探测识别与客户端拉起/唤窗；Windows 服务不归 Hanxi 管辖（无人值守
 *     的被控常驻正是它的价值），卸载引导至系统设置、不代执行。
 * 
 * 远程桌面画面本身不内嵌（上游 GUI 完整，内嵌不可行）：
 * 连接、局域网设置（用户名/密码/CIDR 白名单/端口 21118）均在 SubnetDesk 自有窗口操作。
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as instance$0 from "./instance/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as version$0 from "./version/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * CreateDesktopShortcut 在桌面为当前使用版本创建快捷方式（同名覆盖）。
 * @returns {$CancellablePromise<void>}
 */
export function CreateDesktopShortcut() {
    return $Call.ByID(1986796573);
}

/**
 * DownloadVersion 后台下载指定版本：立即返回，全程经事件 subnetdesk:version-download 推送进度。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function DownloadVersion(targetVersion) {
    return $Call.ByID(3811975943, targetVersion);
}

/**
 * GetActiveForm 返回当前设定形态（portable / installed；未指定时为空）。
 * @returns {$CancellablePromise<string>}
 */
export function GetActiveForm() {
    return $Call.ByID(3346018279);
}

/**
 * GetActiveVersion 返回当前设定版本（空字符串 = 未指定，冷启动自动回退）。
 * @returns {$CancellablePromise<string>}
 */
export function GetActiveVersion() {
    return $Call.ByID(1687989943);
}

/**
 * GetFollowOnExit 返回"随 Hanxi 退出一起关闭"开关值（默认 true）。
 * @returns {$CancellablePromise<boolean>}
 */
export function GetFollowOnExit() {
    return $Call.ByID(1943214455);
}

/**
 * GetStatus 返回引擎当前状态快照（先做一次静止态外部校正，弥补 5s 轮询间隙的即时性）。
 * @returns {$CancellablePromise<instance$0.Snapshot>}
 */
export function GetStatus() {
    return $Call.ByID(694110397);
}

/**
 * ImportLocal 导入本机已有的 SubnetDesk 便携 exe（文件路径或所在目录均可）。
 * 配置恒在 %APPDATA%\SubnetDesk 不受导入影响；仅迁移 exe。
 * 运行中的实例拒绝导入：Windows 下运行中的 exe 被独占，拷贝必然失败。
 * @param {string} srcPath
 * @returns {$CancellablePromise<version$0.SDVersionInfo>}
 */
export function ImportLocal(srcPath) {
    return $Call.ByID(3457014507, srcPath);
}

/**
 * InstallVersion 下载并安装指定版本的安装版（MSI，后台编排）：进度经事件
 * subnetdesk:version-download（kind=installer）推送；安装本体是 Windows Installer
 * 前台向导（含 UAC），全程由用户操作，Hanxi 只等待结果并以注册表探测做事实核验。
 * 与 DownloadVersion 共用下载锁：Windows Installer 同一时刻只允许一个装机任务。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function InstallVersion(targetVersion) {
    return $Call.ByID(2268596448, targetVersion);
}

/**
 * ListInstalledVersions 获取本地可用版本列表（两形态合流）：隔离目录便携安装
 * 按版本序在前，系统安装版（如探测命中）追加于尾部——安装版全局至一条
 * （MSI MajorUpgrade 就地替换，真机实证），且不可按删目录方式卸载。
 * @returns {$CancellablePromise<version$0.SDVersionInfo[] | null>}
 */
export function ListInstalledVersions() {
    return $Call.ByID(1556135992);
}

/**
 * ListReleases 获取远程可用版本列表（多镜像回退，10 分钟缓存）。
 * @returns {$CancellablePromise<version$0.SDRelease[] | null>}
 */
export function ListReleases() {
    return $Call.ByID(216997223);
}

/**
 * OpenConfigDir 打开 SubnetDesk 的用户数据目录（身份密钥与设置所在）——纯托管下用户想看"数据在哪"的直达入口。只读导航，不改写。
 * @returns {$CancellablePromise<void>}
 */
export function OpenConfigDir() {
    return $Call.ByID(1130163510);
}

/**
 * OpenDir 在资源管理器中打开版本隔离目录（"打开位置"按钮）。
 * 刻意不复用 AppService.OpenPath：其 explorer.exe <file> 语义在文件对象上是"执行"
 * 而非"打开"（markeron 事故教训）；这里入参恒为目录，语义安全。
 * @param {string} dir
 * @returns {$CancellablePromise<void>}
 */
export function OpenDir(dir) {
    return $Call.ByID(1570404644, dir);
}

/**
 * OpenRepository 用默认浏览器打开上游仓库页面。
 * @returns {$CancellablePromise<void>}
 */
export function OpenRepository() {
    return $Call.ByID(556761061);
}

/**
 * OpenUninstallSettings 打开系统「安装的应用」设置页，引导用户卸载系统安装版。
 * 刻意不代执行 msiexec /x：卸载系统级软件（连带移除服务）属用户决策的高危
 * 操作，交还 Windows 原生入口（遵循项目敏感操作确认守则）。
 * @returns {$CancellablePromise<void>}
 */
export function OpenUninstallSettings() {
    return $Call.ByID(2149527892);
}

/**
 * OpenWindow 窗口唤起编排中枢（SubnetDesk 无唤窗信使，二次拉起 = 新窗口实例）：
 *   - external：EnumWindows 唤起外部便携实例窗口；驻留托盘无窗可唤时只给指引，不越权拉起；
 *   - running：优先唤起自有窗口；窗口已销毁（藏托盘）则派生新实例开窗（同 Job 托管）；
 *   - stopped/failed：解析 active 版本冷启动。
 * @returns {$CancellablePromise<$models.ControlOutcome>}
 */
export function OpenWindow() {
    return $Call.ByID(399714349);
}

/**
 * Quit 退出引擎托管的 SubnetDesk（终止整个进程树，含被控监听端）。
 * external 状态不越权强杀：仅返回人性化指引。
 * @returns {$CancellablePromise<$models.QuitOutcome>}
 */
export function Quit() {
    return $Call.ByID(91288314);
}

/**
 * RemoveVersion 卸载指定便携版本（删除隔离目录；正在运行的版本拒绝卸载）。
 * 仅作用于隔离目录：系统安装版不走本入口（卸载引导至系统设置，见 OpenUninstallSettings）。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<void>}
 */
export function RemoveVersion(targetVersion) {
    return $Call.ByID(1474110115, targetVersion);
}

/**
 * RepositoryURL 上游 GitHub 仓库地址（页面展示与复制）。
 * @returns {$CancellablePromise<string>}
 */
export function RepositoryURL() {
    return $Call.ByID(4248868794);
}

/**
 * SetActiveVersion 设定使用版本与形态（先校验可用，再持久化）。
 * form：portable = 隔离目录版本；installed = 系统安装版（版本须与探测一致）。
 * @param {string} targetVersion
 * @param {string} form
 * @returns {$CancellablePromise<string>}
 */
export function SetActiveVersion(targetVersion, form) {
    return $Call.ByID(982722155, targetVersion, form);
}

/**
 * SetFollowOnExit 设定开关（下次启动生效）。
 * @param {boolean} b
 * @returns {$CancellablePromise<void>}
 */
export function SetFollowOnExit(b) {
    return $Call.ByID(83085867, b);
}

/**
 * Shutdown 模块停用/应用退出：停后台轮询 + 终止自有实例。
 * 外部实例不受影响（非我方托管）；自有实例另受 JobObject KILL_ON_JOB_CLOSE 内核兜底。
 * @returns {$CancellablePromise<void>}
 */
export function Shutdown() {
    return $Call.ByID(3048288383);
}
