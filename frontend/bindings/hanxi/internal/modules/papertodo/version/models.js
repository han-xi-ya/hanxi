// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * DownloadProgress 下载过程实时进度（事件 papertodo:version-download 载荷）。
 * @typedef {Object} DownloadProgress
 * @property {string} version - 目标版本
 * @property {string} stage - resolve/downloading/verify/done/error（单 exe 无解压阶段）
 * @property {number} done - 已下载字节
 * @property {number} total - 总字节（未知为 0）
 * @property {string} message - 附加信息（如错误描述）
 */

/**
 * PaperAsset 单个发布资产的下载要素。
 * SHA256 为 GitHub API digest（官方计算）。实证：PaperTodo 当前所有 release
 * 资产均无 digest 字段——存在时（GitHub 逐批回刷后）Download 会升级为官方哈希硬校验，
 * 缺失时降级为"字节数 + PE 版本核对 + sha256 指纹存档"链（详见 manager 注释）。
 * @typedef {Object} PaperAsset
 * @property {string} name - 资产文件名（原样用于拼接上游下载 URL）
 * @property {number} size - API 声明字节数
 * @property {string} sha256 - 官方 sha256（可为空 = 上游未提供 digest）
 */

/**
 * PaperRelease 远程可用的 PaperTodo Windows x64 GA 版本（双变体齐备才入列表）。
 * @typedef {Object} PaperRelease
 * @property {string} version - tag 原样，如 v3.31
 * @property {string} published - 发布时间（RFC3339）
 * @property {PaperAsset} selfContained - 完整版资产
 * @property {PaperAsset} noRuntime - 精简版资产
 */

/**
 * PaperVersionInfo 本地托管安装信息（固定单目录 versions/papertodo，至多一条）。
 * @typedef {Object} PaperVersionInfo
 * @property {string} version - 权威版本（hanxi-meta tag → PE FileVersion → vunknown 兜底）
 * @property {string} variant - self-contained / no-runtime / ""（导入安装无记录）
 * @property {string} exePath - PaperTodo.exe 完整路径
 * @property {string} dir - 托管目录（exe 与 data.json 同目录）
 * @property {number} size - exe 字节数
 * @property {string} installedAt - 安装时间 yyyy-MM-dd HH:mm:ss
 * @property {boolean} isImport - 是否经「导入本地」安装（非远程下载）
 * @property {string} source - 导入来源目录 / 下载资产文件名
 * @property {boolean} hasData - 同目录是否存在 data.json 便签数据
 * @property {string} assetSha256 - 下载时计算的 sha256 指纹（审计/篡改感知）
 * @property {string} officialSha - 官方 digest（上游提供时非空，已硬校验）
 * @property {string} peVersion - PE FileVersion 资源（"" = 读取失败，仅核对降级）
 */

// In interface mode, this file is likely to contain just comments.
// We add a dummy export statement to ensure it is recognised as an ES module.
export {};
