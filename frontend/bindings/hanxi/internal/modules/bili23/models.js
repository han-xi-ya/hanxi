// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * ControlOutcome 打开窗口操作的执行结果说明。
 * @typedef {Object} ControlOutcome
 * @property {string} action - started（冷启动）/ opened（自有实例唤窗）/ external-opened（外部实例唤窗）
 * @property {boolean} external
 * @property {string} message - 面向用户的执行说明
 */

/**
 * QuitOutcome 退出执行结果。Bili23 特有：上游"关闭窗口"行为用户可配，
 * 退出按钮可能只换来"收入托盘"或"弹窗询问"，必须如实分叉上报而非假装已退出。
 * @typedef {Object} QuitOutcome
 * @property {boolean} stopped - 是否真正终止了自有实例
 * @property {boolean} external - true = 当前为外部实例，未越权终止
 * @property {boolean} hidden - true = 进程收入 Bili23 托盘（仍托管）
 * @property {boolean} asked - true = Bili23 弹出退出询问对话框，等待用户选择
 * @property {string} message - 面向用户的执行说明
 */

/**
 * Status 控制台聚合视图：引擎快照全字段平铺 + 窗口可见性 + 运行时长。
 * 刻意平铺而非内嵌 instance.Snapshot——规避 Wails v3 对内嵌结构的 TS 绑定生成歧义，
 * 前端拿到的就是单一扁平对象，一次 GetStatus 得到渲染所需的一切。
 * @typedef {Object} Status
 * @property {string} version
 * @property {string} state
 * @property {number} pid
 * @property {number} exitCode
 * @property {string} error
 * @property {boolean} external
 * @property {string} startedAt
 * @property {string} stoppedAt
 * @property {boolean} windowVisible - 自有实例是否有可见窗口（false=收入托盘/尚未开窗）
 * @property {number} uptimeSeconds - 自有实例已运行秒数（非 running 为 0）
 */

// In interface mode, this file is likely to contain just comments.
// We add a dummy export statement to ensure it is recognised as an ES module.
export {};
