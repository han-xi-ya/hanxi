// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * CPUInfo 处理器档案（HARDWARE 注册表 + 逻辑处理器枚举）。
 * @typedef {Object} CPUInfo
 * @property {string} name
 * @property {string} vendor
 * @property {number} speedMHz - 标称标频（registry ~MHz 为当前值，如实注明口径）
 * @property {number} cores
 * @property {number} logical
 * @property {number} sockets
 */

/**
 * DisplayInfo 活动显示器（EnumDisplayDevices/Settings：分辨率与刷新率为
 * 当前模式快照；EDID 品牌型号首版缺席，见包注释）。
 * @typedef {Object} DisplayInfo
 * @property {string} name - \\.\DISPLAY1 形态
 * @property {number} width
 * @property {number} height
 * @property {number} refreshHz
 * @property {number} colorBits
 * @property {boolean} primary
 */

/**
 * GPUInfo 显卡（display 设备类注册表逐项：驱动视角的硬件档案）。
 * @typedef {Object} GPUInfo
 * @property {string} desc
 * @property {string} provider
 * @property {string} driverVersion
 * @property {string} driverDate
 */

/**
 * MachineInfo 整机与固件档案（注册表 HARDWARE\DESCRIPTION\System\BIOS 直读，
 * 无需 WMI 的机型号路径）。
 * @typedef {Object} MachineInfo
 * @property {string} hostname
 * @property {string} manufacturer - OEM（联想/戴尔/组装件为主板厂）
 * @property {string} model - 整机型号 / 主板型号回落
 * @property {string} biosVendor - BIOS 厂商（LENOVO/American Megatrends…）
 * @property {string} biosVersion - BIOS 版本串
 * @property {string} productId - Windows 产品 ID（注册表，本地展示用）
 * @property {string} systemSku
 */

/**
 * MemoryInfo 内存容量与当前水位（GlobalMemoryStatusEx；"静态档案"里的
 * 唯一实时项——容量是静态的，占用是快照值，前端按"快照"语义呈现）。
 * @typedef {Object} MemoryInfo
 * @property {number} totalBytes
 * @property {number} availableBytes
 * @property {number} loadPercent
 * @property {number} commitTotal
 * @property {number} commitLimit
 */

/**
 * NetInfo 网络接口档案（标准库 net：MAC/状态/IP；不含网关与 DNS——
 * 网络诊断归 lan/publicip 模块，本表只给"这台机器有什么"）。
 * @typedef {Object} NetInfo
 * @property {string} name
 * @property {string} description
 * @property {string} mac
 * @property {number} mtu
 * @property {boolean} up
 * @property {boolean} loopback
 * @property {string[] | null} addresses
 */

/**
 * OSInfo 操作系统档案。ProductName 经构建号规一（注册表在 Win11 上仍写
 * "Windows 10 ..."的已知事实，规一逻辑带单测锁死）。
 * @typedef {Object} OSInfo
 * @property {string} productName
 * @property {string} edition
 * @property {string} version - 22H2 / 23H2 形态
 * @property {string} build - 22631.4317（BuildLabEx 优先，缺则 CurrentBuild）
 * @property {string} installDate
 * @property {boolean} is64Bit
 * @property {string} uptime - 人类可读（天/时/分）
 * @property {number} uptimeSeconds - 机器可读（前端轮询/排序用）
 */

/**
 * Report 一次全量档案快照（各段独立降级：单段采集失败其余照常送达）。
 * @typedef {Object} Report
 * @property {MachineInfo} machine
 * @property {OSInfo} os
 * @property {CPUInfo} cpu
 * @property {MemoryInfo} memory
 * @property {GPUInfo[] | null} gpus
 * @property {DisplayInfo[] | null} displays
 * @property {VolumeInfo[] | null} volumes
 * @property {NetInfo[] | null} network
 * @property {string[] | null} [errors] - 段级采集告警（空=全量成功；有告警如实透出前端）
 */

/**
 * VolumeInfo 逻辑卷档案（盘符/类型/容量/文件系统/卷标）。
 * @typedef {Object} VolumeInfo
 * @property {string} letter - "C:"
 * @property {string} label
 * @property {string} fileSystem
 * @property {string} type - fixed / removable / network / cdrom
 * @property {number} totalBytes
 * @property {number} freeBytes
 */

// In interface mode, this file is likely to contain just comments.
// We add a dummy export statement to ensure it is recognised as an ES module.
export {};
