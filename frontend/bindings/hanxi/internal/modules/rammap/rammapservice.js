// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * RAMMapService 面向前端的 RAMMap 托管服务：官方直链版本管理（日期版模型）
 * 与会话内启停。外部实例感知为拉取式（GetStatus 读取前复探）。
 * 
 * 提权现实（#17 三重契约，本模块与 rufus/litemonitor/bcu 同族）：
 * ① 未提权 Hanxi spawn 载荷必撞 740——引擎层改写为含"管理员"关键词的指引
 * 
 * 	文案（ElevateRestart 一键通道），RPC 错误如实上抛不谎报成功；
 * 
 * ② 提权态 Hanxi 对同完整性 RAMMap 实例治理完整可用；对外部**自启且被
 * 
 * 	UAC 提权**的实例，medium-IL Hanxi 的 WM_CLOSE/Terminate 被 UIPI 拦 →
 * 	externalquit 如实回 blocked 降级指引；
 * 
 * ③ 前端 stopped 引导行经 ElevationStatus RPC 预告提权要求（见该方法注释）。
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as instance$0 from "./instance/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as version$0 from "./version/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * DownloadVersion 异步下载最新版：立即返回 "started"；已装返回
 * "already-installed"、同版本下载中返回 "in-progress"。进度经
 * "rammap:version-download" 事件推送；全程持后台租约事务（P0 批 2b）。
 * version 入参传空串 = 上游当前最新版。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function DownloadVersion(targetVersion) {
    return $Call.ByID(4217814487, targetVersion);
}

/**
 * ElevationStatus 提权预告（三重契约之③）：载荷 manifest 强制管理员是上游
 * 事实；宿主未提权时前端引导行/启动钮 title 须如实告知"将以管理员重启
 * Hanxi 后才能启动"，别让用户首撞 740 才看懂。
 * @returns {$CancellablePromise<$models.StatusInfo>}
 */
export function ElevationStatus() {
    return $Call.ByID(373968874);
}

/**
 * GetActiveVersion 返回使用版本号（未设置为空串，error 恒 nil）。
 * @returns {$CancellablePromise<string>}
 */
export function GetActiveVersion() {
    return $Call.ByID(205790119);
}

/**
 * GetFollowOnExit 返回"随 Hanxi 退出一起关闭"开关值（默认 false）。
 * @returns {$CancellablePromise<boolean>}
 */
export function GetFollowOnExit() {
    return $Call.ByID(2126338055);
}

/**
 * GetStatus 返回引擎状态快照；读取前做一次外部探针校正（拉取式感知）。
 * @returns {$CancellablePromise<instance$0.Snapshot>}
 */
export function GetStatus() {
    return $Call.ByID(3609421133);
}

/**
 * ImportLocal 导入本机已有 RAMMap 目录为托管版本；未设使用版本时自动激活。
 * @param {string} srcDir
 * @returns {$CancellablePromise<version$0.RammapVersionInfo>}
 */
export function ImportLocal(srcDir) {
    return $Call.ByID(1126193435, srcDir);
}

/**
 * ListInstalledVersions 扫描本地已装版本目录。
 * @returns {$CancellablePromise<version$0.RammapVersionInfo[] | null>}
 */
export function ListInstalledVersions() {
    return $Call.ByID(1375591624);
}

/**
 * ListReleases 拉取"当前最新版"单条列表（HEAD 探测 Last-Modified 日期版）。
 * @returns {$CancellablePromise<version$0.RammapRelease[] | null>}
 */
export function ListReleases() {
    return $Call.ByID(2222552439);
}

/**
 * OfficialSiteURL / OpenOfficialSite 官方工具页入口。
 * @returns {$CancellablePromise<string>}
 */
export function OfficialSiteURL() {
    return $Call.ByID(428607592);
}

/**
 * OpenDir 用资源管理器打开版本目录（先校验存在）。
 * @param {string} dir
 * @returns {$CancellablePromise<void>}
 */
export function OpenDir(dir) {
    return $Call.ByID(87075124, dir);
}

/**
 * OpenOfficialSite 在默认浏览器打开官方工具页。
 * @returns {$CancellablePromise<void>}
 */
export function OpenOfficialSite() {
    return $Call.ByID(421159995);
}

/**
 * OpenWindow 窗口唤起编排（多实例观察工具，见 instance 包注释）：
 *   - running：聚焦自有实例主窗口；
 *   - external：唤回用户自开窗口（RAMMap 窗口无最小化常驻语义，聚焦即达；
 *     无可聚焦窗口如实回指引——二次拉起只会另开一个新窗，不是唤回，
 *     不做"假装唤回"的把戏）；
 *   - stopped/failed：解析 active 版本冷启动（多实例共存无障碍，无需
 *     launch gate）。
 * @returns {$CancellablePromise<$models.ControlOutcome>}
 */
export function OpenWindow() {
    return $Call.ByID(3067903485);
}

/**
 * Quit 退出实例：自有走分层退出（WM_CLOSE→宽限→Job 兜底）；外部按
 * force-free 档（优雅优先，不生效直接强杀不打扰——零状态观察工具损失≈0；
 * 自启且被提权的外部实例 UIPI 拦截，如实降级指引）。
 * @returns {$CancellablePromise<$models.QuitOutcome>}
 */
export function Quit() {
    return $Call.ByID(3346313098);
}

/**
 * RemoveVersion 删除本地版本；本会话运行中或为当前使用版本时拒绝。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<void>}
 */
export function RemoveVersion(targetVersion) {
    return $Call.ByID(1111102931, targetVersion);
}

/**
 * SetActiveVersion 切换使用版本（ResolveExe 校验载荷在场后落盘）。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function SetActiveVersion(targetVersion) {
    return $Call.ByID(1972564923, targetVersion);
}

/**
 * SetFollowOnExit 设定开关（下次启动生效）。
 * @param {boolean} next
 * @returns {$CancellablePromise<string>}
 */
export function SetFollowOnExit(next) {
    return $Call.ByID(2023871515, next);
}

/**
 * Shutdown RPC：经调用门执行收尾（与内部版 shutdown 同语义）。
 * @returns {$CancellablePromise<void>}
 */
export function Shutdown() {
    return $Call.ByID(3736850991);
}
