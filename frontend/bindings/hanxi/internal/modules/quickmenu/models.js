// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * MenuItem 轮盘的一个扇区条目：复用托盘 TrayMenu 配置（同一份条目、同一套分发），
 * Index 为当前层展示序下标，前端点击经 Launch(path) 回传完整路径。
 * Type 为 group 且 Children 非空时该扇区可展开二级盘（二级轮盘关闭时后端已把
 * 子条目拍平到主盘，前端看到的数据形态天然只随开关变化，无需感知配置）。
 * @typedef {Object} MenuItem
 * @property {number} index
 * @property {string} label - 已解析显示名（自定义名缺省回退）
 * @property {string} type - exe | command | route | group
 * @property {string} hint - 辅助说明：exe 路径 / 命令与页面的引用键；group 留空
 * @property {string} icon - 前端图标名（AppIcon 注册表，按类型解析，未知名前端回退）
 * @property {MenuItem[] | null} children - group 的二级条目（非组为空切片，绑定侧恒在）
 */

/**
 * Skin 轮盘皮肤账（机主拍板 2026-09-26"轮盘皮肤做"）：纯视觉偏好入后端账，
 * 皮肤随 hanxidata 数据目录 NAS 同步随身。前端呈现侧归一语义与此同源
 * （wheelSkin.ts：预设白名单/数值钳域，坏值归正不报错）：Preset ∈ frost|veil|ink；
 * FaceAlpha 为盘纱不透明度整数百分比（有效域 35–100，再透桌面噪点透出、
 * 盘面读不出"一块盘"）；Stroke 为扇区描边强度 0–100；FollowModuleColor 开启时
 * 描边改用条目真图标主色（灰图标/矢量轨回落类型色）。
 * @typedef {Object} Skin
 * @property {string} preset
 * @property {number} faceAlpha
 * @property {number} stroke
 * @property {boolean} followModuleColor
 */

/**
 * Status 快捷菜单运行态（模块页展示 + 开关回显）。
 * @typedef {Object} Status
 * @property {boolean} trapActive - 全局鼠标钩子是否在位
 * @property {number} holdMs - 触发所需按住时长（ms）
 * @property {number} moveTol - 抬手前允许的光标位移（物理像素）
 * @property {number} itemCount - 当前主盘扇区数（含分组节点；二级轮盘关闭时为拍平后的叶子数）
 * @property {boolean} twoTier - 二级轮盘是否启用（分组展开为子盘，否则拍平）
 */

// In interface mode, this file is likely to contain just comments.
// We add a dummy export statement to ensure it is recognised as an ES module.
export {};
