// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * Record 一条历史记录。四文本字段（Summary/Input/Output/Extra）在 Save 入口
 * 统一过脱敏与截断，调用方直接传原文即可。
 * @typedef {Object} Record
 * @property {number} id - ID 全局单调递增：时间戳毫秒*1000 起步，与桶内已见最大值取严格递增， 重启后天然续接（时钟回拨也单调）。
 * @property {string} funcType - FuncType 分桶键："ocr" / "portkill" / "envcheck" …（对齐模块注册 ID）。
 * @property {string} summary - Summary 一行摘要；留空则 Save 时取 Input 前 40 字（对齐 MooTool 口径）。
 * @property {string} input - Input 输入侧留档（如图片路径、端口号、工具 ID），截断上限 4000 rune。
 * @property {string} output - Output 输出侧留档（如识别全文、错误信息），截断上限 4000 rune。
 * @property {string} extra - Extra 元信息标记，如 "snip"、"npm-install|fail"、"elevated|denied"。
 * @property {string} createdAt - CreatedAt RFC3339；留空则 Save 时补当前时间。
 */

/**
 * Store 历史记录存储：内存权威副本 + 每次变更全量原子覆写落盘。
 * 所有公开方法整体持锁（单用户桌面写入频率远低于全量写成本，一把锁收口最简）；
 * 损坏（loadErr 驻留）后禁一切写，读侧返回错误供面板提示。
 * @typedef { {
 * } } Store
 */

// In interface mode, this file is likely to contain just comments.
// We add a dummy export statement to ensure it is recognised as an ES module.
export {};
