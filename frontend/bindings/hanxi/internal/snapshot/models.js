// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * FileDiff 单文件新旧对照（行 diff 在前端算，后端只回原样文本，N33 §4）。
 * Old/New 各按 maxPreviewBytes 截断，对应 Truncated 标记如实置位。
 * @typedef {Object} FileDiff
 * @property {string} path
 * @property {string} status - 该版本对此文件的变化类型
 * @property {string} old
 * @property {string} new
 * @property {boolean} oldTruncated
 * @property {boolean} newTruncated
 * @property {string} summary - 该版整体摘要
 */

/**
 * FilePreview 单文件内容预览（≤512KB 文本，超出截断）。
 * @typedef {Object} FilePreview
 * @property {string} path
 * @property {string} content
 * @property {number} size
 * @property {boolean} truncated
 */

/**
 * FileRevision 文件时间线的一行（该文件在此版本发生变化）。
 * @typedef {Object} FileRevision
 * @property {string} revisionId
 * @property {string} time - RFC3339
 * @property {string} status - A/M/D/R（备份模式由相邻 manifest 差集读时算出，N33 §3）
 * @property {string} summary - 该版整体摘要，可空→前端回落 status 词表
 */

/**
 * Preferences 分区内可调偏好（落 settings.Store 持久化）。
 * @typedef {Object} Preferences
 * @property {boolean} enabled
 * @property {number} idleSeconds
 * @property {number} intervalMinutes
 */

/**
 * Revision 历史版本列表行。ID 为 git commit hash 或影子备份时间戳目录名，
 * 对 PreviewFile/RestoreFile 不透明回传。
 * @typedef {Object} Revision
 * @property {string} id
 * @property {string} time - RFC3339
 * @property {string} summary - "config.json, memo/xxx.md 等 N 个文件"
 */

/**
 * RevisionFile 某个版本包含的文件及其变化类型（git 语义首字母 M/A/D/R；备份模式恒 M）。
 * @typedef {Object} RevisionFile
 * @property {string} path
 * @property {string} status
 */

/**
 * StatusInfo "历史版本"分区顶部状态行。
 * @typedef {Object} StatusInfo
 * @property {boolean} enabled
 * @property {string} mode - "git" | "backup"（引擎实际生效模式；未启动为空）
 * @property {boolean} gitAvailable - 探测结论（false 时恒为 backup）
 * @property {string} lastCommitAt - 最近一次成功提交 RFC3339；空=本进程从未提交
 * @property {number} revisionCount - 历史版本份数
 * @property {number} idleSeconds - 生效的空闲阈值
 * @property {number} intervalMinutes - 生效的最小提交间隔
 * @property {string} snapshotDir - .snapshots 目录绝对路径（展示/打开用）
 */

/**
 * TrackedFile 受保文件清单行（N33 批 A，文件为轴左栏）。
 * Display 回落文件名——便签标题经装配根注入的 resolver 映射，config/state 的
 * 中文名表在批 C 落地；Revisions 口径受 maxListRevisions 观察窗约束，如实标注。
 * @typedef {Object} TrackedFile
 * @property {string} path - 白名单相对路径（RPC 往返用）
 * @property {string} display - 中文名（映射不到回落文件名）
 * @property {string} group - "memo" | "config" | "state"
 * @property {number} revisions - 观察窗内该文件的历史条数（0=从未入版）
 * @property {string} lastChange - RFC3339；观察窗内该文件最近一次变化
 * @property {boolean} alive - 磁盘上当前是否还在（false=已删除，仍可见历史）
 */

// In interface mode, this file is likely to contain just comments.
// We add a dummy export statement to ensure it is recognised as an ES module.
export {};
