// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * MarkerOnService 向前端暴露 MarkerOn 版本管理与标注开关能力。
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as instance$0 from "./instance/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as version$0 from "./version/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * CreateDesktopShortcut 在桌面为当前使用版本创建快捷方式（同名覆盖）。
 * @returns {$CancellablePromise<void>}
 */
export function CreateDesktopShortcut() {
    return $Call.ByID(3925020656);
}

/**
 * DownloadVersion 后台下载指定版本：立即返回，全程经事件 markeron:version-download 推送进度。
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function DownloadVersion(targetVersion) {
    return $Call.ByID(2571289958, targetVersion);
}

/**
 * GetActiveVersion 返回当前设定版本（空字符串 = 未指定，冷启动自动用最新已装）
 * @returns {$CancellablePromise<string>}
 */
export function GetActiveVersion() {
    return $Call.ByID(2712815708);
}

/**
 * GetFollowOnExit 返回"随 HubKit 退出一起关闭"开关值（默认 true）。
 * @returns {$CancellablePromise<boolean>}
 */
export function GetFollowOnExit() {
    return $Call.ByID(3055385814);
}

/**
 * GetStatus 返回引擎当前状态快照（先做一次静止态外部校正，弥补 5s 轮询间隙的即时性）
 * @returns {$CancellablePromise<instance$0.Snapshot>}
 */
export function GetStatus() {
    return $Call.ByID(3203714152);
}

/**
 * ListInstalledVersions 获取本地已安装版本列表
 * @returns {$CancellablePromise<version$0.MarkerVersionInfo[] | null>}
 */
export function ListInstalledVersions() {
    return $Call.ByID(466486481);
}

/**
 * ListReleases 获取远程可用版本列表（多镜像回退，10 分钟缓存）
 * @returns {$CancellablePromise<version$0.MarkerRelease[] | null>}
 */
export function ListReleases() {
    return $Call.ByID(1669086612);
}

/**
 * OpenRepository 用默认浏览器打开上游仓库页面。
 * @returns {$CancellablePromise<void>}
 */
export function OpenRepository() {
    return $Call.ByID(16251378);
}

/**
 * RemoveVersion 卸载指定版本（正在运行的版本拒绝卸载）
 * @param {string} targetVersion
 * @returns {$CancellablePromise<void>}
 */
export function RemoveVersion(targetVersion) {
    return $Call.ByID(3662718674, targetVersion);
}

/**
 * RepositoryURL 上游 GitHub 仓库地址（页面展示与复制）。
 * @returns {$CancellablePromise<string>}
 */
export function RepositoryURL() {
    return $Call.ByID(2347212051);
}

/**
 * SetActiveVersion 设定使用版本（先校验已安装，再持久化）
 * @param {string} targetVersion
 * @returns {$CancellablePromise<string>}
 */
export function SetActiveVersion(targetVersion) {
    return $Call.ByID(61158640, targetVersion);
}

/**
 * SetFollowOnExit 设定开关（下次启动生效）。
 * @param {boolean} b
 * @returns {$CancellablePromise<void>}
 */
export function SetFollowOnExit(b) {
    return $Call.ByID(338896202, b);
}

/**
 * Shutdown 模块停用/应用退出：停后台轮询 + 终止自有实例。
 * 外部实例不受影响（非我方托管）；自有实例另受 JobObject KILL_ON_JOB_CLOSE 内核兜底。
 * @returns {$CancellablePromise<void>}
 */
export function Shutdown() {
    return $Call.ByID(3206428140);
}

/**
 * StopAnnotate 停止引擎托管的 MarkerOn。
 * external 状态不越权强杀（互斥体探测拿不到 PID）：仅返回人性化指引。
 * @returns {$CancellablePromise<$models.StopOutcome>}
 */
export function StopAnnotate() {
    return $Call.ByID(2059572302);
}

/**
 * ToggleAnnotate 标注开关编排中枢：
 *   - external：任一已装 exe 充当单实例信使，触发外部主实例 toggle_drawing；
 *   - running：自有实例直接翻转（启动后极短时间内先补足消息窗口注册缓冲）；
 *   - stopped/failed：仅启动到后台运行（不自动进入标注）——进入/退出标注交给下一次点击或 Ctrl+Shift+D。
 * @returns {$CancellablePromise<$models.ToggleOutcome>}
 */
export function ToggleAnnotate() {
    return $Call.ByID(310110558);
}
